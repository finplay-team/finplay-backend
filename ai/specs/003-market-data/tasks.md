# Tasks: 종목과 시세

> **MVP 주식 시세 방향 (확정)**: 직전 영업일 분봉을 KIS Open API REST로 08:10에 수집하고 09:00부터 재생하는 경로 **하나뿐**이다. KIS 실시간 WebSocket 체결 수신·틱 집계는 MVP에서 쓰지 않으며, **그것을 고르기 위한 설정 전환 구조(`StockFeedConfig`·`StockFeedProvider`·`ServiceExposure`·`stock-feed.*`)도 함께 삭제한다** — 실시간 경로 방어 외에 참조가 없어 좀비 코드가 되기 때문이다. 전환 구조와 fail-fast는 KIS 실시간 후속에서 실시간 구현체와 **함께** 되살린다. 배경과 근거는 `spec.md` 개요·MKT-007, `plan.md`의 "KIS 과거 분봉 수집 설계"·"배치 실행 시각"·"리네이밍"·"실행 환경 조합" 참조.

## 완료된 항목

- [x] instruments 마이그레이션 + 16+12종 시드 + 엔티티·Repository + 종목 목록 API `GET /api/instruments?market=` (+ @DataJpaTest·@WebMvcTest) — 이슈 #14
- [x] 종목 단건 조회 API `GET /api/instruments/{instrumentId}` (+ @WebMvcTest) — 이슈 #15
- [x] stock_candles 마이그레이션(UNIQUE(instrument_id, trading_date, candle_time), validation_status 컬럼 없음) + 엔티티·Repository — 이슈 #16 (가격 조회 최소 스키마. 재생 Provider가 읽어 가격 계약을 만족시키는 데 필요 — 실물 데이터 대신 테스트 픽스처로 시딩)
- [x] stock_replay_sessions 마이그레이션(UNIQUE(service_date), preparation_status는 PREPARING·READY·FAILED 3개만, resolved_at·source_trading_date·failure_reason은 nullable — prepared_at이 아닌 resolved_at 이름 사용) + 엔티티·Repository — 이슈 #16 (시장상태 OPEN/CLOSED·sourceTradingDate 계산에 필요 — 세션 값은 테스트 픽스처로 시딩, 실제 전환은 아래 Scheduler 항목에서)
- [x] StockReplayService (StockReplaySession 읽기 전용 + Clock으로 OPEN·CLOSED 계산, 첫 분봉 시가/이후 마감 종가, 공휴일 목록, 재생세션 DB 쓰지 않음) (+ 단위 테스트) — 이슈 #16
- [x] StockPriceProvider 인터페이스 + 재생 Provider(StockReplayService 위임, 공개 기본) + StockFeedConfig (STOCK_FEED_PROVIDER·SERVICE_EXPOSURE·KIS_PUBLIC_DISPLAY_APPROVED로 빈 1개 선택, PUBLIC+KIS_REALTIME+미승인은 기동 실패) (+ 단위 테스트: 허용 조합 Provider 선택, 금지 조합 컨텍스트 로드 실패, KIS 키 없이 재생 Provider 기동 성공) — 이슈 #16. **이 중 `StockFeedConfig`(+ `StockFeedProvider`·`ServiceExposure`·`stock-feed.*`)는 아래 이슈 #19 ①에서 삭제한다** — 실시간 경로를 방어하기 위해서만 존재하는 코드라, 실시간 구현체가 빠지면 좀비가 된다. 남는 것은 `StockPriceProvider` 인터페이스와 재생 구현체뿐이며, 구현체는 ②에서 `KisHistoricalReplayPriceProvider`로 리네이밍한다
- [x] PriceStore (Redis 코인 최신 시세·과거 틱 무시, stale 10초) + BithumbFeedClient 인터페이스·Fake 구현 (+ 단위 테스트) — 이슈 #16
- [x] PriceQueryService 유효성 판정 (주식은 주입된 StockPriceProvider만 사용 — 구현체를 알지 못함) (+ 단위 테스트) — 이슈 #16
- [x] 가격 API `GET /api/instruments/{instrumentId}/price` (+ @WebMvcTest: 401, 404, 409, 200 계약) — 이슈 #16
- [x] 캔들 API `GET /api/instruments/{instrumentId}/candles?interval=1m&from=&to=` (StockPriceProvider에 1분봉 조회 메서드 추가 + 재생 구현체에 구현, CandleInterval 검증 — 1m만 허용·그 외 400 VALIDATION_ERROR, CandleResponse DTO + StockCandle 매핑, StockCandleRepository 시각 범위 조회, from·to ISO-8601·from > to면 400·둘 다 생략 시 재생 중인 거래일 전체, 아직 마감하지 않은 분봉 미노출, 재생세션 미준비면 200 빈 배열, 코인 instrumentId는 400) (+ 단위 테스트·@DataJpaTest·@WebMvcTest) — 이슈 #17
- [x] 캔들 API 통합 테스트 (픽스처 분봉 + 고정 Clock으로 장중·마감 후 시나리오 검증) + ai/api-routes.md·docs/api-contracts.md 갱신 — 이슈 #17
- [x] SSE 공통 골격 — `@EnableScheduling` + `spring.mvc.async.request-timeout` 설정, SSE 이벤트 DTO 3종(`snapshot`/`price`/`status`), `SseEmitterRegistry`(market별 emitter 관리·`retry: 3000`·20초 heartbeat·콜백 정리), `PriceQueryService`에 예외를 던지지 않는 조회 경로 추가 (+ 단위 테스트) — 이슈 #18
- [x] `/api/stocks/stream` SSE (`StockPriceSseController`, fetch+Bearer 인증, `SseEmitterRegistry.register(STOCK)`, 구독 직후 주식 16종 전체 `snapshot` 1건 — 가격 없는 종목도 UNAVAILABLE로 포함, id는 `price` 이벤트에만, 매분 새로 공개된 가격 push, 장 마감 후 마지막 유효가격 유지, marketStatus와 종목별 status 분리) (+ @WebMvcTest) — 이슈 #19 **(PR #94에 구현 완료 — 방향 맞음, 유지)**
- [x] 코인 1분봉 차트 연동 (MKT-008) — `CryptoCandleProvider` 인터페이스 + `BithumbRestCandleProvider`(빗썸 공개 캔들 REST `GET /v1/candles/minutes/1`, 심볼→`KRW-{symbol}` 변환, `from`·`to`→`to`+`count` 변환·최대 200개, 빗썸 내림차순→시각 오름차순 반전, **진행 중인 분봉 포함**(주식과 반대 — 코인은 실시간이라 가릴 대상이 없다. StockReplayService의 공개 컷오프 로직을 재사용하지 않는다), `trade_price`→`close`·`candle_acc_trade_volume`→`volume` 매핑, 연결·읽기 타임아웃 설정) + `FakeCryptoCandleProvider`(자동 테스트용) + `CandleQueryService` 시장 분기(CRYPTO는 CryptoCandleProvider 위임, 기존 "코인이면 400 VALIDATION_ERROR" 거부 제거) + `CandleResponse.volume`을 long→BigDecimal로 확대 + `ErrorCode.MARKET_DATA_PROVIDER_ERROR`(502) 추가. 저장·캐시 없음 — 마이그레이션·Redis 키 추가 금지 (+ 단위 테스트: 정렬 반전, 진행 중 분봉 포함(주식과 반대라는 것을 고정), 필드 매핑(거래대금과 수량 혼동 없음), 심볼 변환, from/to 조합별 count 산출·200 캡, 소수 volume 미절단, 타임아웃·비정상 상태코드·파싱 불가 시 502 발생(빈 배열 아님), Redis·MySQL 미기록, 주식 캔들 회귀(volume 확대 후 값·정렬·공개 컷오프·200 빈 배열 유지), 코인 캔들 경로와 현재가 경로의 독립성 / @WebMvcTest: 401, 코인 200, interval 오류 400, 빗썸 실패 502) + ai/api-routes.md·docs/api-contracts.md 같은 커밋 갱신 — 이슈 #20 (실제 빗썸 REST 조회는 외부 스모크로 구분 보고)

## 이슈 #19 — 남은 작업 (구현 순서)

- [x] **① 실시간 코드와 공급자 전환 설정 통째로 삭제** — 좀비 코드를 남기지 않는 것이 목적이다. 실측 결과 `StockFeedProvider`·`ServiceExposure`·`stock-feed.*`는 `StockFeedConfig`와 그 전용 테스트 2개 밖에서 전혀 참조되지 않아, 실시간 구현체가 빠지면 뒤에 아무것도 연결되지 않은 스위치만 남는다.
  - **삭제**: `KisRealtimePriceProvider`·`KisTickAggregator`·`FakeKisRealtimePriceProvider` (+ 테스트 `KisTickAggregatorTest`·`FakeKisRealtimePriceProviderTest`) / `StockFeedConfig`·`StockFeedProvider`·`ServiceExposure` (+ 테스트 `StockFeedConfigTest`·`StockFeedConfigContextTest`) / `application.yml`의 `stock-feed:` 블록 / `.env.example`의 `STOCK_FEED_PROVIDER`·`SERVICE_EXPOSURE`·`KIS_PUBLIC_DISPLAY_APPROVED`.
  - **유지**: `StockPriceProvider` 인터페이스 — `PriceQueryService`·`CandleQueryService`·`StockPriceStreamService`가 실제로 의존하는 이음매이므로 좀비가 아니다. 구현체는 하나뿐이므로 설정으로 고르지 않고 **`@Service`로 직접 등록**한다.
  - **유지**: `KIS_APP_KEY`·`KIS_APP_SECRET` — 이제 REST 과거분봉 수집 배치가 쓴다. `.env.example` 주석의 "`KIS_REALTIME`일 때만 필요"를 "과거 분봉 수집 배치가 사용"으로 고친다. 계좌번호 계열·WebSocket URL은 실시간 전용이므로 함께 정리.
  - (+ 회귀 확인: `KisHistoricalReplayPriceProvider`가 `@Service`로 등록돼 `PriceQueryService`가 정상 주입받고 기존 가격·캔들·SSE 테스트가 그대로 통과. 삭제 후 `src/`에 `StockFeedProvider`·`ServiceExposure`·`stock-feed` 참조가 하나도 남지 않는지 grep으로 확인. `KIS_REALTIME` 선택 시 기동 성공/실패를 다루는 테스트는 전부 사라진다 — 설정 자체가 없으므로 검증 대상이 아니다)
- [x] **② 클래스 리네이밍** — `KrxReplayPriceProvider` → `KisHistoricalReplayPriceProvider`. 참조 테스트(`PriceQueryServiceTest`·`StockCandleRepositoryTest`·`CandleQueryServiceIntegrationTest`)를 같은 커밋에서 함께 고친다. **설정값 `KRX_REPLAY`→`KIS_HISTORICAL` 리네이밍은 ①에서 enum·프로퍼티가 삭제되므로 불필요해졌다** — 남는 것은 클래스명 하나뿐이라 `application.yml`·`.env.example` 변경도 없다. DB 스키마·API 응답 계약에도 이 이름이 노출되지 않으므로 마이그레이션·api-contracts 변경 없음. (+ 기존 테스트 전체가 회귀 없이 통과하는지 확인 — 새 테스트는 추가하지 않는다)
- [x] **③ market_data_imports 마이그레이션 + 엔티티 + Repository** — `V11__create_market_data_imports.sql` (id PK, source, source_trading_date DATE, collected_at, status(SUCCESS·PARTIAL_SUCCESS·FAILED·SKIPPED_DUPLICATE), failure_reason, 인덱스(source_trading_date)). 현재 최신이 `V10__create_order_ledger_tables.sql`이므로 다음 번호는 11 (ADR-0004). 엔티티 `MarketDataImport` + `MarketDataImportRepository`(source_trading_date 기준 조회). (+ @DataJpaTest: 저장·조회, status 4종 매핑, source_trading_date 조회)
- [x] **④ KisHistoricalCandleClient (REST 호출·응답 파싱 격리 지점)** — 인터페이스 + HTTP 구현 + 테스트용 Fake. `/uapi/domestic-stock/v1/quotations/inquire-time-dailychartprice`(`tr_id: FHKST03010230`)를 `FID_COND_MRKT_DIV_CODE=J`·`FID_INPUT_ISCD`·`FID_INPUT_DATE_1`(직전 영업일)·`FID_INPUT_HOUR_1`로 호출하고, **한 번에 최대 120건** 제약 때문에 09:00~15:30(390분)을 종목당 약 4회 연속 호출해 이어붙인다. `output2` → 내부 분봉 DTO 매핑을 이 클래스 한 곳에만 둔다 — `output2` 개별 필드명과 timestamp 기준(구간 시작/종료)은 **Decision Gate**이므로 임의 해석하지 말고, 실제 스모크 후 이 지점만 교정할 수 있게 격리한다. 앱키·시크릿이 없어도 애플리케이션 기동과 빌드는 성공해야 한다. (+ 단위 테스트: Fake 응답으로 연속 호출 페이징 경계(120건 단위), 구간 이어붙이기, 응답 → 분봉 DTO 매핑, 빈 응답 처리)
- [x] **⑤ KisHistoricalCandleCollector (평일 08:10 KST)** — `KisHistoricalCandleClient`가 가져온 직전 영업일 분봉을 명백한 오류만 검증(MVP 허용 16종 여부, 필수 필드, 종목코드 형식, 거래일·분봉시각 형식, 동일 종목·거래일·분봉시각 중복, 분봉시각의 시장 시간 범위, 가격·거래량 음수 아님, `high >= open/close/low`·`low <= open/close`, 조회 대상 거래일과 응답 거래일 일치) → 정규화 → `StockCandle` 저장 + `MarketDataImport` 이력 기록. 전체 응답 오류는 미저장·FAILED, 특정 종목만 구조 오류면 그 종목 행만 거래일 단위로 미저장·나머지 저장·PARTIAL_SUCCESS(문제 종목·원인 포함). `UNIQUE(instrument_id, trading_date, candle_time)` 기반 재실행 멱등. **`StockReplaySession`을 직접 생성·수정하지 않는다.** 분봉 수가 적다는 이유만으로 손상 판정하지 않는다(임계치는 Decision Gate — 임시 숫자도 넣지 않는다). `@Scheduled` cron으로 평일 08:10, 테스트는 Clock 제어. (+ 단위 테스트: 정상 저장·SUCCESS, 전체 응답 오류→미저장·FAILED, 종목별 구조 오류→그 종목만 미저장·PARTIAL_SUCCESS, 재실행 멱등, StockReplaySession 미변경, 08:10 실행 시나리오)
- [x] **⑥ StockReplaySessionScheduler (평일 08:40 KST)** — `MarketDataImport`·`StockCandle`의 수집 결과를 확인해 오늘의 `StockReplaySession`을 생성하고 `source_trading_date`를 고정한다. 검증 완료된 **최신 거래일**을 고르며, 직전 영업일 데이터가 아직 없으면 그 전 영업일로 자연 폴백한다(별도 폴백 분기를 추가하지 않는다). 고를 거래일이 없으면 `FAILED` → 그날 주식 시장 미개장. `preparation_status`는 `PREPARING`→`READY`/`FAILED`로만 전환하고 **`StockCandle`을 직접 저장하지 않는다.** 상태별 nullable 규칙은 애플리케이션 검증으로 강제(DB CHECK 제약 없음). `@Scheduled` cron으로 평일 08:40, 테스트는 Clock 제어. (+ 단위 테스트: PREPARING→READY, PREPARING→FAILED, 폴백 선택, StockCandle 미변경, nullable 규칙 — PREPARING+resolved_at 존재/PREPARING+failure_reason 존재/READY+source_trading_date 없음/READY+resolved_at 없음/READY+failure_reason 존재는 거부, FAILED+resolved_at 없음/FAILED+failure_reason 없음은 거부, FAILED+source_trading_date NULL·존재는 둘 다 허용)
- [x] **⑦ 수집·재생 파이프라인 통합 테스트** (Testcontainers MySQL+Redis) — Fake KIS 응답으로 08:10 수집 → 08:40 세션 READY 전환 → 09:00 재생 → 가격 조회(첫 분봉 시가, 09:01 이후 마감 종가), 서버 재시작 시나리오에서 같은 원본 거래일 유지, DB에 OPEN·CLOSED가 저장되지 않음 확인, 전체 응답 오류 시 세션 FAILED·미개장, 특정 종목만 오류 시 나머지 종목 정상 개장, 화면 가격(SSE·가격 API)과 모의 주문 체결가가 같은 Provider에서 나오는지 확인.

## 이슈 #104 — 코인 시세 피드가 기동하지 않아 매수·매도가 상시 실패하는 문제 수정

> **범위 확인**: MKT-003·MKT-004는 spec.md·PRD 어디에도 "MVP 범위 아님" 표시가 없다 — 이전 tasks.md가 실제 `BithumbFeedClient` 구현을 "후속 이슈(MVP 범위 아님)"에 이슈 번호 없이 잘못 끼워 넣은 것을 이슈 #104에서 바로잡는다. `FakeBithumbFeedClient`는 "테스트·로컬용" 더블로 설계됐지만(클래스 주석), `start()`·`emitTick()`을 호출하는 컴포넌트가 어디에도 없어 `!prod`·`prod` 모두 코인 시세가 항상 `PRICE_UNAVAILABLE`이 된다(실측 확인, 이슈 본문 참조).

- [x] **⑧ BithumbFeedLifecycle** — 활성 `BithumbFeedClient` 빈 1개(구현체가 무엇인지 모른다 — 기존 이음매 원칙)의 생명주기를 애플리케이션과 맞춘다. `ApplicationReadyEvent`에서 `start()`를 1회 호출하고, `@PreDestroy`에서 `stop()`을 호출한다. 이 컴포넌트가 없으면 `PriceStore`의 `feed:crypto:status`가 fail-closed 기본값(`DISCONNECTED`)에서 영원히 벗어나지 못한다. (+ 단위 테스트: 목 `BithumbFeedClient`로 `ApplicationReadyEvent` 처리 시 `start()` 1회, 빈 파괴 시 `stop()` 1회 호출 검증)
- [x] **⑨ BithumbFeedSimulator (로컬·데모 전용)** — `@Profile("!prod")` + 프로퍼티(`bithumb.feed.simulate.enabled`, 기본 `true`)로 켜지는 컴포넌트. 시딩된 코인 종목 전체를 대상으로 `@Scheduled(fixedRate=...)`로 주기적인 합성 틱을 `FakeBithumbFeedClient.emitTick()`에 주입해, 실제 빗썸 연결 없이도 로컬 실행(`./gradlew bootRun`)에서 코인 현재가·주문이 정상 동작하게 한다. **자동 테스트 전체 스프링 컨텍스트에서는 반드시 비활성화한다.** 시행착오 3단계를 거쳐 확정했다(자세한 경위는 run-log.md 참조) — ① `src/test/resources/application.yml`로 오버라이드: Gradle 테스트 클래스패스 우선순위 때문에 `src/main/resources/application.yml` 전체가 가려져 폐기. ② `TestcontainersConfiguration`의 `@DynamicPropertySource`: `@Import`된 설정 클래스의 메서드는 Spring이 수집하지 않아 무효(PR #110 리뷰로 확인). ③ 대안으로 시도한 `DynamicPropertyRegistrar` 빈도 프로퍼티 등록이 `@ConditionalOnProperty` 평가 시점보다 늦어 마찬가지로 무효(바이트코드로 재확인). **최종**: `build.gradle`의 `test` 태스크에 `systemProperty 'spring.config.additional-location', 'classpath:/bithumb-feed-simulator-disabled-for-tests.yml'`을 추가해, `bithumb.feed.simulate.enabled: false` 하나만 담은 전용 파일을 `@SpringBootTest` 부트스트랩(`ConfigDataEnvironmentPostProcessor`)에만 얹는다 — 이 위치는 `ApplicationContextRunner` 기반 슬라이스 테스트(`BithumbFeedSimulatorConditionalTest`)에는 영향을 주지 않아 "프로퍼티 미설정 시 기본값" 케이스도 그대로 유지된다. `feed:crypto:status`(전역 키)를 직접 통제하는 기존 테스트(`AccountSummaryIntegrationTest`·`PortfolioSummaryIntegrationTest`의 CONNECTED/DISCONNECTED 시나리오)와 경쟁하지 않는다. (+ `BithumbFeedSimulatorDisabledIntegrationTest`로 `@SpringBootTest` 컨텍스트에 이 빈이 실제로 없는지 단언 — 격리 장치가 다시 조용히 무효화돼도 테스트가 잡아낸다.) (+ 단위 테스트: 시딩된 코인 종목마다 `emitTick` 호출 검증, 프로퍼티 `false`면 빈이 생성되지 않음)
- [x] **⑩ 실제 BithumbFeedClient — 빗썸 WebSocket 연동** (`@Profile("prod")`) — 빗썸 공개 WebSocket에 연결해 시딩된 코인 12종의 실시간 체결 틱을 구독하고 `PriceStore.saveTick()`으로 저장한다. 연결 성공 시 `CONNECTED`, 끊기면 `DISCONNECTED`로 전환하고 재연결을 시도한다(MKT-004). 메시지 파싱은 `KisHistoricalCandleClient`와 같은 원칙으로 이 클래스 한 곳에만 둔다. **빗썸 WebSocket ticker 메시지의 정확한 JSON 필드 구성은 실제 연결로 확인된 적이 없는 미확정 항목이다(Decision Gate — KIS `output2` 필드명과 같은 성격)** — 공개된 문서 기준 최선 추정으로 구현하고, 실제 연결 검증은 자동 테스트와 구분된 외부 스모크로 별도 보고한다(C-005, 임의 해석 금지 원칙은 유지하되 두절 상태로 방치하지 않기 위해 최선 추정 구현 후 스모크로 교정). 자동 테스트는 메시지 파싱 로직을 고정된 샘플 페이로드로 검증한다(샘플 자체의 기대 결과만 검증, production 기본값으로 쓰지 않음).

## 후속 이슈 (MVP 범위 아님)

> **KIS 실시간 항목에는 대응 GitHub 이슈가 없다.** 이전 문서가 "이슈 #82"로 적어 왔으나 실제 #82는 「[MVP][포트폴리오] 내 체결 내역 조회 API」이고 KIS 실시간과 무관하다 — 틱 집계 이슈는 만들어진 적이 없다(2026-07-31 이슈 #109에서 확인). 착수할 때 이슈를 새로 만들고, **그 시점에 `prd.md`·`spec.md`·`plan.md`·이 파일에 흩어진 "KIS 실시간 후속"·"후속(KIS 실시간 틱 집계)" 서술을 새 이슈 번호로 일괄 치환한다** — 대상은 `grep -rn "KIS 실시간 후속\|후속(KIS 실시간" docs/`로 찾는다. 아래 `이슈 #83` 항목은 실제 이슈와 일치하므로 번호를 그대로 둔다.

- [ ] **(KIS 실시간 후속) 공급자 전환 구조와 fail-fast 방어 복원 — 실시간 구현체와 반드시 같은 작업에서.** `StockFeedConfig`·`StockFeedProvider`(`KIS_REALTIME`·`KIS_HISTORICAL`)·`ServiceExposure`·`KIS_PUBLIC_DISPLAY_APPROVED`·`application.yml`의 `stock-feed:` 블록·`.env.example` 항목 3종을 되살리고, 허용 조합 4종과 `PUBLIC`+`KIS_REALTIME`+미승인 기동 실패(fail-fast)를 다시 구현한다. 계약 정본은 `plan.md`의 "실행 환경 조합" 절. **구현체만 먼저 들어오고 이 방어가 빠지면 공개 환경에서 무허가 실시간 표출이 가능해진다 (C-007 위반)** — 순서를 뒤집지 않는다. (+ 단위 테스트: 조합별 Provider 선택, 금지 조합 컨텍스트 로드 실패)
- [ ] **(KIS 실시간 후속)** `KisRealtimePriceProvider` + `FakeKisRealtimePriceProvider` (KIS 국내주식 WebSocket 체결 틱 수신·재연결·연결상태, 끊김 시 가격 무효·재연결 후 복귀. 키는 KIS_REALTIME일 때만 바인딩) (+ 단위 테스트는 Fake로) — 실제 KIS WebSocket 연결은 외부 스모크로 구분 보고. 위 이슈 #19 ①에서 삭제한 코드를 실시간 표출 허용이 확인된 뒤 다시 도입한다
- [ ] **(KIS 실시간 후속)** `KisTickAggregator` (체결 틱 → 1분 OHLCV 서버 집계, 결과 모델이 캔들 API·`KisHistoricalReplayPriceProvider`와 동일) (+ 단위 테스트: open/high/low/close/volume 산출, 분 경계 전환)
- [ ] **(이슈 #83 — 장기운영 방어 로직)** 동일 거래일 재수집 정책 (수집 전 market_data_imports에서 해당 source_trading_date의 기존 SUCCESS·PARTIAL_SUCCESS와 수집 결과 비교. 동일 데이터는 StockCandle·세션 불변 + SKIPPED_DUPLICATE 이력만, 상충 데이터는 거부 + FAILED 기록, FAILED 이력만 있으면 재시도 허용) (+ 단위 테스트)
- [ ] **(이슈 #83)** `StockCandleCleanupJob` (20영업일 초과 삭제, 재생 중 거래일 보존) (+ 단위 테스트)
- [ ] **(이슈 #83)** 동일 거래일 다른 수집 결과 재수집 거부 시 기존 READY 세션·StockCandle 불변 확인 통합 테스트

## 이슈 #107 — 로컬에서 코인이 실제 빗썸 데이터로 동작하게 (캔들 빈 배열 + 합성 현재가 해소)

> **범위**: 로컬(`!prod`)만 다룬다. prod은 캔들이 이미 실제 provider이고 현재가는 이슈 #104의 WebSocket이 담당한다. `BithumbFeedSimulator` 코드는 수정하지 않고 기존 플래그(`bithumb.feed.simulate.enabled=false`)로 끄기만 한다. 캔들만 살리면 차트(BTC 9,100만원대)와 주문창 현재가(합성 350만원)가 어긋나므로 **캔들·현재가를 한 플래그로 함께** 전환한다. 플래그는 `oauth-real` 선례를 따라 **`crypto-real` 스프링 프로필** 하나로 통일한다. 기본값(프로필 미적용)은 현행 유지 — 시뮬레이터 + Fake 캔들이며, 자동 테스트는 외부 네트워크에 의존하지 않는다 (C-005).

- [x] **⑪ 코인 캔들 provider를 `crypto-real` 프로필로 로컬에서도 켠다** — `BithumbRestCandleProvider`를 `@Profile("prod")` → `@Profile({"prod", "crypto-real"})`, `FakeCryptoCandleProvider`를 `@Profile("!prod")` → `@Profile("!prod & !crypto-real")`로 바꾼다(기존 `oauth-real` 패턴). Provider 코드 자체는 손대지 않는다 — 프로필 조건만 바뀐다. (+ 컨텍스트 테스트: `KisHistoricalReplayPriceProviderContextTest` 방식으로 `@SpringBootTest` + `@Autowired CryptoCandleProvider` 단일 필드를 두어, 기본 프로필에서는 `FakeCryptoCandleProvider` 하나만, `crypto-real` 프로필에서는 `BithumbRestCandleProvider` 하나만 등록되는지 확인 — 두 구현이 동시에 등록되면 주입 실패로 드러난다. 실제 빗썸 REST 호출은 하지 않는다)
- [x] **⑫ BithumbRestTickerPoller (로컬 전용 실시세 주입)** — `@Profile("!prod & crypto-real")` 컴포넌트. `InstrumentRepository.findByMarketAndTradableTrueOrderByIdAsc(Market.CRYPTO)`로 코인 종목을 읽어 빗썸 공개 ticker REST(`GET https://api.bithumb.com/v1/ticker?markets=KRW-BTC,...`)를 **3초 주기**(`@Scheduled(fixedRate)`)로 조회하고, 응답의 `trade_price`를 `FakeBithumbFeedClient.emitTick(symbol, price, receivedAt)`으로 주입한다. 주기는 `PriceStore`의 stale 기준 10초보다 반드시 짧아야 한다(길면 가격이 있는데도 `409 PRICE_UNAVAILABLE`). 심볼 변환은 `BithumbRestCandleProvider`와 같은 `KRW-{symbol}` 규칙, 타임아웃 설정도 같은 방식으로 둔다. 조회 실패·타임아웃·파싱 불가는 **그 회차를 건너뛰고 로그만 남긴다** — 마지막 값이 10초 뒤 자연히 stale이 되어 `PRICE_UNAVAILABLE`로 정직하게 드러난다. 임의값 대체 금지 (MKT-004). 연결상태 `feed:crypto:status`는 건드리지 않는다 — 이슈 #104의 `BithumbFeedLifecycle`이 `ApplicationReadyEvent`에서 `FakeBithumbFeedClient.start()`를 호출해 이미 `CONNECTED`로 만든다. (+ 단위 테스트: 고정 응답으로 종목별 `emitTick` 호출·`trade_price`→가격 매핑, `KRW-{symbol}` 변환, 조회 실패 시 `emitTick` 미호출·예외 전파 없음, 비정상 상태코드·파싱 불가도 같은 skip 처리, `PriceStore`·`feed:crypto:status`에 직접 쓰지 않음. 외부 네트워크 호출 없이 검증한다)
- [x] **⑬ `crypto-real` 프로필로 캔들·현재가를 함께 전환** — `src/main/resources/application-crypto-real.yml`을 추가해 `bithumb.feed.simulate.enabled: false`를 담는다(폴러 주기는 `@Scheduled(fixedRate)` 제약상 코드 상수다). 프로필 하나만 켜면 시뮬레이터가 꺼지고(빈 자체가 생성되지 않음) 캔들·현재가가 **동시에** 실데이터로 바뀐다 — 한쪽만 켜지는 조합이 생기지 않는 것이 이 항목의 목적이다. `BithumbFeedSimulator`와 `application.yml` 기존 키는 수정하지 않는다. (+ 테스트: `ApplicationContextRunner` 또는 `@ActiveProfiles("crypto-real")` 슬라이스로 프로필 ON일 때 `BithumbFeedSimulator` 빈 부재 + `BithumbRestTickerPoller` 빈 존재를 확인. + 회귀: 프로필 OFF(기본)에서 시뮬레이터·`FakeCryptoCandleProvider`가 그대로 뜨고 기존 테스트가 변화 없이 통과 — `build.gradle`의 테스트용 `bithumb-feed-simulator-disabled-for-tests.yml` 격리 장치와 충돌하지 않는지 포함)
- [x] **⑭ 문서 — 켜는 방법과 결과 기록** — `.env.example`(또는 `docs/api-contracts.md` 코인 절)에 `crypto-real` 프로필을 켜는 방법(`SPRING_PROFILES_ACTIVE=crypto-real ./gradlew bootRun`)과 그때 바뀌는 것(캔들·현재가 실데이터, 시뮬레이터 OFF), 켜지 않았을 때의 기본 동작을 적는다. 코인 가격이 더미가 아님을 구분할 수 있게 명시한다(`ai/agent-mistakes.md`의 삼성전자 7만원 사례 교훈). `ai/specs/003-market-data/run-log.md`에 구현 결과와 외부 스모크(실제 ticker REST 응답 확인)를 자동 테스트와 **구분해** 기록한다 (C-005). run-log·tasks 갱신은 **파일 끝에만 추가**한다 — PR #100 때 실제 병합 충돌이 났다. (+ 완료 조건 검증: 프로필 ON + `bootRun` 후 `/api/instruments/{id}/price`와 `/api/instruments/{id}/candles?interval=1m`이 모두 실제 값이고 **같은 자릿수**인지 수동 확인, 프로필 OFF 시 기존 동작 유지, `./gradlew build` 통과)

## 이슈 #564 — 빗썸 시세 피드 리더 선출 (다중 인스턴스 대비)

> **위 ⑧·⑫ 서술 일부가 prod에 한해 더 이상 정확하지 않다.** ⑧의 "`ApplicationReadyEvent`에서 `start()`를 1회 호출"과 ⑫의 "이슈 #104의 `BithumbFeedLifecycle`이 `ApplicationReadyEvent`에서 `FakeBithumbFeedClient.start()`를 호출해 이미 `CONNECTED`로 만든다"는 원문을 고치지 않고 이 절에서 갱신 사실만 남긴다(위 run-log·tasks 규칙과 같은 이유로 파일 끝에 추가). 다중 인스턴스로 확장하면 인스턴스마다 빗썸 웹소켓에 독립적으로 연결해 레이트리밋 위험이 커지는 문제를 ADR-0014가 이미 결정한 Redis 분산 락 패턴으로 해결했다. 근거는 ADR-0014, 이슈 #564, PR #566.

- [x] `BithumbFeedLifecycle`을 `@Profile("prod")` 전용으로 좁히고, `@Scheduled` 리더 선출 루프로 바꿨다 — 리더가 아니면 `tryLock`을 시도해 성공 시에만 `start()`, 이미 리더면 주기마다 `renew`를 시도해 실패하면(다른 인스턴스로 락이 넘어감) `stop()` 후 팔로워로 전환한다. `start()`가 실패해도 리더 자리를 계속 쥐지 않도록 즉시 `unlock`한다. 종료 시 리더였다면 TTL 만료를 기다리지 않고 즉시 `unlock`해 다른 인스턴스가 곧바로 이어받는다. ⑧의 "`ApplicationReadyEvent`에서 1회 시작" 서술은 prod에는 더 이상 해당하지 않는다.
- [x] dev·test·`crypto-real`처럼 prod가 아닌 모든 프로필의 기존 동작(`ApplicationReadyEvent`에서 즉시 `start()`, `@PreDestroy`에서 즉시 `stop()`)은 새 컴포넌트 `BithumbFeedImmediateLifecycle`(`@Profile("!prod")`)로 그대로 옮겼다 — `FakeBithumbFeedClient`·`BithumbFeedSimulator`·⑫ `BithumbRestTickerPoller` 쪽 동작은 전혀 바뀌지 않았다. ⑫의 "이슈 #104의 `BithumbFeedLifecycle`이 ... 이미 `CONNECTED`로 만든다"는 지금은 `BithumbFeedImmediateLifecycle`이 대신 수행한다고 읽는다.
- [x] `RedisLock`에 보유자 토큰이 일치할 때만 TTL을 연장하는 `renew(key, token, ttl)`을 추가하고, 이를 감싸는 `BithumbFeedLeaderLock`을 신설했다(고정 키 `market:bithumb-feed:leader`, `RankingRebuildLock`·`CryptoWatchLock`과 같은 얇은 래퍼 패턴).
- [x] 리더 락 TTL(`bithumb.feed.leader.lock-ttl-seconds`, 기본 30초)과 선출 주기(`bithumb.feed.leader.election-interval-ms`, 기본 10초)를 기동 시 교차 검증한다 — TTL이 선출 주기의 2배 미만이면 기동 자체가 `IllegalStateException`으로 실패한다.
- [x] 실제 `BithumbWebSocketFeedClient`가 한 번만 start/stop된다는 전제로 설계돼 있던 것을, 리더 선출로 같은 인스턴스가 반복 start/stop되면서 생긴 문제 2건도 함께 고쳤다 — 재연결 실행기가 `stop()` 이후 영구 종료돼 재선출된 인스턴스가 다시는 재연결하지 못하던 문제, 뒤늦게 도착한 이전 세대의 연결 성공·종료 콜백이 새 리더의 상태를 덮어쓸 수 있던 문제. 연결 시도마다 델리게이트 핸들러(중첩 클래스 `ConnectionSession`)를 새로 만들어 세대를 구분한다.
- [x] 단위 테스트(`BithumbFeedLifecycleTest`·`BithumbFeedLeaderLockTest`·`BithumbFeedImmediateLifecycleTest`·`BithumbWebSocketFeedClientTest`), `@Profile` 상호배타 검증(`BithumbFeedLifecycleConditionalTest`), Testcontainers 기반 리더 경합·인계 통합 테스트(`BithumbFeedLeaderLockConcurrencyIntegrationTest`) — 이슈 #564, PR #566.
