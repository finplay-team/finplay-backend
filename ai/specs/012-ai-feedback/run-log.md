# Run Log: 012-ai-feedback

## AI 로그 (에이전트 참조용)
| 시각 | 에이전트 | 실행 명령 | 근거 |
|---|---|---|---|
| 11:40 | reviewer(리뷰) | `git log/diff origin/dev`(문서 6종) + `src/main/java`·`db/migration` 대조 | conventions.md, ADR-0002·0003·0004·0011, prd.md C-004, api-contracts/routes |
| 12:20 | reviewer(리뷰) | `git show 523ffb1 --stat` + spec.md Part D(FEED-009·004)·api-contracts 개장 전 브리핑·prd.md 대조, `StockReplaySessionScheduler`·`StockReplayService`·`application.yml`·`Dockerfile`·`compose.deploy.yaml` 코드 확인 | spec.md, api-contracts.md, prd.md, conventions.md, ADR-0002·0003·0004·0011 |
| 12:35 | reviewer(리뷰) | `git show 523ffb1` + spec.md Part B(FEED-007·010·011, 파생 사실·반사실·집단 비교·장 마감 재방문·`trade_feedbacks`/`price_move_peer_stats`)·api-contracts 매도 직후 피드백 대조, `OrderExecutionService`·`PortfolioSellService`·`StockReplayService`·`Trade`/`HoldingLot`/`TradeAllocation`/`Holding`·V9·V10 코드 확인 + 계약 예시 수치 직접 재계산 | spec.md, api-contracts.md, api-routes.md, prd.md C-003·C-004·§5 오류표, conventions.md, ADR-0002·0003·0004 |
| 12:35 | reviewer(리뷰) | `git show 523ffb1 --stat` + spec.md Part A(FEED-001~006·탐지 알고리즘·코인 수익률 이력·노출 판정·뉴스 매칭 범위·데이터 모델)·api-contracts 변동 원인 카드 대조, `StockReplayService`·`PriceStore`·`BithumbRestCandleProvider`·`StockReplaySessionScheduler`·`StockCandleRepository`·V7~V12·`application.yml` 코드 확인 | spec.md, api-contracts.md, api-routes.md, 003-market-data spec/plan, conventions.md, ADR-0002·0004 |
| 12:10 | reviewer(리뷰) | `git show 523ffb1`(Part C 한정) + `StockReplayService`·`StockReplaySessionScheduler`·`PriceStore`·`V7`·`V10`·`SecurityConfig` 대조 | spec.md FEED-008·노출판정·프롬프트·후검증·템플릿·데이터모델, api-contracts.md 종목 뉴스 목록·요약, ADR-0011, conventions.md, ADR-0002·0004 |
| 13:10 | reviewer(리뷰) | `git diff 523ffb1..HEAD` + spec.md Part C·D(FEED-004·008·009, 프롬프트·후검증·템플릿·뉴스 매칭 범위·공시 날짜 판정·`instrument_news_summaries`/`market_briefings`)·api-contracts 종목 뉴스 요약·개장 전 브리핑·api-routes·ADR-0011·prd.md 대조, `application.yml`(pool)·`@Scheduled` 5종·`StockReplayService.resolveRevealCutoff`·`StockReplaySessionScheduler`·`Dockerfile`·`compose.deploy.yaml`·V7 시드 코드 확인 | spec.md, api-contracts.md, api-routes.md, prd.md C-004, ADR-0002·0003·0004·0011, conventions.md |
| 12:25 | reviewer(리뷰) | `git diff 523ffb1..HEAD`(spec.md·api-contracts·api-routes·ADR-0011) + spec.md Part A·B(FEED-001~007·010·011, 탐지 알고리즘·코인 가격 스냅샷·노출 판정·파생 사실·반사실/집단 비교·데이터 모델)·api-contracts 변동 원인 카드/매도 직후 피드백 대조, `StockReplayService`·`PriceStore`·`OrderExecutionService`·`PortfolioSellService`·`HoldingLot`/`TradeAllocation`·`KisHistoricalCandleCollector`·`StockReplaySessionScheduler`·`application.yml`·V8~V10 코드 확인 + 계약 예시 수치 재계산 | spec.md, api-contracts.md, api-routes.md, conventions.md, ADR-0002·0003·0004·0011 |
| 13:30 | reviewer(리뷰) | `git diff 523ffb1..HEAD`(spec.md·api-contracts·api-routes·ADR-0011·run-log) + §마다 중복 기재된 값 전수 대조(크론 7종·상태값·범위 정의·수수료·예시 수치) + 완료 조건 85건 상호 모순 감사 + spec이 이름 대는 코드 요소 실재 확인(`StockReplayService`·`PriceStore`·`StockReplaySessionScheduler`·`OrderExecutionService`·`PortfolioSellService`·`TradeService`·`HoldingLot`/`TradeAllocation`·`ErrorCode`·`application.yml`·`compose.yaml`·`compose.deploy.yaml`·`Dockerfile`·`.env.example`·V1~V12) | spec.md, api-contracts.md, api-routes.md, prd.md, ADR-0002·0003·0004·0011, conventions.md |
| 14:20 | reviewer(리뷰) | `git diff 629fb1c..HEAD`(§확정값 단일 출처 개편분) + 목표 4종 검증 — ①spec.md·api-contracts·api-routes·prd·ADR-0011 전체에서 값(크론 7종·임계값·범위·상태값·컬럼 타입·클래스/메서드명·수수료율·예시 수치) 이중 기재 전수 대조 ②`§` 참조 60건 전수 추적 ③삭제된 237줄이 §확정값으로 이관됐는지 대조 ④FEED-001~011 ↔ 완료 조건 85건 커버리지·상호 모순 감사 + 문서가 이름 대는 코드 실재 확인(`StockReplayService`(findRevealedCandle/resolveRevealCutoff)·`StockReplaySessionScheduler`·`PriceStore`·`BithumbRestCandleProvider`·`OrderExecutionService`(수수료 FLOOR)·`TradeService`·`HoldingLot`/`TradeAllocation`·`ClockConfig`·`application.yml`(pool·@Scheduled 5종)·`Dockerfile`·`compose.deploy.yaml`·`.env.example`·V1~V12) | spec.md, api-contracts.md, api-routes.md, prd.md C-003·C-004, ADR-0002·0003·0004·0011, conventions.md |
| 15:10 | reviewer(리뷰) | `git diff b8ac9ab..HEAD`(8건 정정분) + `git diff origin/dev..HEAD`(PR 전체) — ①직전 라운드 차단 8건 해소 여부 개별 확인 ②`15:30`·`09:00`·"마지막 분봉" 리터럴 전수(spec 30곳·api-contracts 9곳)를 §C-2-1 3개 행에 대입해 용도 판정 ③`§` 참조 60여 건 대상 절 실재 추적(§C-2-1 피참조 0건 확인) ④FEED-001~011 ↔ 완료 조건 커버리지·모순 감사 ⑤값 이중 기재 재스캔(종목 수 16/12·근거창 30/5/35·상한 2/6·문장 수) ⑥문서가 이름 대는 코드 실재 확인(`StockReplayService.getRevealedCandles`/`findRevealedCandle`/`resolveRevealCutoff`·`BusinessDayCalendar.previousBusinessDay`·`PriceStore`(@Component)·`BithumbRestCandleProvider.MAX_COUNT=200`·`OrderExecutionService`(0.00015/0.0005·FLOOR)·`TradeService.getMyTrades`·`HoldingLot.original_quantity`·`ClockConfig`·`application.yml`(pool 5·@Scheduled 5종)·`.env.example`·V7 시드 16/12·V10) + `git merge-base`로 dev 대비 4커밋 뒤처짐·충돌 없음 확인 | spec.md, api-contracts.md, api-routes.md, prd.md, ADR-0002·0003·0004·0011, conventions.md |
| 16:35 | reviewer(리뷰) | `git diff dev...HEAD`(PR #148, `plan.md` 단독 119줄) + spec.md §완료 조건(1147-1272)·FEED-004/008/009 전문 대조, `ai/specs/README.md`·`_template/plan.md`·002/003/004/005/006/011의 `plan.md` 구조 비교, `docs/team-conventions.md` 이슈 분할 기준 대조, `gh pr view 148`으로 체크리스트 실측 대조 | ai/specs/012-ai-feedback/{spec.md,plan.md}, ai/specs/README.md, docs/team-conventions.md, ADR-0011·0003·0004 |
| 16:05 | reviewer(리뷰) | `git diff 5462007..HEAD`(8건 정정분) + `git diff $(git merge-base origin/dev HEAD)..HEAD`(PR 실제 전체) — ①이번 수정 8건 개별 해소 확인 ②`15:30`·`09:00`·"마지막 분봉" 리터럴 전수(spec 27곳·api-contracts 14곳)를 §C-2-1 3개 행에 대입해 용도 판정 ③`§` 참조 전수 대상 절 실재 추적(§C-2-1 피참조 5건 확인) ④동일 값 이중 기재 재스캔(카드 상한 2·종목 수 16/12·근거창 35·문장 수·홈더 5로) ⑤FEED-001~011 ↔ 완료 조건 커버리지·통과 가능성 감사 ⑥계약 예시 수치 전수 재계산(반사실 3종 포함) ⑦문서가 이름 대는 코드 실재 확인(`BusinessDayCalendar.previousBusinessDay`·`KisHistoricalCandleClientImpl`/`KisHistoricalCandleCollector`의 15:30 허용 범위·`StockReplayService`(findRevealedCandle/resolveRevealCutoff/getRevealedCandles 공개범위)·`PriceStore`·`BithumbRestCandleProvider.MAX_COUNT`·`OrderExecutionService`(0.00015/0.0005·FLOOR)·`TradeRepository`가 order 소유·`HoldingLot.original_quantity`·`@Scheduled` 6개 중 cron 3·fixedRate 3·`pool.size=5`) + `git merge-tree`로 dev 대비 4커밋 뒤처짐·무충돌 재확인 | spec.md, api-contracts.md, api-routes.md, prd.md, ADR-0002·0003·0004·0011, conventions.md |
| 17:10 | reviewer(리뷰) | `git diff ca5a4e6..85d450f -- ai/specs/012-ai-feedback/plan.md` + spec.md §완료 조건(1147-1272) 절별 bullet 전수 카운트(awk)로 91건 검산표 재계산, `gh issue view 147`로 원장 불변 반영 확인, `gh pr list --search "feat: in:title"`로 후속 이슈용 PR 규모 통계 재검증, `ai/adr/` 실재 파일명·011/003 plan.md의 ADR 링크 관례 대조 | ai/specs/012-ai-feedback/{spec.md,plan.md}, ai/adr/0002·0003·0004·0011, ai/specs/{003-market-data,011-order-ledger-schema}/plan.md, docs/team-conventions.md |
| 17:45 | reviewer(리뷰) | `git diff 85d450f..18f8227 -- ai/specs/012-ai-feedback/plan.md` + spec.md §완료 조건(1147-1272) 10개 절을 awk로 bullet 전수 재카운트해 91건·8행 배정표(9/3/6/20/16/18/9/9=90+원장불변1)를 처음부터 다시 대조, `ai/adr/` 실재 파일명 확인, `gh pr list --json additions`로 feat: PR 41건 통계(중앙값 1,090·IQR 606~1,563·최대 3,740·500줄초과 34/41) 독립 재계산 | ai/specs/012-ai-feedback/{spec.md,plan.md}, ai/adr/0004-flyway-migrations.md |
| 16:49 | implementer | `gradlew.bat compileJava` + `spotlessApply` (tasks.md 1번 — `feedback.llm.*` 바인딩) | spec.md §C-7, ADR-0011(값은 프로퍼티로), conventions.md, `market/config/KisProperties` 선례 |
| 17:15 | implementer | `gradlew.bat compileJava compileTestJava` + `spotbugsMain` + `test --tests MeIntegrationTest`(키 없이 컨텍스트 기동), Spring AI 2.0.0 jar `javap`로 옵션 매핑 확인 (tasks.md 2번 — `NarrativeGenerator`) | ADR-0011(실패는 반환값·Fake 테스트), spec.md §C-6·§실패 처리·§외부 API 호출 상세, conventions.md, agent-mistakes 2026-07-29·07-30 |
| 17:35 | implementer | `gradlew.bat test --tests "com.finplay.api.feedback.*"`(25건) + 임시 프로브로 fail-fast 실패 원인 확인 (tester 회귀 수정) | tester 권고 1번, ADR-0003(슬라이스 테스트 경계), conventions.md |
| 18:20 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain` + `test --tests "com.finplay.api.feedback.*"`(25건) + 임시 덤프 테스트로 조립 결과를 spec 예시와 대조 (tasks.md 3번 — `NarrativePromptBuilder`) | spec.md §LLM 프롬프트·§후검증·§파생 사실 계산·§C-2·§C-6, api-contracts.md 매도 직후 피드백, conventions.md, agent-mistakes 2026-07-29 |
| 19:05 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain` + `test --tests "com.finplay.api.feedback.*"`(48건) + 임시 프로브로 경계 케이스·가정법 어미 실동작 확인 (tasks.md 4번 — `NarrativeValidator`) | spec.md §후검증 표 5줄·§C-4·§C-8, api-contracts.md 문구 제약, ADR-0011, conventions.md |
| 19:50 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain` + `test --tests "com.finplay.api.feedback.*"`(163건) + 임시 프로브로 템플릿 3종의 후검증 통과 확인 (spec 가정법 목록 수정 + tasks.md 5번 — `NarrativeTemplateBuilder`) | spec.md §후검증·§템플릿 문장·§파생 사실 계산, api-contracts.md 카드 예시 서술, conventions.md(공통화 기준) |
| 20:30 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain test --tests "com.finplay.api.feedback.*"`(221건) + Fake 생성기로 1단계·2단계 흐름 6종 직접 확인 (tasks.md 6번 — `NarrativeService`) | spec.md §후검증 2단계 흐름·§실패 처리·§C-6·§C-7, ADR-0011, tasks.md 6번 검증 항목 |
| 20:55 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain test --tests "com.finplay.api.feedback.*"`(257건) (tester 지적 — `NarrativeResultDto` 불변식 강제) | spec.md §C-4 판정 순서, conventions.md, tester 예고 테스트 |
| 21:40 | reviewer(리뷰) | `git diff dev...HEAD`(PR 브랜치 `feat/147-llm-narrative`, 37파일) + spec.md 4개 수정 자리 정당성 대조(§C-6·§후검증 가정법·§템플릿 문장 부호·§실패 처리 신규 행), `awk`로 §완료 조건 체크박스 절별 전수 카운트(91=16/15/15/9/7/7/7/6/5/4)와 plan.md 8행 배정표(9/3/6/20/16/18/9/9=90+원장불변1) 재검산, `gh issue view 147`로 범위 축소 주석 실재 확인, Spring AI 2.0.0 jar `javap`로 `spring.ai.openai.timeout` 적용 경로·`OpenAiSetup` 키 처리 확인, nullable 필드(holdHigh·holdLow)와 코인 자정 구간을 record 계약에 대입 | spec.md, plan.md, api-contracts.md, ADR-0002·0003·0011, conventions.md, agent-mistakes.md |
| 21:40 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain test --tests "com.finplay.api.feedback.*"`(260건) (reviewer 차단 2건 수정) | spec.md §파생 사실 계산 746줄·§C-9 372줄·§C-4, api-contracts.md 459·461줄, ADR-0011 |
| 21:09 | implementer | `git mv` 후 `gradlew.bat compileJava compileTestJava` + `spotlessCheck` (이슈 #160 tasks 1번 — enum 2종 `feedback/domain/` 이동) | tasks.md 1번, spec.md §C-6 패키지 배치·§C-8, ADR-0002, conventions.md |
| 11:05 | implementer | `gradlew.bat compileJava` (이슈 #212 tasks 2번 — `HolderPopulationQueryService`·`HoldingQuantitySum`·리포지토리 2건 신설) | tasks.md 2번, spec.md §반사실·집단 비교 계산 [집단 비교] 블록·§C-6, ADR-0002, conventions.md |
| 21:20 | implementer | `gradlew.bat test --tests OrderLedgerSchemaTest --tests "order.repository.*" --tests MeIntegrationTest` + 임시 프로브로 `information_schema` 덤프(V13 적용·컬럼 타입·`sub_part` NULL 확인 후 삭제) (tasks.md 2번 — V13 일곱 테이블) | spec.md §데이터 모델·§C-8·§C-9, ADR-0004, V10·V11 DDL 관례 |
| 21:25 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain` + `test --tests MeIntegrationTest --tests OrderLedgerSchemaTest`(`ddl-auto=validate` 기동 확인) (tasks.md 3번 — `MarketNewsItem`) | tasks.md 3번, spec.md §데이터 모델·§C-8·§C-3·§정책 전제, conventions.md Entity 규칙, `order/domain/Trade` 선례 |
| 21:35 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain` + `test --tests MeIntegrationTest`(`validate` 기동) + 컬럼명 오타 음성 프로브로 새 엔티티가 실제 검증 대상인지 확인 후 원복 (tasks.md 4번 — `PriceMoveEvent`·`PriceMoveEventSource`) | tasks.md 4번, spec.md §C-8·§C-9·§탐지 알고리즘(코인) 648줄·§데이터 모델, conventions.md, `order/domain/Order`의 `columnDefinition` 선례 |
| 21:50 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain` + `test --tests MeIntegrationTest`(`validate` 기동) (tasks.md 5번 — `InstrumentNewsSummary`·`MarketBriefing`) | tasks.md 5번, spec.md §데이터 모델·§C-8·§C-9·§C-4, conventions.md, 4번의 `columnDefinition = "TEXT"` 선례 |
| 22:05 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain` + `test --tests MeIntegrationTest --tests OrderLedgerSchemaTest`(일곱 엔티티 전부 매핑된 상태의 `validate` 최종 확인) + §C-8 타입표 ↔ 일곱 엔티티 `@Column` 전수 대조 (tasks.md 6번 — `TradeFeedback`·`PriceMovePeerStat`) | tasks.md 6번, spec.md §C-8·§C-9·§데이터 모델·§C-4, conventions.md, ADR-0004 |
| 22:35 | implementer | `gradlew.bat spotlessApply compileJava compileTestJava spotbugsMain` + `test --tests "feedback.repository.*"`(52건) (reviewer 차단 1건·권장 1건 수정 — `createCrypto`의 `originTradeDate` 파생, 두 팩토리의 시장 일치 검증) | reviewer 지적, spec.md §C-9·§C-8, conventions.md(팩토리는 항상 유효한 상태로 반환) |
| 22:40 | reviewer(리뷰) | `git diff dev...HEAD`(브랜치 `feat/160-feedback-schema`, 36파일) + V13 ↔ spec §C-8 타입·NULL·유니크·인덱스·FK 전수 대조, 일곱 엔티티 `@Column` ↔ V13 대조, `@DataJpaTest` 7종이 실제로 DB를 태우는 축과 Hibernate에서 멈추는 축 구분 판정, `gh issue view 160` 완료 조건 4건 추적, `db/migration` 기존 파일 미수정·enum 이동 잔존 참조 0건·`gh pr list` 확인 | spec.md §데이터 모델·§C-6·§C-8·§C-9·§완료 조건, tasks.md, conventions.md, ADR-0002·0003·0004, ai/specs/README.md |
| 22:50 | reviewer(리뷰) | `git show 518f382`(수정 커밋 단독) + `createCrypto`/`createStock` 호출부 전수 확인, V13 ↔ spec §C-8 NULL 축 ↔ `FeedbackSchemaConstraintsTest` 기대 맵 3자 대조, `IllegalArgumentException` 선례(`StockReplaySession`)와 `GlobalExceptionHandler` 매핑 확인, **임시 프로브 `@DataJpaTest` 1건으로 `origin_trade_date=null` 저장의 실제 예외 원인 확인 후 삭제** | 직전 리뷰 지적 3건, spec.md §C-9·§C-8, conventions.md, ADR-0003·0004, `ClockConfig` |
| 23:30 | implementer | `gradlew.bat compileJava spotlessApply` + `test --tests FeedbackLlmPropertiesIntegrationTest`(신설 yml 블록으로 컨텍스트 기동 확인) (이슈 #167 tasks 1번 — 수집 설정 블록·환경변수 4종) | tasks.md 1번, spec.md §C-1 크론 2종·§C-7 설정 방침과 자격증명 키 경로·§외부 API 호출 상세·§실패 처리, conventions.md 시크릿 규칙, ADR-0002, `FeedbackLlmProperties`·`KisProperties` 선례 |
| 00:10 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain` + 임시 프로브 1개로 접두 관계 종목쌍 14조합(BTC/BCH·ETH/ETC·삼성전자/삼성SDI·현대차/현대모비스) 양방향 판정 확인 후 삭제 (이슈 #167 tasks 2번 — 질의어 조립·제목 필터) | tasks.md 2번, spec.md FEED-001·§C-6·§튜닝, V7 시드 종목명 28종, `NarrativeValidator`(순수 계산 @Component) 선례, ADR-0002 |
| 00:55 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain` + `test --tests FeedbackLlmPropertiesIntegrationTest --tests NewsCollectionEnvironmentVariablesTest`(실컨텍스트 기동) + 임시 프로브(`ApplicationContextRunner` + `spring.profiles.active=prod`)로 prod 수집기 조립 확인 후 삭제, `javap -v`로 Lombok 생성자의 파라미터 애노테이션 부재 확인 (이슈 #167 tasks 3번 — 뉴스 수집기 3종) | tasks.md 3번, spec.md §외부 API 호출 상세·§실패 처리·§정책 전제·§C-8·FEED-001, `ResendEmailSender`/`FakeEmailSender`·`BithumbRestCandleProvider` 선례, agent-mistakes.md 2026-07-30 `@Value` 행 |
| 01:40 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain` + `test --tests "feedback.collector.*" --tests NewsCollectionEnvironmentVariablesTest`(24건) + `FeedbackLlmPropertiesIntegrationTest`(실컨텍스트 기동) + 임시 프로브로 공시 수집기 prod/비-prod 조립·매핑 로딩 확인 후 삭제. corp_code 실값은 확보 실패(`developers.naver.com`·블로그 403, corpCode.xml은 `crtfc_key` 필요) (이슈 #167 tasks 4번 — 공시 수집기 3종·매핑 리소스) | tasks.md 4번, spec.md §외부 API 호출 상세·§실패 처리·§C-3·§C-8·FEED-001, plan.md §착수 전 확인, `BusinessDayCalendar`/`holidays-2026.txt` 선례, `NewsCollectorProfileTest` 선례 |
| 02:30 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain` + `test --tests "feedback.*" --tests InstrumentServiceTest --tests InstrumentRepositoryTest`(412건 전건 통과) + 실컨텍스트 기동으로 `@Scheduled` 플레이스홀더·파생 쿼리 파생 검증, 임시 `@DataJpaTest` 프로브로 `existsByInstrumentIdAndUrl`을 실제 MySQL에서 실행 확인 후 삭제 (이슈 #167 tasks 5번 — 저장 경로·스케줄 2종) | tasks.md 5번, spec.md §C-1 크론·풀 계산·§데이터 모델·§C-6·FEED-001·§실패 처리, agent-mistakes.md 2026-08-03 파생 쿼리 행·2026-07-30 이슈 #125 풀 개수, `KisHistoricalCandleCollector`(트랜잭션 밖 HTTP·zone) 선례 |
| 03:10 | implementer | `gradlew.bat spotlessApply test --tests MissingExternalKeysIntegrationTest`(3건) — 키 4종 부재 조합 고정. **코인 질의어 실측은 수행하지 못함**(`NAVER_SEARCH_*` 미발급, 실제 호출 자동 테스트는 PRD C-005 금지) (이슈 #167 tasks 6번 — 키 없는 빌드 최종 확인·실측 기록) | tasks.md 6번, spec.md §튜닝 "코인 질의어 보정" 행·§실패 처리·FEED-001, ADR-0011, agent-mistakes.md 2026-08-03(OpenAI 빈 문자열 거부)·2026-07-30(공유 컨테이너 오염) |
| 03:40 | reviewer(리뷰) | `git diff dev...HEAD`(브랜치 `feat/167-news-collector`, 44파일) + 수집기·필터·질의어·저장 경로 ↔ spec §C-1 크론/zone/풀·§C-2 `전장`·§C-3 공시 날짜·§C-6 클래스 배치·§C-7 설정 방침·§C-8 컬럼·§실패 처리·§외부 API 호출 상세 전수 대조, `application.yml`·`.env.example`·`compose.deploy.yaml` 확인, `Market`/`InstrumentRepository`/`MarketNewsItem`/`BithumbFeedSimulator`/`BithumbRestTickerPoller`/`application-crypto-real.yml`로 풀 개수 근거 재계산, `java T.java` 프로브로 `URI.create`의 비ASCII·공백 처리 실측, `gh issue view 167`·`gh pr list`·`git show ba0d914`로 완료 주장 대조 | spec.md §확정값 전절·FEED-001·§완료 조건, tasks.md #167, plan.md, conventions.md(레이어·Lombok·시크릿·테스트), ADR-0002·0003·0004·0011, api-contracts.md, agent-mistakes.md |
| 03:50 | implementer | `gradlew.bat spotlessApply compileJava spotbugsMain` + `test --tests "feedback.*"` + 임시 프로브 4건으로 호스트 파싱 실패 시 `link` 폴백을 재현 확인 후 삭제 (reviewer 권장 3건 수정 — publisher 폴백 누락, `.env.example` 문구, `pool.size` 정본 불일치) | reviewer 지적, spec.md §C-8·§실패 처리·§정책 전제, tasks.md 3번(키 검사 미도입 결정), §튜닝 "코인 질의어 보정" 행 |
| 09:20 | 메인 세션 | `gradlew.bat build`(1,900건, 실패 0) — PR #174 리뷰 권장 2건 반영. 중복 판정을 건별 `exists`에서 종목 단위 `findExistingUrls`(`@Query`) 일괄 조회로 전환하고, 일괄 조회에서 새로 생기는 "같은 응답 내 중복" 유니크 위반을 `seen` 집합으로 막았다. 실제 MySQL `@DataJpaTest` 2건 추가. 이슈 #167 완료 조건 체크박스 7개 갱신 (커밋 `6875ab2`) | PR #174 리뷰 [권장] 1·2, `ai/agent-mistakes.md` 2026-08-03(파생 쿼리 단일 필드 프로젝션) |
| 10:10 | 메인 세션 | 외부 키 4종 발급 후 **실호출 검증** — 네이버 `삼성전자` 질의 HTTP 200·`items` 100건, DART `삼성전자` 4건·`카카오` 1건 `status=000`(`corp_name` 일치 확인), `corpCode.xml`에서 16/16 매핑 확보. 네이버 검색이 NAVER API HUB로 이전돼 호스트·헤더를 정정하고 spec §외부 API 호출 상세에 플랫폼 행 신설. 코인 질의어 실측 7종 완료. `gradlew.bat build`(1,901건, 실패 0) (커밋 `c76744c`) | 실호출 응답, spec §외부 API 호출 상세·§튜닝, `plan.md` §착수 전 확인 |
| 10:40 | 메인 세션 | `gradlew.bat test --tests DartCorpCodeRegistryTest` — 재리뷰 [참고] 반영. 시드 종목 목록을 프로젝트 루트 상대경로(`Path.of`) 대신 클래스패스로 읽어 작업 디렉터리 의존을 없앴다. 이 표에 `6875ab2`·`c76744c` 행 추가([권장] 반영) | PR #174 2차 리뷰 [권장] 1·[참고] 1, `ai/specs/README.md` 기록 형식 |
| 11:20 | implementer | `gradlew.bat compileJava` + `spotlessApply` + `spotbugsMain` (이슈 #180 tasks 1번 — `feedback.detection` 설정 블록·`PriceMoveDetector`) | tasks.md #180 1번, spec.md §탐지 알고리즘(주식)·§C-2-1·§C-6·§C-7·§C-8·FEED-002, conventions.md(레이어·DTO record·Lombok), ADR-0002, `FeedbackLlmProperties`/`FeedbackLlmConfig`·`NewsTitleFilter` 선례 |
| 11:50 | implementer | `gradlew.bat compileJava spotlessApply spotbugsMain` + `test --tests "*StockCandleRepositoryTest*"`(파생 쿼리 이름이 실제 부트스트랩되는지 확인) (이슈 #180 tasks 2번 — `market` 조회 메서드 4종) | tasks.md #180 2번, spec.md §C-6 표·§C-2-1·§코드 배치와 설정·FEED-002·FEED-004, conventions.md(레이어·DTO record), ADR-0002, agent-mistakes.md 2026-08-03(파생 쿼리) |
| 12:05 | implementer | `gradlew.bat compileJava spotlessApply spotbugsMain` + `test --tests "*MarketNewsItemRepositoryTest*" --tests "*NewsCollectionPropertiesTest*"`(새 파인더 2종 부트스트랩·record 재바인딩 확인) (이슈 #180 tasks 3번 — `NewsMatcher`) | tasks.md #180 3번, spec.md §C-2·§C-2-1·§C-3·§C-6·§C-7·§뉴스 매칭 범위·FEED-003, conventions.md, agent-mistakes.md 2026-08-03(파생 쿼리) |
| 12:20 | implementer | `gradlew.bat compileJava spotlessApply spotbugsMain` + `test --tests "*PriceMoveEventRepositoryTest*"`(유니크 축과 같은 exists 파인더 부트스트랩 확인) (이슈 #180 tasks 4번 — `PriceMoveCardService`) | tasks.md #180 4번, spec.md §노출 판정·§데이터 모델·§실패 처리·§C-3·§C-4·§C-6·FEED-003·FEED-004, ADR-0004(V13 미수정), conventions.md |
| 12:45 | implementer | `gradlew.bat compileJava spotlessApply spotbugsMain` + `test --tests "*NewsCollectionIntegrationTest*" --tests "*NewsCollectionScheduleTest*"`(13건 — 풀 크기 ≥ 등록된 `@Scheduled` 수 재확인, 새 크론 플레이스홀더로 실컨텍스트 기동) (이슈 #180 tasks 5번 — 개장 전 배치 골격) | tasks.md #180 5번, spec.md §C-1(크론·zone·풀 계산)·§C-6(생성 순서)·§코드 배치와 설정·FEED-002·FEED-004·§실패 처리, agent-mistakes.md 이슈 #125(풀 개수) |
| 13:10 | implementer | `gradlew.bat compileJava spotlessApply spotbugsMain` + `test --tests "*PriceMoveEventRepositoryTest*" --tests "*PriceMoveEventSourceRepositoryTest*"`(게이트 파인더·`JOIN FETCH` 질의 부트스트랩 확인) (이슈 #180 tasks 6번 — 카드 조회 API) | tasks.md #180 6번, api-contracts.md "종목 변동 원인 카드 조회" 행, spec.md §C-5·§C-6·§노출 판정·§정책 전제·FEED-006·§실패 처리, conventions.md(레이어·DTO·응답 포맷), `InstrumentController` 선례 |

| 13:40 | reviewer(리뷰) | `git diff dev...HEAD`(48파일) + `gh issue view 180`(완료 조건 21건) 대조, `@Scheduled` 전수 세기(기본 8·prod 7·crypto-real 8)·spring-tx 7.0.8 `AnnotationTransactionAttributeSource(false)` 확인으로 패키지 전용 `@Transactional` 유효성 검증, `gradlew.bat compileJava compileTestJava` | conventions.md, ADR-0002·0003·0004·0011, spec.md §C-1·C-2·C-2-1·C-3·C-5·C-6·C-7·C-8·§탐지 알고리즘(주식)·§노출 판정·§뉴스 매칭 범위·§실패 처리, tasks.md #180 완료 조건 소유표, api-contracts.md, agent-mistakes.md |

| 14:20 | implementer | `gradlew.bat compileJava compileTestJava spotlessApply` + `test --tests NewsMatcherTest --tests NewsCollectionPropertiesTest --tests FeedbackDetectionPropertiesYamlTest`(상수 이동 후 기존 단정 유지 확인) (이슈 #188 tasks 1번 — 목록 상한 3종·`MarketSessionTimes`) | tasks.md #188 1번, spec.md §C-2·§C-2-1·§C-6·§C-7·§뉴스 매칭 범위, ADR-0002·0004(스키마 무변경), conventions.md, `FeedbackDetectionProperties`·`StockCandleAggregator` 선례 |

| 15:50 | implementer | `gradlew.bat compileJava compileTestJava spotlessApply` + `test --tests "*PropertiesYamlTest" --tests FeedbackBatchPropertiesTest --tests NewsCollectionPropertiesTest --tests "*ScheduleTest"`(37건) + `test --tests NewsCollectionPropertiesIntegrationTest --tests NewsCollectionIntegrationTest`(`@SpringBootTest`로 새 설정 파일 적재·풀 단정 확인), `javap -c`로 `Scheduled.CRON_DISABLED` 단락 확인 (이슈 #188 tasks 2번 — 테스트 크론 비활성화) | tasks.md #188 2번, spec.md §C-1, ADR-0003, `bithumb-feed-simulator-disabled-for-tests.yml`·`build.gradle` 선례, agent-mistakes.md 2026-08-04(추가한 행) |

| 16:40 | implementer | `gradlew.bat compileJava compileTestJava spotlessApply spotbugsMain` + `test --tests FeedbackBatchServiceTest --tests FeedbackBatchIntegrationTest` + 임시 프로브 3건으로 **상한 위 픽스처**의 공시 우선 절단을 재현 확인 후 삭제 (이슈 #188 tasks 4번 — 주식 요약·브리핑 생성) | tasks.md #188 4번, spec.md §뉴스 매칭 범위(2026-08-04 절단 규칙)·§C-2·§C-2-1·§C-3·§C-4·§C-6·§C-7·§데이터 모델·FEED-008·FEED-009, ADR-0002·0004(V13 미수정), conventions.md, `PriceMoveCardService`·`NewsMatcher` 선례 |

| 18:10 | implementer | `gradlew.bat compileJava compileTestJava spotlessApply spotbugsMain spotlessCheck` + `test --tests "com.finplay.api.feedback.*"`(전건 통과 — 새 파생 파인더 부트스트랩·컨텍스트 기동 확인) (이슈 #188 tasks 5번 — Part C 조회 API) | tasks.md #188 5번, api-contracts.md "종목 뉴스 목록·요약 조회" 행, spec.md §C-2·§C-3·§C-4 판정 순서·§C-5·§C-6·§C-7·§뉴스 매칭 범위·FEED-008, conventions.md(레이어·DTO record·GET 부수효과 없음), `PriceMoveController`/`PriceMoveQueryService` 선례 |

| 19:30 | implementer | `gradlew.bat compileJava compileTestJava spotlessApply spotbugsMain spotlessCheck` + `test --tests "com.finplay.api.feedback.*"`(전건 통과 — 새 파인더 부트스트랩·컨텍스트 기동 확인), `@RequestParam Market` 선례 5곳 확인으로 400 매핑 경로 대조 (이슈 #188 tasks 6번 — Part D 조회 API) | tasks.md #188 6번, api-contracts.md "개장 전 브리핑 조회" 행, spec.md §C-2·§C-3·§C-4 판정 순서·§C-6·§C-7·§C-8·§뉴스 매칭 범위·FEED-009, `GlobalExceptionHandler`(VALIDATION_ERROR 매핑), conventions.md, `InstrumentNewsQueryService`·`AccountController` 선례 |

| 21:00 | implementer | `gradlew.bat compileJava compileTestJava spotlessApply spotbugsMain spotlessCheck` + `test --tests "com.finplay.api.feedback.*"`(694건, 실패 0 — 새 파인더 4종 부트스트랩·풀 크기 단정 재확인) (이슈 #188 tasks 7번 — 코인 경로) | tasks.md #188 7번, spec.md §C-1(크론·zone·풀 계산)·§C-2 `ROLLING_24H`·§C-4·§C-6·§C-9·§데이터 모델·FEED-008·FEED-009, api-contracts.md 코인 절, ADR-0004(스키마 무변경), agent-mistakes.md 이슈 #125(풀 개수) |

| 22:10 | reviewer(리뷰) | `git diff dev...HEAD`(59파일) + `gh issue view 188`(완료 조건 16건 + 공통 1건) 대조, `FeedbackBatchService` 호출 순서·시그니처 무변경 확인, 상한 3종 사용처 전수 확인(`max-items-per-summary` vs `-news-list`/`-briefing`), `@Scheduled` 전수 세기(기본 9·prod 8·crypto-real 9)로 두 yml 주석 대조 | conventions.md(리뷰 체크 질문·DTO 각주·GET 부수효과), ADR-0002·0003·0004, spec.md §C-1·C-2·C-2-1·C-3·C-4·C-5·C-6·C-7·C-9·§노출 판정·§뉴스 매칭 범위(2026-08-04 절단 규칙)·§튜닝, tasks.md #188 완료 조건 소유표, api-routes.md·api-contracts.md, agent-mistakes.md |

| 21:40 | implementer | `gradlew.bat spotlessApply compileJava compileTestJava spotlessCheck` + `grep minusHours(24)`로 잔존 선언 0건 확인 (PR #194 reviewer [권장 1] — 코인 롤링 창 상수 통합) | reviewer [권장] 1, spec.md §C-2·§C-6, conventions.md("세 번째 중복" 문턱), `MarketSessionTimes` 선례 |

| 14:20 | implementer | `gradlew.bat compileJava` + `spotlessApply compileJava` (이슈 #208 tasks 1번 — 컨트롤러·조회 서비스 골격) | tasks.md #208 1번, spec.md FEED-007·§C-4·§C-5·§C-6(신설 요소·`portfolio` 배분·lot 조회)·§핵심 제약(재생 시간축), api-contracts.md 매도 직후 피드백, conventions.md(레이어·DTO record·정적 팩토리 예외), ADR-0002·0004(스키마 무변경), agent-mistakes.md 2026-07-29(record 방어적 복사)·2026-08-01(CRLF) |
| 16:10 | implementer | `gradlew.bat compileJava compileTestJava spotlessApply spotlessJavaCheck` (이슈 #208 tasks 2번 — 파생 사실·보유 구간 카드) | tasks.md #208 2번, spec.md §파생 사실 계산·§C-5(카드 게이트)·§C-6(`getFullDayCandles` 게이트 우회 조건), api-contracts.md 매도 직후 피드백(예시 값 `-0.0325`·`0.0059`·`105`·`115`·`195`), `PriceMoveQueryService`·`PriceMoveEventSourceRepository` 선례, ADR-0002·0004(스키마 무변경) |
| 16:40 | implementer | `gradlew.bat compileJava compileTestJava spotlessJavaCheck` (이슈 #208 — `holdingMinutes` 음수 구멍 수정, tester 발견 + 사용자 결정) | 2026-08-04 사용자 결정(원본 거래일 역전 시 `null`, clamp·벽시계 대체 금지), spec.md §파생 사실 계산(이 커밋에서 규칙 추가), api-contracts.md 매도 회고 소절(nullable 목록 불변) |
| 17:10 | implementer | `gradlew.bat compileJava compileTestJava spotlessJavaCheck` (이슈 #208 — 과거 서비스 날짜 게이트 상한 `LocalTime.MAX` → `withNano(0)`, tester 실측 진단) | tester 진단(`select cast(? as char)`가 `LocalTime.MAX`에 `"00:00:00"` 반환, `PostSellFeedbackGateIntegrationTest` 실패), spec.md §C-5(카드 게이트), §C-8(`reveal_time`은 `TIME`), `PriceMoveEventRepositoryTest`의 트랩 회귀 테스트 |
| 10:20 | implementer | `gradlew.bat compileJava compileTestJava spotlessJavaCheck` (이슈 #208 tasks 3번 — 매도 후 흐름·반사실·시각 게이트) | tasks.md #208 3번 + §제약의 6·7번 경계표, spec.md §C-5(장 마감 게이트)·§C-2-1(마지막 분봉)·§C-4·§파생 사실 계산·§반사실·집단 비교 계산, api-contracts.md 매도 회고(`returnRate` null 명시), `MarketSessionTimes.MARKET_CLOSE_TIME`·`getPreviousTradingDayClose` 선례 |
| 13:30 | implementer | `gradlew.bat compileJava compileTestJava spotlessApply spotlessJavaCheck` (이슈 #208 tasks 4번 — AI 서술 생성·저장·재사용) | tasks.md #208 4번 + §제약(LLM을 트랜잭션 밖에·자기호출 무효), spec.md §C-4(`narrativeStatus` 항상 `READY`)·§C-6(`NarrativeService` 하나만 주입)·§템플릿 문장·§실패 처리·§왜 반사실은 AI 문장에 넣지 않는가, `PriceMoveCardWriter` 선례, `application.yml`의 `open-in-view: false`, #198 LLM 응답 시간 실측 |
| 16:20 | implementer | `gradlew.bat compileJava compileTestJava spotlessApply spotlessJavaCheck` (이슈 #208 tasks 5번 — 서술 재생성·누적 상한) | tasks.md #208 5번, spec.md §C-5(재생성 게이트 — `NO_EVENT`·`INSUFFICIENT_SAMPLE`도 확정)·§C-7(`max-narrative-retry`)·§C-8(`narrative_finalized`·`regeneration_attempts` 기본값)·FEED-007, api-contracts.md 매도 회고(재생성 1회·반사실 미반영), 4번이 세운 `TradeFeedbackWriter` 경계 |

| 03:40 | reviewer(리뷰) | `git diff dev...HEAD`(38파일) + `gh issue view 208`(완료 조건 18+공통 1건) ↔ tasks.md §완료 조건 소유표 19행 대조, `gh pr list --head`(PR 미생성 확인), 트랜잭션 경계 실측 확인(spring-tx 7.0.8 소스에서 `AnnotationTransactionAttributeSource(false)` — package-private @Transactional 유효), `ClockConfig`·`OrderExecutionService`·V10 `executed_at DATETIME(6)`로 운영 체결시각의 초·마이크로초 존재 확인, `grep LocalTime.MAX/LocalTime.now(clock)`로 같은 트랩 잔여 위치 전수 확인 | conventions.md(레이어·DTO·Lombok·리뷰 체크 질문), git-conventions.md(브랜치·커밋), ADR-0002·0003·0004·0010·0011, spec.md §C-4·C-5·C-6·C-7·C-8·§노출 판정·§파생 사실 계산·§반사실·집단 비교 계산·§템플릿 문장·§실패 처리·FEED-007·§완료 조건, api-contracts.md 매도 직후 피드백·api-routes.md, prd.md §3, agent-mistakes.md 2026-08-03·2026-08-04 |
| 18:40 | implementer | `gradlew.bat compileJava compileTestJava spotlessApply spotlessJavaCheck` (reviewer 차단 1건 + 권장 1 + 참고 1 반영) | reviewer [차단](체결시각 소수 초가 분봉 경계를 밀어냄)·[권장](`DataIntegrityViolationException` 흡수 폭)·[참고](게이트 상한 나노초), V10 `executed_at DATETIME(6)`·`OrderExecutionService`의 `LocalDateTime.now(clock)`, spec.md §파생 사실 계산("양 끝 포함")·§C-5, agent-mistakes.md 2026-08-04(`LocalTime.MAX` 자정 접힘) |
| 20:10 | implementer | `gradlew.bat compileJava compileTestJava spotlessApply spotlessJavaCheck` (reviewer 후속 2건 — 분 계산 절삭 버그·`sameSessionCompleted` 역전) | 2026-08-05 사용자 결정(역전이면 `sameSessionCompleted=false`), api-contracts.md 예시값(`holdingMinutes: 310`·`minutesAfterBuy: 115`·`buyToNewsMinutes: 105`), tasks.md 1번("계약 예시가 재현되지 않으면 구현이 틀린 것"), spec.md §파생 사실 계산(이 커밋에서 개정), V10 `executed_at DATETIME(6)` |
| 10:40 | implementer | `gradlew.bat compileJava` (이슈 #212 tasks 1번 — `counterfactuals` 3종 `returnRate` 수수료 재계산) | tasks.md #212 1번, spec.md §반사실·집단 비교 계산(FLOOR 식·시나리오 3종)·§C-6·§왜 반사실은 AI 문장에 넣지 않는가, api-contracts.md 매도 회고 예시값(`atClose -0.0117`·`atHoldHigh 0.0111`·`atFirstMoveAfterBuy -0.0103`), `OrderExecutionService.STOCK_FEE_RATE`(값·라운딩 재사용) |
| 12:10 | implementer | `gradlew.bat compileJava` (이슈 #212 tasks 3번 — `PeerStatsBatchService` 신설·크론·풀 크기) | tasks.md #212 3번, spec.md §반사실·집단 비교 계산 [집단 비교] 블록·§C-1·§C-6·§C-9, `FeedbackBatchService`(배치 패턴)·`HolderPopulationQueryService`(#212 2번) 선례, agent-mistakes.md 이슈 #125(풀 개수 실측) |
| 12:45 | implementer | `gradlew.bat compileJava spotlessCheck` (이슈 #212 tasks 4번 — `peerComparison` 상태 판정) | tasks.md #212 4번, spec.md §C-4·§C-9·§반사실·집단 비교 계산 [집단 비교] 블록, api-contracts.md 매도 회고 예시값(`priceMoveId:12`·`holderCount:47`·`soldWithin30MinRate:0.38`)·"모집단 지표 셋 null" 문단, `PriceMovePeerStat`·`HeldPriceMoveItem` 선례 |
| 12:55 | implementer | `gradlew.bat test --tests PostSellFeedbackRegenerationIntegrationTest`(5건 통과) + `buildPeerComparison`의 `NO_EVENT` 분기를 임시로 `peerComparisonNotYet()`으로 되돌려 카드 0건 관련 3개 테스트가 실제로 빨개지는지 확인 후 원복 (이슈 #212 tasks 5번 — `@MockitoBean PostSellFeedbackReader` 제거, 실제 픽스처 전환) | tasks.md #212 5번, spec.md §C-4, `PostSellFeedbackPeerComparisonGateIntegrationTest`(4번 항목의 실 픽스처 패턴 재사용) |
| 13:20 | implementer | `gradlew.bat compileJava compileTestJava` + `test --tests PeerStatsBatchServiceIntegrationTest --tests PostSellFeedbackNarrativeIntegrationTest`(전건 통과) + 두 배치·서비스에 임시 뮤테이션(각각 `holding_lots.remaining_quantity`를 1씩 감소)을 넣어 신설 원장 불변 테스트 2건이 실제로 빨개지는지 확인 후 원복 (이슈 #212 tasks 6번 — 원장 불변 확장) | tasks.md #212 6번, `FeedbackBatchIntegrationTest`·`PostSellFeedbackNarrativeIntegrationTest`(기존 스냅샷 대조 패턴 재사용), agent-mistakes.md 2026-08-04(flush 전제·행 수만으론 UPDATE 못 잡음) |

| 13:03 | orchestrator | `gradlew.bat build`(Spotless + SpotBugs + jacoco 커버리지 40% + Testcontainers 통합 전체, 6분 25초) — BUILD SUCCESSFUL. 검증 SHA `92bd9401a7a6db737a3103747914d71457378536` | 이슈 #212 7개 항목 커밋 완료 시점 |
| 17:30 | reviewer(리뷰) | `git diff dev...HEAD`(34개 파일) + `PostSellFeedbackReader`·`HolderPopulationQueryService`·`PeerStatsBatchService`·리포지토리 신설분 코드 확인 + `./gradlew.bat compileJava compileTestJava spotlessCheck` 재실행 + api-contracts.md 예시값 수기 재계산 대조 | tasks.md(이슈 #212 7개 항목), spec.md §C-4·C-5·C-6·C-9·§반사실·집단 비교 계산, `gh issue view 212`, conventions.md, ADR-0002·0003·0004·0010·0011 |
| 09:10 | implementer | `gradlew.bat compileJava` + `gradlew.bat spotlessApply` (이슈 #225 tasks 1번 — `CryptoPriceSnapshotService` 신설) | tasks.md #225 1번·"이 이슈 전체에 걸리는 제약", spec.md §코인 가격 스냅샷·§C-1·§C-6, conventions.md(Redis key는 PriceStore 안에서만 조립), agent-mistakes.md 2026-08-04(`RestClient` 빈 중복 사고 — 새 빈 안 만듦) |
| 09:35 | implementer | `gradlew.bat compileJava` (이슈 #225 tasks 2번 — `FeedbackCryptoProperties` 신설 + `NewsMatcher.matchCrypto`) | tasks.md #225 2번·"이 이슈 전체에 걸리는 제약"(기존 리포지토리 메서드 재사용·값 재정의 금지), spec.md §C-2 근거창(코인)·§C-7·§뉴스 매칭 범위, `FeedbackDetectionProperties`·`FeedbackNewsProperties` 선례 |
| 09:50 | implementer | `gradlew.bat compileJava` (tester 발견 버그 수정 — `FeedbackCryptoProperties` 빈 등록 누락) | tester 보고(`NoSuchBeanDefinitionException`, `NewsMatcher` 조립 실패로 `@SpringBootTest` 전체 캐시 오염), `FeedbackDetectionConfig` 선례(프리픽스마다 설정 클래스 분리 방침) |
| 10:15 | implementer | `gradlew.bat -p . compileJava --console=plain` (이슈 #225 tasks 3번 — `CryptoPriceMoveWatcher` 신설) | tasks.md #225 3번·"이 이슈 전체에 걸리는 제약"(k 공유·새 인덱스 불필요·원장 불변·pool 11→12), spec.md §탐지 알고리즘(코인)·§C-1·§C-9, `PriceMoveDetector`(σ 계산·병합 패턴)·`PriceMoveCardService`/`PriceMoveCardWriter`(확정·저장 재사용)·`CryptoFeedbackBatchService`(종목별 예외 격리) 선례 |
| 11:05 | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" compileJava --console=plain` (이슈 #225 tasks 4번 — `PriceMoveQueryService` 코인 분기를 실제 조회로 교체) | tasks.md #225 4번, spec.md §C-2 `ROLLING_24H`·§C-5(카드 코인 — 게이트 없음)·§C-9, api-contracts.md "종목 변동 원인 카드 조회" 행(정렬·`originTradeDate=null`), `PriceMoveEventRepository`(기존 코인 쿨다운·일일 상한 파인더와 같은 인덱스 재사용) |
| 21:50 | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" spotlessApply compileJava compileTestJava --console=plain` + `test --tests CryptoPriceSnapshotServiceIntegrationTest --tests PriceMoveQueryGateIntegrationTest`(Docker, 14건 통과) (이슈 #225 tasks 5번 — 원장 불변 확장) | tasks.md #225 5번, agent-mistakes.md 2026-08-04 "원장 불변" 행(값 비교·flush 시점), `CryptoPriceMoveWatcherIntegrationTest`(3번 항목이 이미 만든 원장 불변 선례) |
| (문서) | implementer | `docs/api-contracts.md` "코인 분기는 아직 구현되지 않았다" 인용 블록 제거 + `ai/prd.md` §3 "AI 피드백 — 코인 변동 감시" 행 완료 갱신 (이슈 #225 tasks 6번 — 문서 동기화) | tasks.md #225 6번, CLAUDE.md 규칙 7·10 |
| 18:40 | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" compileJava compileTestJava --console=plain` (이슈 #244 tasks 1번 — `FeedbackCryptoProperties.watchLockTtlSeconds` 신설) | tasks.md-244 1번, spec.md §C-7 `watch-lock-ttl-seconds`, ADR-0014 §결정(TTL 30초 근거) |
| 18:55 | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" compileJava compileTestJava spotlessApply --console=plain` (이슈 #244 tasks 2번 — `CryptoWatchLock` 신설) | tasks.md-244 2번, ADR-0014 §결정(키 형식·SET NX PX·Lua check-then-delete), `PriceStore`(StringRedisTemplate 직접 사용 패턴, 단 거치지 않고 독립 주입) |
| 19:05 | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" compileJava compileTestJava spotlessApply --console=plain` + `test --tests CryptoPriceMoveWatcherTest`(20건 통과) (이슈 #244 tasks 3번 — `watchOne()`에 락 통합) | tasks.md-244 3번, ADR-0014 §결정(락 범위·건너뛰기), spec.md §탐지 알고리즘(코인) |
| 19:15 | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" test --tests CryptoWatchLockConcurrencyTest`(Docker, 2건 통과 — Testcontainers MySQL+실제 Redis) + `spotlessApply compileJava compileTestJava` (이슈 #244 tasks 4번 — 재현 테스트: 방어 우회 시 카드 2건·NarrativeService 2회, 방어 켠 상태 대조는 기존 테스트 이전) | tasks.md-244 4번(재현 우선), spec.md §완료 조건 "동시성 방어" 2건, `LimitOrderConcurrencyIntegrationTest`(ready/start latch 관례) |
| 19:20 | implementer | `grep` 대조(spec.md §C-9·§탐지 알고리즘(코인)·§C-7·§실패 처리·§완료 조건, ADR-0014 원문) ↔ `CryptoWatchLock.java`·`CryptoPriceMoveWatcher.java`·`application.yml` 코드 직접 확인(코드 변경 없음) (이슈 #244 tasks 5번 — 문서 동기화 검증) | tasks.md-244 5번, spec.md §C-9(432줄)·§탐지 알고리즘(코인)(688·708줄)·§C-7(366줄)·§실패 처리(1125줄)·§완료 조건(1289-1292줄), ADR-0014 §결정 |
| 19:35 | reviewer(리뷰) | `git diff dev...HEAD`(브랜치 fix/244-crypto-watch-duplicate-guard 전체) + `.\gradlew.bat compileJava compileTestJava` + `test --tests CryptoWatchLockTest --tests CryptoPriceMoveWatcherTest --tests FeedbackCryptoPropertiesTest`(단위, 통과) + `test --tests CryptoWatchLockConcurrencyTest --tests CryptoWatchLockIntegrationTest`(Docker, 5건 통과) | ADR-0014, spec.md §C-9·§탐지 알고리즘(코인)·§C-7·§실패 처리·§완료 조건, tasks-244.md, docs/conventions.md, ai/adr/ 전체 |
| 19:57 | reviewer(리뷰) | `git diff dev...HEAD`(브랜치 fix/244-crypto-watch-duplicate-guard 전체, 독립 2차 검토) + `.\gradlew.bat test --tests "*CryptoWatchLockConcurrencyTest" --rerun`(Docker, 2건 통과 — 재현 테스트 경합 재확인) + `git log --name-status`(fb467de 리네임 R058 확인) + `build/test-results` 실제 산출물 대조 | docs/conventions.md(테스트 네이밍·Lombok·레이어), ADR-0002·0003·0004, ADR-0014, spec.md §C-7·§C-9·§탐지 알고리즘(코인)·§실패 처리·§완료 조건, tasks-244.md, plan.md §진행 상태(#198 선례) |
| 20:10 | implementer | `git mv`(파일명 복원) + `gradlew.bat compileJava compileTestJava spotlessApply --console=plain` + `test --tests FeedbackCryptoPropertiesTest --tests CryptoWatchLockTest --tests CryptoWatchLockConcurrencyIntegrationTest`(Docker, 17건 통과) (2차 리뷰 권장 4건 + 참고 1건 반영) | 2차 리뷰(Opus) 권장 1~4·참고, docs/conventions.md(테스트 네이밍), spec.md §C-7, ADR-0014, ai/specs/README.md(run-log 시각 표기) |
| 21:05 | implementer | `gradlew.bat compileJava compileTestJava spotlessApply --console=plain` + `test --tests FeedbackCryptoPropertiesTest --tests FeedbackCryptoPropertiesYamlTest --tests CryptoWatchLockTest --tests CryptoPriceMoveWatcherTest --tests CryptoWatchLockConcurrencyIntegrationTest`(Docker, 43건 통과) (`/review-pr 254` 승인 + 권장 6건·참고 4건 전부 반영) | PR #254 리뷰(승인, 권장 6·참고 4), ADR-0014, spec.md §C-7, ai/specs/012-ai-feedback/tasks-244.md, context-notes.md |
| 22:34 | implementer | 임시 프로브(`unlock`의 `deleted == 1L` 분기에 `AssertionError`)로 정상 경로 커버 여부를 실측 후 원복 + `gradlew.bat spotlessApply compileJava compileTestJava` + `test --tests FeedbackCryptoPropertiesTest --tests FeedbackCryptoPropertiesYamlTest --tests CryptoWatchLockTest --tests CryptoPriceMoveWatcherTest --tests CryptoWatchLockConcurrencyIntegrationTest`(Docker, 44건 통과) (PR #254 3라운드 리뷰 권장 2건·참고 4건 반영) | PR #254 3라운드 리뷰(승인, 권장 2·참고 4), ADR-0014 §후속, ai/specs/012-ai-feedback/tasks-244.md |
| 23:30 | implementer | 임시 프로브(`unlock`의 `deleted == null` 가드 제거)로 제안받은 테스트가 가드를 지키지 못함을 실측(9건 전부 통과) → 로그 관찰 방식으로 교체 후 같은 프로브 재실행(해당 1건만 실패) → `git checkout`으로 원복 + `gradlew.bat spotlessApply` + `test --tests CryptoWatchLockTest`(9건 통과) + `gradlew.bat build`(전체 게이트 통과) (PR #254 4라운드 리뷰 권장 1건·참고 3건 반영) | PR #254 4라운드 리뷰(승인, 권장 1·참고 3), `DartDisclosureCollectorTest`(ListAppender 로그 관찰 헬퍼 선례), CLAUDE.md 규칙 6, context-notes.md |
| 09:27 | reviewer(리뷰) | `git diff dev...HEAD`(브랜치 fix/179-coin-news-filter) + `gradlew.bat test --tests "*NewsTitleFilterTest" --tests "*NewsSearchQueryBuilderTest" --tests "*NaverNewsCollectorTest" --tests "*CoinNewsFilterMeasurementTest"`(통과) + 임시 프로브 3종(`appearsIndependently` 첫 등장만 검사 / `swallowedByLongerName` 첫 등장만 검사 / 길이 가드 완화)으로 단정 유효성 실측 후 `git checkout` 원복 | spec.md §FEED-001(개정 문단), docs/conventions.md, ADR-0002·0003·0004, CLAUDE.md 규칙 7·10, V7__create_instruments.sql(심볼 시드), run-log.md 2026-08-07 항목 |
| 09:38 | implementer | `gradlew.bat compileJava`(문서·javadoc·측정 도구만 변경) + `680 − 115 = 565`를 보고서 `S` 열 12행 합계로 검산 + `python -c "ast.parse"`로 `fetch.py` 구문 확인 | PR #258 리뷰 2건(차단 3·권장 11), spec.md §FEED-001 개정 문단, run-log 2026-08-07 절, ai/specs/README.md(로그 형식), CLAUDE.md 규칙 6·7·10 |
| 00:41 | implementer | `gradlew.bat -p "C:\Users\user\orca\workspaces\tradeclass-api\2-ai" compileJava --console=plain`(통과) (이슈 #275 tasks 1번 — `FeedbackBatchProperties.cryptoPeerStatsCron` + `application.yml` `crypto-peer-stats-cron` 추가, `pool.size`는 미변경) | tasks-275.md 1번, spec.md §C-1 스케줄 표(`0 5 0 * * *`)·풀 크기 불변식, §FEED-012 결정 3, docs/conventions.md |
| 01:05 | implementer | `gradlew.bat -p "C:\Users\user\orca\workspaces\tradeclass-api\2-ai" compileJava spotlessCheck --console=plain`(통과) (이슈 #275 tasks 2번 — `PeerStatsBatchService.runCryptoPeerStatsBatch` 신설 + `pool.size` 12 → 13) | tasks-275.md 2번, spec.md §C-1(zone·풀 개수 계산)·§FEED-012 결정 3, `PriceMoveEventRepository.findByMarketAndOccurredAtBetween` Javadoc, `CryptoPostSellFeedbackReader.buildPeerComparison`(조회 키) |
| 02:35 | implementer | `ai/prd.md` §3 195·196행 근거 칸에 코인 지원·이슈 #275 추가 (코드 변경 없어 빌드 미실행) | tasks-275.md 6번, CLAUDE.md 규칙 10(갱신 대상 판정·근거 칸 규약), spec.md §FEED-012 결정 0~4, prd.md §3 갱신 규약 |
| 04:10 | reviewer(리뷰) | `git log/diff dev...HEAD`(브랜치 feat/275-crypto-post-sell, 커밋 9건, HEAD `03ee459a`) + `PostSellArithmetic`↔구 `PostSellFeedbackReader` 산술 대조 + `BithumbRestCandleProvider.resolveCount`(200봉 캡 계산)·`CachedCryptoCandleProvider`·`FakeCryptoCandleProvider` 대조 + `NarrativePromptBuilder.postSellPrompt` 확인 (빌드는 재실행하지 않음 — `03ee459a`에서 통과 확인됨) | spec.md §FEED-012 결정 0~4, docs/api-contracts.md "코인 체결의 차이" 소절, ai/api-routes.md 66행, docs/conventions.md(리뷰 체크 질문·레이어), ADR-0002·0003·0004, CLAUDE.md 규칙 7·10 |
| 05:20 | implementer | `gradlew.bat -p "C:\Users\user\orca\workspaces\tradeclass-api\2-ai" spotlessApply build --console=plain`(3307건, BUILD SUCCESSFUL) + `BithumbRestCandleProvider.resolveCount`·`CachedCryptoCandleProvider`(일봉 캐시 미적용) 코드 확인 (리뷰 차단 3건 + 권장 2건 반영) | 리뷰 차단 1~3·권장 1~2, spec.md §FEED-012 결정 4·§C-1, docs/api-contracts.md 실패 처리 표, ai/api-routes.md 66행, CLAUDE.md 규칙 7 |
| 06:05 | reviewer(리뷰) | `git show 5ad19e8d`(수정 커밋 1건 재리뷰) + `PostSellFeedbackReader.isSameSessionCompleted`↔`toPromptInput`의 `multiDayHold` 판정 대조(주식에서 언제나 거짓임을 코드로 확인) + `CandleQueryService.getCryptoCandles`·`BithumbRestCandleProvider`의 `MARKET_DATA_PROVIDER_ERROR` 발생 지점 전수 확인 + `git show 5ad19e8d -- src/test`(신규 테스트 유무) (빌드는 재실행하지 않음 — `5ad19e8d`에서 통과 확인됨) | spec.md §FEED-012 결정 0~4, docs/api-contracts.md "코인 체결의 차이" 소절·실패 처리 표, ai/api-routes.md 66·92행, ai/prd.md 229행, ADR-0003(테스트 레벨), CLAUDE.md 규칙 3·7 |
| 06:15 | implementer | `gradlew.bat -p "C:\Users\user\orca\workspaces\tradeclass-api\2-ai" spotlessApply build --console=plain`(3307건, BUILD SUCCESSFUL) (재리뷰 권장 2건 반영 — 카드·기사 시각 `LocalDateTime` 확대, prd.md 229행 정정) | 재리뷰 권장 1~2, spec.md §FEED-012 결정 4, `NarrativePromptBuilder.postSellPrompt` 마지막 지시 줄, CLAUDE.md 규칙 10 |
| 23:53 | implementer | `gradlew.bat -p "C:\Users\user\orca\workspaces\tradeclass-api\fix-ai-rest" compileJava` 통과 + `spotlessApply spotbugsMain` 통과(`PostSellFeedbackContext`가 `Trade`를 담아도 `EI_EXPOSE_REP` 미발생 확인) (이슈 #282 tasks 1번 — 컨텍스트 리더·주식 리더 신설, 오케스트레이터 전환) | tasks-282.md 1번, spec.md §FEED-012 결정 5(빈 경계 표·lazy 초기화 문단), docs/conventions.md(레이어·Lombok), ADR-0002, agent-mistakes.md 2026-07-29(record SpotBugs)·2026-08-01(Write LF ↔ spotlessApply) |
| 00:10 | implementer | `gradlew.bat -p "C:\Users\user\orca\workspaces\tradeclass-api\fix-ai-rest" compileJava compileTestJava spotlessCheck spotbugsMain` 통과 + `test --tests CryptoPostSellFeedbackReaderTest`(18건 0실패, 결과 XML 시각 대조) (이슈 #282 tasks 2번 — `CryptoPostSellFeedbackDbReader` 신설, 코인 리더를 4단계 순서로 재배열) | tasks-282.md 2번, spec.md §FEED-012 결정 5(트랜잭션 B·C·자기호출 함정), docs/conventions.md(레이어·Lombok), ADR-0002, agent-mistakes.md 2026-08-01(Write LF ↔ CRLF) |
| 00:21 | tester | `gradlew.bat spotlessApply compileTestJava` + `spotlessCheck test --tests CryptoPostSellFeedbackReaderTest --tests CryptoPostSellFeedbackDbReaderTest`(20 + 9건 0실패, 결과 XML 시각 대조) + 순서 단정 변이 확인(`buildPeerComparison`을 REST 앞으로 옮겨 `VerificationInOrderFailure` 재현 후 `md5sum` 대조로 원복) (이슈 #282 tasks 2번 테스트) | tasks-282.md 2번(검증 줄 — 두 관심사 분리), spec.md §FEED-012 결정 5·§C-9·§C-4, ADR-0003(단위 레벨), docs/conventions.md 테스트 작성 규칙 |
| 00:45 | implementer | `gradlew.bat spotlessApply compileTestJava` + `test --tests CryptoPostSellFeedbackTransactionBoundaryIntegrationTest --tests "PostSellFeedback*IntegrationTest" --tests "CryptoPostSellFeedback*IntegrationTest"`(37건 0실패, 결과 XML 시각 대조) + 변이 확인(`PostSellFeedbackReader.read`에 `@Transactional`을 되살려 캔들 4종이 `[true,true,true,true]`로 red 되는 것 확인 후 원복, `git diff -- src/main` 비어 있음) (이슈 #282 tasks 3번 — 트랜잭션 경계 통합 테스트) | tasks-282.md 3번, spec.md §FEED-012 결정 5(트랜잭션 A·B·C ↔ REST 구간), ADR-0003(Testcontainers 통합), `PostSellFeedbackBoundaryIntegrationTest`·`FeedbackQueryCacheConnectionHoldingIntegrationTest` 선례, agent-mistakes.md 2026-08-04(UNIQUE 픽스처·픽스처 변이 확인)·2026-08-01(Write LF ↔ spotlessApply) |
| 00:50 | implementer | `gradlew.bat -p "C:\Users\user\orca\workspaces\tradeclass-api\fix-ai-rest" compileJava spotlessJavaCheck` 통과 (이슈 #282 tasks 4번 — `PostSellFeedbackReader` 클래스 Javadoc 개정, "알려진 한계" 문단 제거) | tasks-282.md 4번, spec.md §FEED-012 결정 5(빈 경계 표 ↔ 구현 대조), CLAUDE.md 규칙 7·10(문서 대상 여부 판정), docs/conventions.md |
| 09:10 | reviewer(리뷰) | `git diff dev...HEAD`(19파일, 이슈 #282) + spec.md §FEED-012 결정 5 빈 경계 표 ↔ `PostSellFeedbackReader`/`PostSellFeedbackContextReader`/`StockPostSellFeedbackReader`/`CryptoPostSellFeedbackReader`/`CryptoPostSellFeedbackDbReader` 5개 클래스 1:1 대조, 주식 `read()` 본문을 `diff`로 옛 `PostSellFeedbackReader`와 바이트 단위 대조(값·순서 불변 확인), `Trade`의 lazy 필드 4종 ↔ `Hibernate.initialize` 호출 2종 ↔ 조립 코드의 실제 접근 필드 대조, `CryptoPostSellFeedbackTransactionBoundaryIntegrationTest` 상속 기반 관측 설계 타당성 검토, 기존 주식 `*IntegrationTest` 8파일 diff 없음 확인 | spec.md §FEED-012 결정 5, tasks-282.md, docs/conventions.md(레이어·Lombok·패키지 가시성), ADR-0002·0003, run-log.md 기존 항목(mutation 확인 기록) |
| 09:20 | planner(동기화) | `git diff dev...HEAD -- '**/*Controller.java'`(빈 결과) 확인 — 새 controller·엔드포인트 없음, `PostSellFeedbackResponse` 필드 변경 없음이 실제 diff와 일치함을 재확인. reviewer 참고 1건(`PostSellFeedbackService` 클래스 Javadoc 1단계 표) 검토 — `reader.read(...)`가 이번 PR로 무트랜잭션 오케스트레이터가 됐는데 표는 옛 `@Transactional(readOnly = true)`를 남긴 것이 사실이라 한 줄만 고침(`트랜잭션 없음(오케스트레이터) — 협력자별 트랜잭션으로 원장·분봉·카드를 읽는다`), `gradlew spotlessCheck compileJava` 통과 | CLAUDE.md 규칙 7(controller↔문서 동기화 대상 판정), ai/api-routes.md·api-contracts.md(갱신 불요 확인), PostSellFeedbackReader.java·PostSellFeedbackContextReader.java(현재 트랜잭션 경계 재확인) |
| (문서) | implementer | `ai/specs/012-ai-feedback/spec.md` 4개 자리(§탐지 알고리즘(코인) 의사코드·§뉴스 매칭 범위·§실패 처리 표·FEED-005) 갱신 + `ai/prd.md` §3 197행 근거 칸 갱신 (이슈 #285 tasks-285.md 5번 — 문서 동기화, 다른 항목의 코드는 아직 없어 코드 검증 없음) | tasks-285.md 5번, ADR-0017 §결정 1~6, CLAUDE.md 규칙 10 |
| (implementer) | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" compileJava compileTestJava --console=plain`(BUILD SUCCESSFUL) (이슈 #285 tasks-285.md 1번 — `NewsCollectionService.collectForInstrument(Instrument)` 신설) | tasks-285.md 1번, ADR-0017 §결정 2, spec.md §C-6(NewsCollectionService), 기존 `collectNews()`·`save()` 구조 |
| (implementer) | implementer | `gradlew.bat compileTestJava`(2번 항목 생성자 시그니처 변경으로 실패 상태였던 것을 수정) + `test --tests CryptoPriceMoveWatcherTest`(전 nested class 통과) (이슈 #285 tasks-285.md 3번 — `CryptoPriceMoveWatcherTest` 온디맨드 수집 오케스트레이션 단위 테스트) | tasks-285.md 3번, ADR-0017 §결정 3, `NewsCollectionIntegrationTest` mock 선례 |
| (implementer) | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" compileJava compileTestJava --console=plain`(둘 다 BUILD SUCCESSFUL) (이슈 #285 tasks-285.md 4번 — `CryptoPriceMoveWatcherIntegrationTest` Testcontainers 통합 테스트 5건 추가) | tasks-285.md 4번, ADR-0017 §결정 3·5·6, `NewsCollectionIntegrationTest`의 `@MockitoBean NewsCollector` 선례 |
| (리뷰) | reviewer(리뷰) | `git diff dev...HEAD`(PR #290, 13개 파일) + ADR-0017·ADR-0014·tasks-285.md·이슈 #285 본문 대조 + `CryptoPriceMoveWatcher.java`·`NewsCollectionService.java`·`NewsMatcher.java`(쿼리 확인)·전 테스트 diff 정독 + `./gradlew.bat compileJava compileTestJava`(BUILD SUCCESSFUL) | ADR-0017, ADR-0014, docs/conventions.md 레이어 규칙, ai/specs/012-ai-feedback/spec.md·tasks-285.md, CLAUDE.md 규칙 10 |
| (implementer) | implementer | `git ls-tree origin/dev db/migration` + `gh pr diff 381/371 --name-only`로 다음 번호 확정(PR #381이 V36 선점 → **V37**) 후 `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" compileJava spotlessJavaCheck --console=plain`(둘 다 BUILD SUCCESSFUL) (이슈 #386 tasks-386.md 1번 — `V37__add_journal_columns_to_trade_feedbacks.sql` + `TradeFeedback` 필드 2개 매핑) | tasks-386.md 1번, spec.md §C-8 컬럼 타입 표(`journal_fingerprint` VARCHAR(64) NULL / `journal_regenerations` INT NOT NULL DEFAULT 0)·§데이터 모델·§FEED-013 결정 3, ADR-0004, CLAUDE.md 규칙 8 |
| (implementer) | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" compileJava compileTestJava --console=plain` + `test --tests FeedbackLlmPropertiesTest --tests NarrativeServiceTest --tests OpenAiNarrativeGeneratorTest --tests PostSellFeedbackServiceTest`(Docker 불필요분만, 전부 통과) + `spotlessApply` (이슈 #386 tasks-386.md 2번 — `llm.max-tokens` 1024·`llm.max-journal-regeneration` 신설·`feedback.journal` 블록 신설) | tasks-386.md 2번, spec.md §C-7(값 정본, yml ↔ `@DefaultValue` 양쪽 기재 관례)·§FEED-013 결정 3·5, `FeedbackDetectionConfig` 등록 선례, ADR-0011 |
| 23:52 | tester | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" spotlessApply spotlessJavaCheck test --tests` 로 3회 분할 실행 — ① 단위(`FeedbackJournalPropertiesTest`·`FeedbackJournalPropertiesYamlTest` 신설 + `FeedbackLlmPropertiesTest`·`NarrativeServiceTest`·`OpenAiNarrativeGeneratorTest`·`PostSellFeedbackServiceTest`) 통과 ② Testcontainers(`FeedbackSchemaConstraintsTest`·`TradeFeedbackRepositoryTest`·`FeedbackLlmPropertiesIntegrationTest`) — `FeedbackSchemaConstraintsTest` 1건 실패 후 새 컬럼 2개 반영해 통과 ③ 쓰기 경로(`PostSellFeedbackNarrativeIntegrationTest`·`TradeFeedbackWriterTest`) 통과. Docker 기동 상태에서 `ddl-auto=validate` 부팅으로 항목 1 실검증 | tasks-386.md 1·2번, ADR-0003(단위·슬라이스), spec.md §C-7·§C-8, `FeedbackDetectionProperties*Test` 선례 |
| (implementer) | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" spotlessApply compileJava --console=plain`(BUILD SUCCESSFUL) (이슈 #386 tasks-386.md 3번 — `JournalService` 읽기 메서드 2종·`JournalContentDto`·`BuyTradeJournalRepository.findAllByBuyTradeIdIn`·`SellAllocationQueryService.getAllocatedBuyTradeIds` 신설) | tasks-386.md 3번, spec.md §C-6(`journal`·`portfolio` 신설 표와 "회원 ID를 인자로 받지 않는다")·§FEED-013 결정 4, ADR-0002·conventions.md(다른 도메인 리포지터리 직접 주입 금지) |

| 00:10 | tester | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" spotlessApply spotlessJavaCheck test --tests JournalServiceTest --tests BuyTradeJournalRepositoryTest --tests SellAllocationQueryServiceTest`(항목 3 — 단위 4건 + `@DataJpaTest` 7건 추가, 전부 통과). Hibernate `Statistics.getPrepareStatementCount()`로 `findAllByBuyTradeIdIn` 쿼리 1회 확인, 임시 프로브로 파생 파인더 A/B 측정(`afterFind=1 afterGetId=1 afterInitialize=2`) 후 프로브 제거 | tasks-386.md 3번, ADR-0003(단위·슬라이스), spec.md §C-6·§FEED-013 결정 4, `PostCommentRepositoryTest`의 `Statistics` 선례 |
| (implementer) | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" spotlessApply compileJava --console=plain` + `spotbugsMain`(둘 다 BUILD SUCCESSFUL, 컬렉션 필드 record의 `EI_EXPOSE_REP` 확인) (이슈 #386 tasks-386.md 4번 — `PostSellJournalReader`·`JournalDigestDto` 신설) | tasks-386.md 4번, spec.md §FEED-013 결정 3(지문 범위·`null`)·결정 4(상한은 일기 있는 것만·본문 절단)·§C-6, ADR-0002, `HoldExtremes`·`Sha256BcryptPasswordEncoder` 선례 |
| (implementer) | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" spotlessApply compileJava compileTestJava` + `test --tests SellAllocationQueryServiceTest`(9건 전부 통과, Docker 기동 상태) (이슈 #386 항목 4 마무리 — 배분 조회가 체결시각을 함께 돌려주도록 `getAllocatedBuyTrades`·`AllocatedBuyTradeDto`로 교체) | 메인 세션 결정(시각 출처는 배분 조회), spec.md §LLM 프롬프트 일기 덩어리(`- 매수 09:30:`)·§FEED-013 결정 4, 이슈 #275(`multiDayHold`), ADR-0002 |

| 00:33 | tester | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" spotlessApply spotlessJavaCheck test --tests PostSellJournalReaderTest`(항목 4 — 단위 15건 신설, 전부 통과) + `test --tests SellAllocationQueryServiceTest --tests JournalServiceTest`(시그니처 변경 회귀 확인, 통과). 지문 포맷 `.SSSSSS`의 마이크로초 미만 절단은 별도 JDK 프로브로 확인(`collide=true`, DATETIME(6)이라 도달 불가) | tasks-386.md 4번, spec.md §FEED-013 결정 3·4, ADR-0003(단위 레벨) |
| (implementer) | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" spotlessApply compileJava compileTestJava` + `test --tests NarrativePromptBuilderTest --tests NarrativeServiceTest --tests NarrativeTemplateBuilderTest --tests PostSellFeedbackServiceTest`(153건 전부 통과) + 임시 프로브로 조립 결과를 §LLM 프롬프트 예시와 문자 대조 후 삭제 (이슈 #386 tasks-386.md 5번 — 시스템 프롬프트 2줄·일기 덩어리·6~8문장 지시) | tasks-386.md 5번, spec.md §LLM 프롬프트(시스템 블록·일기 두 덩어리 원문)·§FEED-013 결정 1·5·6, 이슈 #275(`multiDayHold`), SpotBugs `VA_FORMAT_STRING_USES_NEWLINE` 관례 |

| 00:48 | tester | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" spotlessApply spotlessJavaCheck test --tests NarrativePromptBuilderTest --tests NarrativeServiceTest --tests NarrativeTemplateBuilderTest --tests PostSellJournalReaderTest`(항목 5 — 일기 덩어리 단위 10건 추가, 전부 통과) + `test --tests PostSellFeedbackServiceTest --tests PostSellFeedbackNarrativeIntegrationTest`(record 컴포넌트 2개 추가 회귀 확인, 통과) | tasks-386.md 5번, spec.md §LLM 프롬프트 예시 블록·§FEED-013 결정 1·5·6, ADR-0003(단위 레벨) |
| 01:07 | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" spotlessApply compileJava compileTestJava` + `test --tests PostSellFeedbackServiceTest --tests TradeFeedbackWriterTest`(24+4건) + `test --tests TradeFeedbackRepositoryTest`(8건, Docker 기동) — 전부 통과 (이슈 #386 항목 6 마무리 — 시그니처 변경을 못 따라간 테스트 호출부 35개 컴파일 오류 복구) | tasks-386.md 6번, spec.md §FEED-013 결정 3(사유 둘·카운터 둘·지문 `null`), ADR-0002, ai/agent-mistakes.md(2026-08-01 `spotlessApply` 줄바꿈) |
| (문서) | planner(동기화) | `git diff 58b61a8c..HEAD --name-only`(이슈 #386 착수 기점 — 로컬 `dev`가 PR #385 머지 전이라 `dev..HEAD`는 OAuth 커밋까지 끌어온다)로 `*Controller.java` 0건 확인(→ `api-routes.md` 대상 아님) 후 문서 4개 갱신 (이슈 #386 tasks-386.md 8번 — `api-contracts.md` 매도 회고 소절에 투자일기 재생성 사유 4문단 추가·"그 밖에는 재생성하지 않는다"와 "평생 LLM 호출 = 최초 1회 + 재생성 상한" 정정, `prd.md` §3에 FEED-013 행 신설·3차 "AI 주간·월간 리포트" 줄의 "투자일기에 의존하지 않으므로" 정정, `spec.md` §FEED-013 9개·§완료 조건 17개 체크). 코드 무변경이라 빌드 미실행 | tasks-386.md 8번, CLAUDE.md 규칙 7·10, spec.md §FEED-013 결정 3·5·§C-5 게이트 표·§완료 조건, ADR-0002 |
| 01:55 | reviewer(리뷰) | `git diff 58b61a8c...HEAD`(43파일, 커밋 10개 — 로컬 `dev`가 PR #385 머지 전이라 분기점 SHA로 범위를 고정) + `git ls-tree origin/dev`·`HEAD -- db/migration`으로 V37 선점 확인(V36은 PR #381), `PostSellFeedbackService`·`PostSellJournalReader`·`TradeFeedbackWriter`·`TradeFeedback`·`NarrativePromptBuilder` 전문 대조, 재생성 두 사유 × 성공/실패 × 상한 8조합을 지문·카운터 전이로 수행 추적, `@Transactional` 부착 지점과 도메인 주입 대상(서비스 한정) 확인 | conventions.md(레이어·Lombok·DTO·리뷰 체크 질문), ADR-0002·0003·0004·0011·0021, spec.md §FEED-013 결정 1~6·§C-5·§C-6·§C-7, tasks-386.md 1~8번, CLAUDE.md 규칙 7·10 |
| (implementer) | implementer | `gradlew.bat -p "C:\Users\user\Desktop\tradeclass-api" spotlessApply compileJava` + `test --tests PostSellJournalReaderTest --tests FeedbackJournalProperties*Test --tests NarrativePromptBuilderTest --tests PostSellFeedbackServiceTest`(BUILD SUCCESSFUL) (이슈 #386 리뷰 권장 ①② 반영 — 상한 비교 `>=`·프로퍼티 하한 검증·본문 개행 접기) | reviewer 리뷰 권장 ①②, spec.md §C-7·§C-4·§FEED-013 결정 4·6, `FeedbackNewsProperties`·`NewsItemTruncator` 선례, CLAUDE.md 규칙 8·10(코드 무증감이라 prd 대상 아님) |

## 모니터링 (사람용 요약)
- (이슈 #386) — 리뷰 권장 ①②를 반영했다. 상한 비교를 `>=`로 바꾸고 `FeedbackJournalProperties`에 `max-buy-journals`·`max-journal-chars` 1 미만 기동 실패 검증을 넣었으며(선례 `FeedbackNewsProperties`), `truncate`가 **자르기 전에** 개행을 공백 하나로 접어 프롬프트 구조 위조를 조립 단계에서 막는다. 지문은 `updated_at`을 해싱하므로 영향이 없고, 기존 골든 마스터(한 줄 본문)와 단위 테스트는 수정 없이 전부 통과했다.
- 01:55 — 이슈 #386 전체 diff 코드 리뷰: **차단 0건 / 권장 3건 / 참고 3건 — 머지 가능.** 두 재생성 사유의 분리(일기 판정이 `narrative_finalized`를 읽지도 쓰지도 않고 카운터 둘이 서로를 소모하지 않음), 지문 수명주기 4지점(최초 저장·두 사유의 성공 갱신·실패 유지), 트랜잭션 경계(LLM·`PostSellJournalReader` 모두 밖), 도메인 경계(서비스만 주입) 전부 규정대로다. 권장은 ① `PostSellJournalReader.selectBuyJournals`의 상한 비교가 `==`라 `max-buy-journals <= 0`이면 상한이 통째로 무효가 되고(기존 메모가 적은 "0이면 0건"과 반대 방향으로 틀린다), `max-journal-chars`가 음수면 `substring`이 500을 낸다 — `FeedbackNewsProperties`처럼 compact 생성자 하한 검증 + `>=` ② 일기 본문의 개행을 중화하지 않아 프롬프트 구조(덩어리 헤더·사실 줄) 위조가 가능 ③ `prd.md` FEED-013 행 근거 칸에 "PR 생성 후 번호 갱신 필요"가 남음(CLAUDE.md 규칙 10). 참고는 V37↔V36(PR #381) 머지 순서 의존, 조회마다 배분 조회가 2번 도는 중복, `JournalDigestDto.isEmpty()` 운영 미사용이다.
- (이슈 #386) — 항목 6의 기존 테스트 호출부는 지문 인자에 `null`, 사유에 `RegenerationReasons(false, true)`를 넣어 원래 의도(일기 없음 · 흐름·집단 사유만 성립)를 그대로 유지했다. `TradeFeedbackWriterTest`의 `getDeclaredMethod` 단정은 컴파일 오류로 드러나지 않아 따로 찾아 고쳤다 — 안 고쳤으면 런타임에서만 터진다.
- (이슈 #386) — 항목 5의 일기 골든 마스터는 spec §LLM 프롬프트 예시 블록을 **문장까지 그대로** 옮겨 전문 비교했고 첫 실행에서 통과했다. 결정 1(일기 없으면 3차와 동일)은 기존 골든 마스터가 "일기 없는 프롬프트 = 3차 전문"을 이미 못 박고 있어, 반대편인 "일기가 붙어도 앞부분이 한 글자도 안 바뀐다"를 3차 마지막 지시를 잘라낸 접두사 비교로 추가해 양방향을 닫았다.
- (이슈 #386) — 항목 4 지문의 시각 포맷이 `uuuu-MM-dd'T'HH:mm:ss.SSSSSS`라 **마이크로초 미만 자리는 예외 없이 잘린다**(프로브 실측: `.123456001`과 `.123456999`가 같은 문자열). `updated_at` 컬럼이 `DATETIME(6)`이라 DB를 거친 값에서는 도달할 수 없어 결함이 아니고, 오히려 `LocalDateTime.toString()`의 뒷자리 생략 문제를 피하려는 고정 포맷의 의도대로다. 컬럼 정밀도를 올리는 변경이 생기면 이 자리가 함께 바뀌어야 한다.
- (이슈 #386) — 항목 3의 중복 제거 근거가 실제 스키마와 어긋난다. `getAllocatedBuyTradeIds` Javadoc과 tasks-386.md 3번이 "lot ↔ 매수 체결이 1:1이라는 것은 스키마로 강제돼 있지 않다"고 적었지만 `holding_lots`에는 V10부터 `uk_holding_lots_buy_trade UNIQUE (buy_trade_id)`가 있다. **중복이 실제로 들어올 수 있는 경로는 `trade_allocations`** 쪽으로, `(sell_trade_id, holding_lot_id)`에 UNIQUE가 없어 같은 lot이 한 매도에 두 번 배분될 수 있다 — `LinkedHashSet` 제거 자체는 여전히 필요하고 테스트도 그 경로로 재현했다. 근거 문장만 틀렸다.
- (이슈 #386) — `findAllByBuyTradeIdIn`의 `join fetch` 근거도 실측과 어긋난다. "지연 프록시의 `getId()`가 초기화를 부를 수 있다"고 적혀 있으나, 같은 연관을 파생 파인더로 조회해 재보니 `getId()`는 추가 쿼리를 내지 않았고(1회 유지) 다른 프로퍼티를 건드릴 때 비로소 2회가 됐다. `join fetch`가 쿼리 1회를 보장하는 것은 사실이라 동작은 옳다.
- (이슈 #386) — 항목 1의 컬럼 추가로 `FeedbackSchemaConstraintsTest`의 `trade_feedbacks` 컬럼 맵 전체 비교가 깨졌다(변경 파일 목록에 없던 테스트다). 그 테스트는 "새 컬럼이 조용히 섞여 들어오는 것"을 막으려고 부분 비교를 일부러 안 쓰므로 **깨지는 것이 설계대로**이며, 기대 맵에 `journal_fingerprint`(YES)·`journal_regenerations`(NO)를 넣고 DDL 기본값 케이스에 `journal_regenerations = 0`을 더해 닫았다.
- (이슈 #386) — 설정 항목 2번에서 `FeedbackLlmPropertiesTest`의 오버라이드 값을 1024 → 2048로 바꿨다. `max-tokens` 기본값이 1024가 되는 순간 그 케이스가 기본값과 같은 값을 덮어써 아무것도 검증하지 못한 채 계속 통과하기 때문이다. 신설 드리프트 테스트 2종(`FeedbackJournalProperties*`)은 tester 몫으로 남겼다.
- (이슈 #386) — 항목 5에서 `PostSellPromptDto`에 컴포넌트 둘(`buyJournals`·`sellJournalContent`)이 늘어 **기존 생성 지점 14곳이 전부 컴파일에서 깨졌다**(운영 1 + 테스트 13). 전부 `List.of(), null`을 덧붙여 닫았고 그 덕에 **매도 회고 골든 마스터 단정이 한 글자도 바뀌지 않은 채 통과한다** — 그것이 결정 1(일기 0건이면 3차와 동일)의 회귀 근거다. 운영 쪽 `toPromptInput`도 지금은 빈 값을 넘긴다: 일기는 응답(`facts`)에 실리지 않으므로 실제 배선은 재생성 판정(항목 6)이 붙인다.
- (이슈 #386) — 항목 4에서 **프롬프트 줄머리 시각의 출처가 없다는 것이 드러나** 메인 세션 결정으로 배분 조회를 확장했다. `- 매수 09:30:`의 시각은 그 매수 체결의 `executed_at`인데 항목 3의 `getAllocatedBuyTradeIds`는 id만 줬다. 그 시각은 이미 같은 쿼리의 **정렬 키**(`lot.executedAt`)라 값으로 함께 내보내는 것뿐이고 조회·조인이 늘지 않는다 — `getAllocatedBuyTrades` → `List<AllocatedBuyTradeDto>`로 바꾸고 `SellAllocationQueryServiceTest`(항목 3 커밋분)를 함께 고쳤다. 대안이던 "`buyAt`(가장 이른 매수 시각)을 모든 줄에" 안은 lot이 둘 이상일 때 **틀린 시각이 조용히 실린다.** 줄머리 표기 형식(`HH:mm` vs 날짜 포함)은 `multiDayHold` 규칙대로 항목 5 몫이라 여기서는 `LocalDateTime`을 포맷 없이 넘긴다.
- (이슈 #386) — 지문 입력의 시각은 `LocalDateTime.toString()`이 아니라 자릿수 고정 포맷(`uuuu-MM-dd'T'HH:mm:ss.SSSSSS`)으로 적었다. `toString()`은 뒷자리 0을 생략해 `10:00:00.000000`이 `10:00`으로 나오는데, 같은 값이 항상 같은 문자열이어야 하는 자리라 표기를 컬럼 타입(`DATETIME(6)`)에 맞춰 고정했다. 정렬은 `Set` 순회나 `hashCode`가 아니라 문자열 자연 정렬만 쓴다.
- (이슈 #386) — 항목 3의 매수 회고 일괄 조회는 `@Query`에 `join fetch j.buyTrade`를 넣었다. 파생 이름만 두면 결과 매핑에서 `journal.getBuyTrade().getId()`가 지연 프록시를 건드리는데, 이 엔티티들이 필드 접근이라 프록시 초기화가 일어나 조회 건수만큼 쿼리가 늘어날 수 있다 — "lot 수만큼 쿼리가 늘어나면 안 된다"는 제약을 이름 하나로 보장할 수 없어 조회를 명시했다. 이 시점에는 두 도메인의 새 메서드를 아무도 부르지 않는다(항목 4가 소비자다).
- (이슈 #386) — 마이그레이션 번호는 계획 시점의 V36이 아니라 **V37**이다. `origin/dev` 최신은 V35지만 열려 있는 PR #381(튜토리얼)이 V36을 이미 쥐고 있어 tasks 문서의 확인 절차대로 재확인해 한 칸 밀었다.
- 09:10 — 이슈 #282(코인 매도 회고 트랜잭션 경계 4단계 분리) 코드 리뷰: 차단 0건. 주식 경로 값·순서 불변 확인(diff 동일), lazy 초기화(instrument·stockReplaySession) 빠짐없음, B→REST 4종→C 순서와 오케스트레이터 무트랜잭션 확인, 통합 테스트 진짜(순서·트랜잭션 활성 여부 상호 검증) — 머지 가능. 참고 1건(PostSellFeedbackService 클래스 Javadoc의 옛 1단계 트랜잭션 서술이 이번 리팩터로 낡음, 이 PR 범위 밖 파일).
- 11:40 — 문서 리뷰 완료, 차단 9건(노출 판정 전장 기사 역전, UNIQUE(url) 잔존 모순, 코인 경로 미정의, 장마감 배치 부재, 배치용 전일치 분봉 조회 경로 부재, PRD 수집주기 모순 등) / 권장 12건.
- 12:20 — Part D(개장 전 브리핑) 정밀 리뷰: 차단 8건(08:40 잔존 모순, 크론 zone 누락, 스케줄러 풀 미증설, 09:00 전 배치 미완료, 브리핑 행 부재 상태 미정의, NOT_YET/EMPTY 우선순위 충돌, 재생세션 조회 경로 부재, 공시 00:00:00 구간 역전) / 권장 9건 — 머지 불가.
- 12:35 — Part B(매도 직후 피드백·반사실·집단 비교) 정밀 리뷰: 차단 9건(장 마감 게이트의 기준 날짜 미정의, peerComparison 기준 카드·카드 0건 미정의, 15:35 배치와 15:30 게이트·재생성 1회 충돌, 서비스 날짜↔원본 거래일 변환 경로 부재, 특정 시점 보유자 집계 근거 부재(holdings에 이력 없음), LLM 실패 시 서술 처리 spec↔계약 모순, 재생성 프롬프트의 반사실 포함 모순, 보유 구간 극값 고가/종가 미정, 예시 수치 불일치) / 권장 7건 / 참고 5건 — 머지 불가.
- 12:35 — Part A(변동 원인 카드) 정밀 리뷰: 차단 8건(09:00 windowStart 유니크 충돌, 09:00/15:30 분봉 존재 전제(003 Decision Gate 미확정), W 구간 정의·결측 분봉 미정, 코인 이력이 가격 아닌 수익률이라 p_past 불가, 이력 타임스탬프 부재, 크론 zone 누락, 스케줄러 pool 미증설, revealAt 잔존 문서 불일치) / 권장 10건 — 머지 불가.
- 12:10 — Part C(종목 뉴스 목록·요약) 정밀 리뷰: 차단 5건(items 범위와 신규 회귀조건 충돌, 공시 00:00 클램프로 장중 접수 공시 09:00 유출, 요약 배치 08:40/08:45 불일치, 코인 온디맨드 동시성·NONE 캐시 미정, 재생성 프롬프트가 적발 표현 미전달) / 권장 13건 / 참고 5건.
- 13:10 — Part C·D 재리뷰(4라운드, 30건 수정분 검증): 차단 12건(데이터 모델·완료조건에 '조회 시 생성+1시간 캐시' 잔존, 코인 배치 이관 미전파, 스케줄러 풀 10 부족(실제 12개), api-contracts FULL 범위·PRE_MARKET 예시 공시·'원본 거래일 기사만' 잔존, items↔summaryScope '정확히 같다' 거짓, Part C 요약 행 부재 상태 미정의, 브리핑 우선 생성 근거 오류, 새 기사 판정 기준 미정, 코인 행 선택 규칙 미정) / 권장 8건 / 참고 3건 — 머지 불가.
- 12:25 — 4차(2차 정정분 30건 회귀 확인) Part A·B 재리뷰: 차단 18건(코인 자정 문제가 windowEnd로 이동해 완료조건 불변식 성립 불가, 코인 근거 창 30/5↔35/0 이중 기재, 15:30 종가 리터럴이 파생사실·반사실에 잔존, post_sell_flow_ready 의미 3중 불일치·regeneration_attempts 스키마 미지원, NO_EVENT 시 재생성 영구 미실행, peer_stats 유니크에 서비스 날짜 부재, holding_lots.quantity 미존재 컬럼, 스케줄러 풀 7개 누락 계산, 코인 요약 1시간 캐시 잔존, 계약 예시 priceMoveId/narrativeSource 누락 등) / 권장 8건 / 참고 6건 — 머지 불가.
- 13:30 — 4라운드 횡단 정합성 리뷰(파트 사이·문서 사이·문서↔코드): 차단 16건(데이터모델 주석의 조회 시 생성+1시간 캐시 잔존, 완료조건 1065/1059/1018이 1103/1088/1054와 동시 만족 불가, §코인 수익률 이력 존재하지 않는 절 참조, 코인 근거 창 30/5↔35/0 이중 기재, 공시 09:00 클램프↔FULL 전용 충돌, items↔summaryScope '정확히 같다' 거짓, 스케줄러 풀 10<실제 12, 15:35↔15:32, 재생성 트리거 spec↔계약 반대, post_sell_flow_ready·regeneration_attempts 스키마 미지원, '배치 전용' 금지와 조회 시 반사실 계산 충돌, holding_lots.quantity 미존재 컬럼, 시가 갭 +1분 예외 문구↔공식) / 권장 14건 / 참고 6건 — 머지 불가.
- 14:20 — §확정값 단일 출처 개편분(5라운드) 리뷰: 차단 8건(시가 갭 revealTime 완료조건이 §노출 판정 공식으로 통과 불가·1146↔1147 모순, `전장` 하한 "D-1 마지막 분봉"을 얻을 경로가 §C-6에 없고 시장 단위 브리핑에서는 정의 불성립, api-contracts의 items 범위·PRE_MARKET 하한 15:30·revealTime 갭 예외 누락·Part C EMPTY 정의가 §C-2/§C-4와 모순(3라운드째 미수정), 코인 요약·브리핑 행의 origin_trade_date 값 정의 유실, 09:00 유니크 충돌 회귀 조건 유실) / 권장 13건 / 참고 4건 — 머지 불가. 값 중복은 spec 내부는 대폭 줄었으나 튜닝 표·탐지 의사코드·api-contracts·ADR-0011에 30여 항목이 잔존.
- 15:10 — 8건 정정분(6라운드, PR 직전 최종 확인): 차단 3건 / 권장 8건 / 참고 6건 — 머지 불가. 8건 자체는 전부 해소됐으나 `전장`·`FULL` 하한을 벽시계로 옮기는 과정에서 `FULL` 전환 시점 표현이 갈렸다 — api-contracts 490이 이번 커밋에서 "15:30 이후" → "마지막 분봉 이후"로 **역방향 수정**되어 바로 위 표(487)와 모순, api-contracts 477의 items 하한 "직전 거래일 마지막 분봉"·spec 1175 완료 조건도 §C-2와 불일치. 신설된 §C-2-1은 피참조 0건(리터럴 근거를 대는 5곳이 모두 §C-2를 가리킨다)이고 §C-4 표는 삽입 문단으로 렌더링이 깨졌다.
- 16:35 — PR #148(plan.md 신설) 리뷰: 차단 1건(완료 조건 배정표 누락 — 원장 불변 조건 미배정, 노출 게이트의 매도 회고(Part B) 게이트 조건 3개 미배정) / 권장 4건(plan.md 구조가 다른 spec plan.md·`_template`와 달리 API 설계·데이터 모델·ADR 링크가 없고 내용상 tasks.md에 더 가까움, team-conventions.md 500줄 기준과의 불일치를 문서 갱신 없이 방치, 자기 규칙(§확정값 재진술 금지)을 일부 스스로 어김, PR 체크리스트의 'ADR 링크' 주장과 달리 plan.md 본문에 ADR 문자열 없음) — 순서 판단(4번 선행, 5번 Part C·D 결합) 근거는 spec.md와 정확히 일치 확인, api-routes/api-contracts 갱신 불요 주장도 맞음.
- 16:05 — 8건 정정분(7라운드, PR 직전 최종 게이트): 차단 2건 / 권장 11건 / 참고 6건 — 머지 불가. 이번 8건은 전부 해소됐고 지난 라운드의 역방향 수정·고아 절 유형은 재발하지 않았다(`FULL` 전환 시점 27곳·§C-2-1 참조 5건 전수 일치, 계약 예시 수치 재계산 일치). 남은 차단은 이번 수정분 밖이다 — 완료 조건의 리플렉션 `zone` 검사가 기존 fixedRate `@Scheduled` 3개에서 구조적으로 실패하고(통과 불가), 매도 회고 프롬프트의 문장 수가 2~3↔3~4로 갈렸다.

- 17:10 — PR #148 정정 커밋(85d450f) 재검토: 차단 2건은 실제로 해소(원장 불변 8개 이슈 공통화·이슈 #147 반영 확인, 노출 게이트 ⑬~⑮ 단일 배정, 중복·누락 없음). 그러나 새로 추가된 "총 91건 검산" 표 자체가 산술 오류다 — 8행 합계가 93인데 표에는 "91"로 적혀 있고, 원인은 (a) "외부 API 키 없이 build" 단일 조건을 이슈1·3에 중복 카운트 (b) 상태값 절 Part C·D 실제 4건을 5건으로 (c) 배치 절 개장 전 배치 실제 5건을 6건으로 각각 과다 계산. 권장 4건 중 1·3은 실질 해소, 2는 부분 해소(파일 경로는 생겼으나 리터럴 "ADR" 문자열은 여전히 없음 — 011/003처럼 `[ADR-NNNN](경로)` 형식은 아님), 4는 후속 이슈 기록 자체는 됐으나 근거 통계(22건/2.6배)가 `gh pr list` 실측(41건/2.18배)과 불일치. 신규 결함 2건 추가 발견 — `ai/adr/0004-flyway-migration.md` 파일 미존재(오타, 's' 누락), team-conventions 후속 근거 수치 오류. 머지 불가.

- 17:45 — PR #148 2라운드 정정 커밋(18f8227) 재검토: 지적한 3가지 원인(build-키 조건 이슈1·3 중복 카운트, 상태값 Part C·D 5건→4건 정정, 배치 개장 전 배치 6건→5건 정정) 전부 실제로 고쳐졌고, spec.md 10개 절을 처음부터 다시 세어 총 91건·8행 배정표 합계(90+원장불변1)가 정확히 일치함을 확인(신규 산술 오류 없음). ADR 링크 `0004-flyway-migrations.md` 실재 확인, 후속 이슈 통계는 `gh pr list` 재계산값(41건, median 1,090, 2.18배→"2.2배" 반올림 표기)과 정확히 일치, line 63 "09:00 하한"은 §C-2 참조로 수정됨. 차단 0건 / 권장 0건 / 참고 0건 — 머지 가능.

- 16:49 — `FeedbackLlmProperties`(record + @DefaultValue 5종)와 등록용 `FeedbackLlmConfig` 신설, `application.yml`에 `feedback.llm` 블록 추가. 컴파일 통과.

- 17:15 — `NarrativeGenerator`(반환 `Optional<String>`) + OpenAI 구현 + 테스트용 Fake 신설, `ChatClient` 빈은 `FeedbackLlmConfig`가 등록. 타임아웃은 Spring AI에서 요청 단위가 아니라 클라이언트 단위 값이라 `spring.ai.openai.timeout`이 `feedback.llm.timeout-seconds`를 참조하게 했고, 최대 토큰은 GPT-5 계열 때문에 `maxCompletionTokens`로 넘긴다. 컴파일·SpotBugs·키 없는 컨텍스트 기동 통과.

- 17:35 — 회귀 수정: `narrativeChatClient` 빈을 `NarrativeChatClientConfig`로 분리해 `FeedbackLlmConfig`를 프로퍼티 등록 전용으로 되돌렸다. `feedback` 테스트 25건 전부 통과했고, fail-fast 케이스가 `ChatClient.Builder` 부재가 아니라 `ConfigurationPropertiesBindException`으로 실패하는 것을 임시 프로브로 확인한 뒤 프로브는 삭제했다.

- 18:20 — `NarrativePromptBuilder`(시스템 1·사용자 4·재생성 1)와 입력 record 7종 신설. 조립 결과가 spec §LLM 프롬프트 예시 4종과 문자 단위로 일치하는 것을 임시 덤프로 확인한 뒤 덤프는 삭제했다. SpotBugs `VA_FORMAT_STRING_USES_NEWLINE` 2건은 `%n`(플랫폼별 CRLF) 대신 개행을 format 문자열 밖으로 빼서 해결했다.

- 19:05 — `NarrativeValidator`(부분 문자열 판정, 5줄 35표현)·`NarrativeValidationDto`·`NarrativeSource` 신설. 정규식 대신 `String.contains`를 쓴 이유는 35개가 전부 리터럴이라 표현력이 같고 어간 일반화 유혹이 구조적으로 닫히기 때문이다. **spec 확인 필요 1건 → 해소됨(19:50)** — `판단·훈수`의 가정법 3종(`았다면`·`었다면`·`였다면`)이 spec 자신의 예시인 "더 기다렸다면"(렸다면)과 "보유했다면"(했다면)을 잡지 못했다. 사용자 결정으로 spec §후검증 목록에 `했다면`·`렸다면`을 추가해 메웠다.

- 19:50 — spec §후검증 `판단·훈수`에 `했다면`·`렸다면`을 추가해(9→11, 합계 35→37) spec 본문·api-contracts 예시와 목록의 불일치를 메우고 `NarrativeValidator`·`NarrativeValidatorTest`를 함께 맞췄다. 이어 `NarrativeTemplateBuilder`(장중 카드·시가 갭·매도 회고 3종) 신설 — 입력은 3번의 record를 그대로 재사용하고, 조립된 문장 3종이 새 37표현 기준으로도 후검증을 통과하는 것을 확인했다. feedback 테스트 163건 통과.

- 20:30 — `NarrativeService`(진입점 4개)·`NarrativeResultDto` 신설로 이슈 #147 작업 항목 6개를 마쳤다. 1단계(카드·매도 회고)는 적발·실패가 한 분기로 수렴해 템플릿, 2단계(요약·브리핑)는 적발 표현을 넣어 `max-regeneration`회 재생성 후 `NONE`이다. 호출 횟수가 `max-regeneration + 1`을 넘지 않는 것을 프로퍼티 0·2로 바꿔 가며 확인했다. spec §실패 처리에 "요약·브리핑의 생성 호출 실패는 재생성 횟수를 쓰지 않고 곧바로 NONE" 행을 추가했다.

- 20:55 — `NarrativeResultDto`의 compact 생성자에 불변식 검사 3종(`source` null 금지, `NONE`이면 서술 null, `LLM`·`TEMPLATE`이면 서술 non-blank)을 넣었다. #5가 `NONE` → `summary=NULL` → `UNAVAILABLE` 매핑을 이 짝 위에 얹으므로 정적 팩토리 규율만으로는 부족하다. tester 예고 테스트를 지우지 않고 뒤집고 정상 조합 케이스를 하나 더했다. feedback 257건 통과.

- 21:40 — PR(이슈 #147) 리뷰: 차단 2건(매도 회고 프롬프트가 `holdHigh*`·`holdLow*` null에서 NPE — 같은 PR이 템플릿 쪽만 막았고 `sameSessionCompleted=false`에서 조회가 500이 된다 / 코인 자정 구간에서 장중 카드 템플릿 구간 길이가 음수) / 권장 3건(가정법 목록의 축약형 잔여 구멍 `샀다면`·`봤다면` 등, `application.yml` 주석의 "이 블록이 없어도 기동한다"가 `${feedback.llm.timeout-seconds}` 참조 때문에 거짓, spec §후검증 "정규식" 표현 미갱신) / 참고 4건. spec 4개 수정·범위 축소·배정표 91건 검산은 전부 정당함을 재확인.

- 21:40 — reviewer 차단 2건 수정. ①`postSellPrompt`가 보유 구간 극값 6필드를 null 검사 없이 역참조해 `sameSessionCompleted=false`면 NPE가 생성기 밖에서 터졌다(→ 조회 500, `narrativeStatus` 항상 READY 위반). 두 그룹을 줄 단위로 생략하고 파생 사실 블록을 따로 모아 빈 줄 중복도 없앴다. ②`ChronoUnit.MINUTES.between(LocalTime, LocalTime)`이 코인 자정 횡단에서 음수가 돼 "-1435분간"이 조용히 나갔다(spec §C-9 사례). `PriceMovePromptDto`에 `windowMinutes`를 필드로 추가해 절대 시각을 가진 호출부가 계산하게 했다. **프롬프트 문자열은 분(minutes)을 렌더링하지 않아 골든 마스터 23건과 spec §LLM 프롬프트 예시는 영향 없음.** 테스트 3건 추가(자정 횡단·극값 없는 프롬프트·극값 없는 서비스 경로), 260건 통과.

- 21:09 — 이슈 #160 착수. `NarrativeSource`·`NewsSummaryScope`를 `feedback/service/` → `feedback/domain/`으로 옮겨 `feedback/domain/` 패키지를 열었다. enum 본문은 그대로 두고 참조 6파일에 import만 더했으며, 클래스 주석의 "#2가 재사용한다"만 현재형으로 다듬었다. 컴파일·포맷 검사 통과.

- 21:20 — `V13__create_ai_feedback_tables.sql` 신설(일곱 테이블·유니크 7·인덱스 4·FK 6). 임시 프로브로 실제 MySQL 스키마를 덤프해 §C-8 타입표와 대조했고, `url` 유니크의 `sub_part`가 `NULL`(접두 없음)인 것과 `narrative_finalized`/`regeneration_attempts` 기본값을 확인했다. 원장 테이블 대상 `ALTER`·`DROP` 0줄, 원장 스키마 테스트(`OrderLedgerSchemaTest`)·주문 리포지토리·컨텍스트 기동 통과.

- 21:25 — `MarketNewsItem` 엔티티·`MarketNewsItemType`(NEWS·DISCLOSURE)·`MarketNewsItemRepository` 신설. 본문 컬럼 없음, `createdAt`은 수집 시각이라는 두 제약을 클래스 주석에 남겼다. 리포지토리는 `JpaRepository` 상속만 두고 조회 메서드를 만들지 않았다(완료 조건 2건은 저장·조회로 검증된다). 테스트는 tester 소유라 작성하지 않았고, `@SpringBootTest` 기동으로 `ddl-auto=validate`가 V13의 `market_news_items`와 매핑이 일치함만 확인했다.

- 21:35 — `PriceMoveEvent`·`PriceMoveEventSource`·`PriceMoveEventType`(INTRADAY·OPENING_GAP)과 리포지토리 2종 신설. 주식·코인이 서로 다른 컬럼을 채우는 것(§C-9)을 정적 팩토리 `createStock`/`createCrypto` 둘로 갈라 형태가 섞이지 않게 했고, `market`은 `market/domain/Market`, `narrative_source`는 1번이 옮긴 `NarrativeSource`를 재사용했다. `narrative`는 `columnDefinition = "TEXT"`로 매핑해야 `validate`가 통과한다(String 기본 매핑은 `varchar(255)`라 `text`와 어긋난다) — 컬럼명 오타 프로브로 새 엔티티가 실제 검증 대상임을 확인한 뒤 원복했다.

- 21:50 — `InstrumentNewsSummary`·`MarketBriefing`과 리포지토리 2종 신설. `scope`는 `NewsSummaryScope`, `market`은 `market/domain/Market`, `summary`는 4번과 같이 `columnDefinition = "TEXT"`다. `origin_trade_date`는 두 테이블 다 `NOT NULL`이고 코인 행도 채운다는 것, `summary`가 `NULL`이면 `narrative_source`가 `NONE`이라는 것을 클래스 주석에 남겼다. UPSERT·재생성 판정과 상태 변경 메서드는 배치 이슈 소유라 만들지 않았다. `validate` 기동 통과.

- 22:05 — `TradeFeedback`·`PriceMovePeerStat`과 리포지토리 2종을 붙여 **일곱 테이블 전부에 매핑이 생겼다.** 그 상태로 `@SpringBootTest`가 기동해 완료 조건 "V13이 `ddl-auto=validate`를 통과한다"를 최종 확인했다. §C-8 타입표를 일곱 엔티티의 `@Column`과 전수 대조했고 불일치 0건이다 — 다만 **Hibernate `validate`는 NULL 허용 여부와 기본값을 검사하지 않으므로** 그 두 축은 이 수동 대조와 tester의 `@DataJpaTest`가 근거다. `trade_id` 연관에는 cascade를 걸지 않아 원장을 쓰는 경로가 생기지 않는다.

- 22:35 — reviewer 차단 1건 수정: `createCrypto`가 `originTradeDate`를 파라미터로 받아 §C-9의 `originTradeDate = occurredAt의 KST 날짜`가 계약(javadoc)으로만 남아 있었다. 파라미터를 없애고 `occurredAt.toLocalDate()`로 파생하게 바꿨다 — 코인은 `window_start`가 `NULL`이라 유니크가, `validate`는 값 자체를 안 봐서 어긋난 행을 아무도 못 잡고, 이 컬럼이 #8의 일일 상한 카운트 기준이라 자정 직후 카드가 전날 몫으로 세어진다. 픽스처(`8/3` + `8/4 00:03`)가 하필 그 케이스여서 테스트 기대값도 `8/4`로 바로잡았다(자정 넘김 검증 의도는 유지). 권장 1건도 함께 반영 — 두 팩토리가 `instrument.getMarket()`이 카드 형태와 같은지 검증하고 다르면 `IllegalArgumentException`을 던진다. feedback 리포지토리 테스트 52건 통과.

- 22:20 — reviewer 권장 3건·참고 2건 처리. ①`application.yml`의 "블록 없어도 기동한다" 주석이 거짓이었다 — 블록을 실제로 지우고 `@SpringBootTest`를 돌려 `ConfigurationPropertiesBindException` → `DurationStyle` 변환 실패로 기동이 깨지는 것을 확인하고 주석을 사실대로 고쳤다. ②spec §후검증의 "정규식" 표기 2곳을 "부분 문자열"로 맞추고 왜 정규식이 아닌지를 적었다. ③가정법 한계(`샀다면`·`봤다면` 등 축약형은 구조적으로 못 잡는다)를 §후검증에 명시하고 §튜닝 관측 항목으로 넣었다 — 목록은 넓히지 않았다. 참고: `maxRegeneration` 음수 차단, 이슈 #147 완료 조건 문구 동기화.

- 22:40 — PR(이슈 #160) 리뷰: 차단 1건(코인 카드의 `originTradeDate`가 `occurredAt`과 독립 파라미터라 §C-9를 어긴 행을 팩토리가 그대로 만들고, 테스트 픽스처가 실제로 어긋난 조합(8/3 + 8/4 00:03)을 단정한다) / 권장 2건(`market` 컬럼과 `instrument.market` 불일치 무방비, NOT NULL 축 단정이 Hibernate에서 멈춰 V13 컬럼 속성을 검증하지 못함) / 참고 5건. V13 ↔ §C-8·§데이터 모델 대조는 타입·유니크·인덱스·FK 전부 일치, 기존 마이그레이션 미수정·원장 불변 확인.

- 22:50 — 수정 커밋(518f382) 재검토: 차단 0건 / 권장 1건 / 참고 4건 — 머지 가능. 차단 1건(코인 `originTradeDate` 파생)과 권장 1건(시장 일치 검증)은 실제로 해소됐고 호출부·기대값도 전수 반영됐다. 다만 **직전 리뷰의 권장 3건 전제가 사실이 아니었다** — `spring-boot-starter-validation`이 붙어 있어 Hibernate가 `check_nullability`를 끄므로 `@Column(nullable=false)`는 flush를 막지 않는다. 프로브로 확인한 실제 예외는 `SQLIntegrityConstraintViolationException: Column 'origin_trade_date' cannot be null`이었다(SQL이 나가고 MySQL이 막는다). 그래서 이 커밋이 두 테스트의 DisplayName·주석을 "엔티티 제약에서 막힌다"로 바꾼 것은 **맞는 문장을 틀린 문장으로 되돌린 것**이다. `FeedbackSchemaConstraintsTest` 자체는 전 컬럼·기본값을 덮어 순가치가 있으므로 유지하고 근거 문장만 고치면 된다.

- 23:30 — 이슈 #167 착수. 수집 크론 2종(`feedback.news.*`)과 자격증명 3종(`naver-search.*`·`dart.api-key`)을 record 3개 + `NewsCollectionPropertiesConfig`로 세우고 `application.yml`에 같은 값을 뒀다 — 시크릿에는 §C-7대로 `@DefaultValue`를 붙이지 않고 빈 플레이스홀더만 뒀다. `.env.example`에 환경변수 4종을 "없으면 무슨 일이 일어나는지"와 함께 추가했고, `compose.deploy.yaml`은 `env_file: .env`로 이미 통과하므로 그 사실과 "environment 블록에 다시 적으면 .env가 무시된다"를 주석으로 드러냈다. 컴파일·컨텍스트 기동 통과.

- 00:10 — `NewsSearchQueryBuilder`(코인에만 `" 코인"` 접미)·`NewsTitleFilter`(같은 시장 다른 종목명 제외) 신설. 필터는 단순 포함 검사로는 접두 관계 종목쌍의 양방향 중 한쪽이 반드시 틀려서(`비트코인` 기준으로 `비트코인캐시` 기사는 빼야 하고, `비트코인캐시` 기준으로 같은 제목은 남겨야 한다) **다른 종목명의 등장 구간이 자기 이름의 등장 구간에 통째로 덮이는지**로 판정한다. 자기 이름을 제목에서 지우고 검사하는 방식은 첫 경우를 놓친다. §C-6에 클래스명이 없던 자리라 두 이름과 "수집기 밖에 두는 이유"를 spec에 추가했다. 컴파일·SpotBugs 통과.

- 00:55 — `NewsCollector`·`CollectedNewsDto`·`FakeNewsCollector`(`@Profile("!prod")`)·`NaverNewsCollector`(`@Profile("prod")`) 신설. **하마터면 앱 전체를 못 띄울 뻔했다** — 처음엔 `naverSearchRestClient` 빈을 따로 등록했는데, 임시 프로브를 돌려 보니 `RestClient` 빈이 둘이 되는 순간 기존 `KisHistoricalCandleClientImpl`까지 `NoUniqueBeanDefinitionException`으로 깨졌다(필드 `@Qualifier`는 Lombok 생성자로 복사되지 않아 장식이었다, `javap` 확인). 빈을 늘리지 않고 `RestClient.Builder`를 받아 클래스 안에서 완성하는 레포 다수 패턴으로 바꾸고 그 빈 설정 클래스는 삭제했다 — 덤으로 `MockRestServiceServer`용 생성자 seam이 생겼다. `agent-mistakes.md`에 행을 추가했다. `publisher`는 사용자 결정대로 `originallink` 호스트(`www.` 제외)이고 §C-8에 정본으로 적었다. **`docs/api-contracts.md`의 `publisher` 예시 3곳(`한국경제`·`매일경제`·`한국경제`, 527·597·634줄)이 한글 언론사명이라 도메인 형태로 정정이 필요하다 — planner(동기화 모드) 몫으로 남긴다.**

- 01:40 — `DisclosureCollector`·`FakeDisclosureCollector`(`@Profile("!prod")`)·`DartDisclosureCollector`(`@Profile("prod")`)·`DartCorpCodeRegistry`·`dart-corp-codes.txt` 신설. 반환 타입은 뉴스와 같은 `CollectedNewsDto`를 재사용했고(저장 컬럼이 같다), OpenDART가 실패해도 HTTP 200을 주므로 본문 `status`를 봐서 `000`만 정상으로 다룬다 — `013`(데이터 없음)은 조용히, 나머지는 WARN이다. **매핑 리소스는 값 없이 채우는 방법만 담은 상태로 커밋한다** — `corp_code` 정본은 `crtfc_key`가 필요한 corpCode.xml뿐이라 확인할 수 없었고, 틀린 8자리는 0건이 아니라 **다른 회사의 공시**를 가져와 조용한 오정보가 되기 때문에 추측값을 넣지 않았다. 매핑 0건은 기동 로그에 WARN으로 남긴다. **완료 조건 ④("매핑 리소스가 §C-7의 주식 종목 전부를 덮는다")는 키 발급 전까지 충족 불가 — 미결로 남긴다.**

- 02:30 — `NewsCollectionService`(크론 2종, `zone = "Asia/Seoul"`)·`InstrumentService.getInstrumentEntities`·`MarketNewsItemRepository.existsByInstrumentIdAndUrl` 신설. 스케줄 메서드에 `@Transactional`을 붙이지 않았다 — 28종목 순차 HTTP 동안 DB 커넥션을 잡지 않기 위해서이고(`KisHistoricalCandleCollector` 선례), 저장이 건별로 커밋돼 한 건이 실패해도 앞선 저장이 남는다. 종목 목록은 §C-6대로 `market` 서비스를 경유하며 리포지토리는 `MarketNewsItemRepository` 하나만 주입해 원장에 닿는 경로가 구조적으로 없다. **`pool.size`를 5 → 7로 올리고 `application.yml` 주석의 개수 계산(기존 5 + 신설 2, 최종 12는 §C-1)도 같은 커밋에서 갱신했다.** 파생 쿼리는 컴파일·기동만으로는 실행을 보장하지 못해(agent-mistakes 2026-08-03) 임시 `@DataJpaTest`로 실제 MySQL 실행까지 확인했다 — 같은 URL이라도 종목이 다르면 중복이 아닌 것까지 봤다.

- 03:10 — `MissingExternalKeysIntegrationTest` 신설(3건). 네 키를 프로퍼티로 비운 실제 컨텍스트에서 ①기동 ②두 수집기가 Fake로 주입 ③수집 종단이 예외 없이 0건 저장을 단정한다. OpenAI만 빈 문자열이 아니라 `not-configured`인 것은 SDK가 빈 문자열을 거부하기 때문이고(agent-mistakes 2026-08-03), 종목이 0건이면 ③이 빈 루프로 통과하므로 주식·코인 목록이 비지 않았음을 먼저 막았다. 앞선 프로필 테스트(슬라이스·수집기 개별)와 `NewsCollectionIntegrationTest`(`@MockitoBean`)와 겹치지 않게 "넷이 동시에 없는 실제 컨텍스트 + 진짜 Fake 빈"으로만 범위를 좁혔다.

- 03:10 — **코인 질의어 보정 실측: 수행하지 못했다.** `NAVER_SEARCH_CLIENT_ID`·`NAVER_SEARCH_CLIENT_SECRET`이 아직 발급 전이라 실제 기사를 한 건도 받아 볼 수 없고, 실제 네이버 API를 부르는 자동 테스트는 PRD C-005가 금지한다. **지금 확인된 것은 "질의어에 `" 코인"`이 붙는다"까지이며(`NewsSearchQueryBuilderTest`), 그것은 "무관 기사가 실제로 줄어드는가"를 말해 주지 않는다** — 후자가 §튜닝이 실측을 요구한 대상이다. **재확인 조건**: 키 발급 후 prod 프로필로 뉴스 수집이 최소 1회 돈 뒤, `market_news_items`에서 코인 종목(특히 일반명사·인명과 충돌하는 `리플`·`에이다`·`트론`·`폴카닷`·`체인링크`)의 `title` 목록을 눈으로 훑어 무관 기사 비율을 확인한다(§튜닝 "코인 질의어 보정" 행). 비율이 높으면 코드가 아니라 spec FEED-001·§C-7을 먼저 고쳐 **종목별 질의어 오버라이드**를 도입한다. **같은 실측에서 반대 방향인 과다 제외(누락)도 함께 본다** — 제목 필터가 부분 문자열로 판정하므로 `리플레이션`·`트론봇`처럼 다른 종목명이 <b>다른 단어의 일부로</b> 들어간 제목은 정상 기사인데도 조용히 빠진다(오탐은 눈에 띄지만 누락은 화면에 흔적이 없다). 수집된 제목만이 아니라 **네이버 검색 결과와 저장된 건수의 차이**를 함께 확인해야 이 방향이 드러난다. 이 줄이 남아 있는 동안 이 항목은 열린 상태다.

- 03:40 — PR(이슈 #167) 리뷰: **차단 0건 / 권장 3건 / 참고 6건 — 머지 가능.** spec §확정값과의 대조에서 크론 2종·zone·종일 수집·발행일자 무필터·(종목,url) 중복 무시·코인 질의어 보정·제목 필터·본문/스니펫 미저장·`publisher` 도메인·`pubDate` KST 변환·DART `corp_code`/구간/`00:00:00`·키 없는 기동이 전부 일치했고, 스키마 무변경이라 마이그레이션 의무도 없다. 권장 3건은 ① `pool.size`가 7이라 spec §C-1의 확정값 12와 문면이 어긋난 채 남음(통합 테스트가 하한을 지키므로 지금 위험은 없고 spec 또는 yml 중 하나를 맞추면 된다) ② `.env.example` 48·52줄이 "키 없으면 Fake"라고 적었으나 prod에는 Fake가 없어 실제로는 401 반복 ③ `NaverNewsCollector.toPublisher`가 호스트 파싱에 실패하면 로그 한 줄 없이 기사를 통째로 버리고 `link` 폴백도 타지 않음(공백·`|` 등이 든 URL에서 재현, 비ASCII 경로는 `URI.create`가 허용함을 프로브로 확인). `gh issue view 167`로 완료 조건 체크박스가 아직 0개임을 확인했고(머지 시 갱신 필요), corp_code 리소스 미충족은 `run-log.md`에 미결로 남아 있어 허위 완료 기록은 없다.

- 03:50 — reviewer 권장 3건 수정. ①`NaverNewsCollector`가 URL 선택과 언론사 추출을 나눠 하고 있어, `originallink`의 호스트를 못 뽑으면(공백·`|`·`[]`로 `IllegalArgumentException`, 언더스코어로 `getHost()==null`) **멀쩡한 `link`가 있는데도 폴백이 타지 않고** 기사가 로그 없이 사라졌다. 둘을 한 루프로 합쳐 후보마다 언론사까지 뽑아 보고 성공한 짝을 쓰게 했으며(`SourceLink`), 두 후보 모두 실패할 때만 WARN 한 줄과 함께 버린다. 임시 프로브 4건으로 두 실패 모양과 "둘 다 못 쓸 때만 버린다"를 재현 확인했다 — **기존 테스트는 `originallink`가 빈 경우만 덮고 있어 이 경로는 무검증이었다.** ②`.env.example`의 "없으면 Fake가 빈 목록을 반환한다"가 prod에서 거짓이었다(수집기 선택은 키가 아니라 프로필이라 배포에는 Fake가 없다). 동작은 그대로 두고 로컬·테스트(Fake, 외부 호출 없음)와 prod(실제 수집기가 401로 실패 → 그 종목만 건너뜀 + WARN, 결과는 0건으로 동일)를 문구에서 갈랐다. ③`pool.size: 7`이 §C-1의 "5 → 12"와 문면상 어긋난다는 지적에는 **코드가 아니라 spec을 고쳤다** — 12는 스케줄 7개가 전부 들어온 뒤의 값인데 그 사실이 적혀 있지 않았다. §C-1에 "이슈별로 자기 몫만큼 올리고, 올릴 때마다 그 시점의 개수를 다시 센다"를 명시했다. 문서 2건도 반영 — `tasks.md` 4번에 "④ 미결" 줄, §튜닝 재확인 조건에 과다 제외(누락) 방향.

- 10:10 — **외부 키 4종 발급 완료. §튜닝의 미결 2건을 실호출로 닫았다.**
  - **네이버 검색 API가 NAVER API HUB(네이버 클라우드 플랫폼 중개)로 이전돼 있었다.** `developers.naver.com`의 일반 애플리케이션 등록 화면에는 `검색`이 없고, 발급되는 값은 콘솔이 "Client ID/Secret"이라 부르지만 실제 헤더는 `X-NCP-APIGW-API-KEY-ID`·`X-NCP-APIGW-API-KEY`이며 호스트도 `naverapihub.apigw.ntruss.com/search/v1/news`다. 구현이 쓰던 `openapi.naver.com` + `X-Naver-Client-*` 조합으로는 이 키가 인증되지 않는다 — **키를 발급받아 실제로 불러 보기 전까지는 드러나지 않는 종류의 어긋남이다.** 엔드포인트·헤더를 고치고 spec §외부 API 호출 상세를 정정했다. 응답 본문 필드는 구 API와 같아 매핑·파싱 코드와 그 테스트는 그대로 살았다. 실호출 확인 — `삼성전자` 질의에 HTTP 200, `items` 100건, 필드 `title`·`originallink`·`link`·`description`·`pubDate`.
  - **`dart-corp-codes.txt`를 채웠다.** `corpCode.xml`(상장사 3,981건)에서 V7 시드 주식 16종목을 `stock_code`로 조회해 **16/16 확보, 누락 0.** 실호출로 검증 — `삼성전자`(4건)·`카카오`(1건) 공시가 `status=000`으로 돌아왔고 **응답의 `corp_name`이 요청한 회사와 일치**했다. corp_code가 틀렸다면 200과 함께 다른 회사 공시가 왔을 것이므로 이 대조가 매핑 정확성의 실질 근거다. 시드명과 DART 등록명이 다른 경우가 있어(`005380`: 시드 `현대차` / DART `현대자동차`) 매칭 축은 이름이 아니라 `stock_code`임을 파일 머리말에 남겼다.
  - **코인 질의어 실측 (§튜닝 "코인 질의어 보정" 행) — 양방향 모두 문제가 확인됐다.** 위험 종목 7개에 `이름 + " 코인"` 질의를 실제로 날려 각 100건을 받고 현행 제목 필터를 적용했다.

| 질의 | 수신 | 통과 | 필터 제외 | 제외율 |
|---|---|---|---|---|
| `트론 코인` | 100 | 27 | 73 | **73%** |
| `에이다 코인` | 100 | 42 | 58 | **58%** |
| `비트코인캐시 코인` | 100 | 54 | 46 | 46% |
| `리플 코인` | 100 | 57 | 43 | 43% |
| `폴카닷 코인` | 100 | 60 | 40 | 40% |
| `체인링크 코인` | 100 | 73 | 27 | 27% |
| `비트코인 코인` | 100 | 89 | 11 | 11% |

    - **누락 방향 (과다 제외)** — 제외된 기사 대부분이 무관 기사가 아니라 **시세 브리핑**이다(`[자정 시세브리핑] … 비트코인 63,340달러, 이더리움 …`). 암호화폐 기사는 여러 코인을 한 문장에 나열하는 것이 기본형이라, "제목에 다른 종목명이 있으면 제외"가 **정상 기사를 대량으로 버린다.** 근거 0건은 카드를 막으므로(FEED-003) 이 방향은 **코인 카드가 아예 안 생기는 결과**로 이어진다.
    - **오탐 방향** — 통과한 기사 중에도 무관한 것이 섞인다. 네이버가 제목이 아니라 본문·요약까지 매칭하기 때문이다. `비트코인캐시 코인` → `260레인 CXL 스위치, 마벨 AI 메모리 병목 겨냥`, `체인링크 코인`·`트론 코인` → 같은 `사토시홀딩스 스테이블코인 결제 플랫폼` 기사 4건. 현행 필터는 **다른 종목명이 있을 때만** 제외하고 자기 이름을 요구하지 않으므로(FEED-001 그대로) 이런 기사는 그대로 통과한다.
    - **판단 — 이 이슈에서 고치지 않는다.** §튜닝의 조정 기준이 "높으면 종목별 질의어 오버라이드를 도입한다"인데, 실측이 보여주는 것은 질의어만의 문제가 아니라 **필터 방향까지 걸린 규칙 문제**다(FEED-001). 규칙을 바꾸려면 spec을 먼저 고쳐야 하고, 이슈 #167의 완료 조건("보정이 붙고, 같은 시장 다른 종목명이 든 제목이 제외된다")은 규칙대로 구현·검증돼 있다. **별도 이슈로 낸다** — 후보는 ①제목에 자기 종목명이 있을 것을 통과 조건에 추가(오탐 차단) ②시세 브리핑류를 살리기 위해 제외 규칙을 "다른 종목명만 있고 자기 이름이 없을 때"로 완화 ③종목별 질의어 오버라이드. 셋 다 카드 생성량에 직접 영향을 주므로 한 번에 정하지 말고 이 표를 기준선으로 삼아 비교한다.

- 11:20 — 이슈 #180 착수. `feedback.detection` 5키(yml + `@DefaultValue`)와 `PriceMoveDetector`(순수 계산) 신설 — 인덱싱을 전부 `TreeMap<LocalTime, 분봉>` 시각 조회로 두어 결측 구간에서는 그 표본·후보를 제외하고, 시가·첫 분봉을 리터럴이 아니라 "가장 이른 분봉"으로 찾는다. 스키마·컨트롤러 무변경. 컴파일·SpotBugs 통과.

- 11:50 — `market`에 조회 4종 신설(`getCurrentReplaySession`·`getSourceTradingDate`·`getFullDayCandles`·`getPreviousTradingDayClose`). 게이트를 우회하는 뒤 두 개에는 "배치에서 부르는 것이 기본, 조회 경로는 §C-5 통과 후"를 주석으로 남겼다. 직전 종가는 `BusinessDayCalendar.previousBusinessDay`로 D-1을 구해 그 날의 마지막 분봉을 읽는다 — "그 종목의 가장 최근 분봉"으로 하면 수집이 빈 종목에서 일주일 전 종가로 없는 갭을 만든다. `StockReplaySessionScheduler`는 건드리지 않았고 `feedback` 쪽 변경도 없다.

- 12:05 — `NewsMatcher` 신설(장중=`[windowEnd±근거창]`의 뉴스만, 시가 갭=전장 뉴스 + `rcept_dt=D-1` 공시). **공시를 뉴스와 같은 datetime 구간에 태우지 않는 것이 이 항목의 핵심**이다 — 그러면 D-1 접수분은 전장 시작보다 일러 빠지고 D 접수분은 장중 접수분까지 끌고 들어온다(§C-3). 그래서 파인더를 둘로 나눴다(뉴스=시각 구간 파생 쿼리, 공시=반열림 날짜 `@Query`). `feedback.news`에 `match-before-minutes`·`match-after-minutes`·`max-sources-per-card` 3키를 §C-7 방침대로 yml+`@DefaultValue` 양쪽에 더했다(이 이슈가 안 쓰는 상한·코인 키는 더하지 않았다).

- 12:20 — `PriceMoveCardService` 신설(중복 확인 → 근거 매칭 → 서술 → `reveal_time` → 저장). 중복 확인을 **서술보다 먼저** 둔 것이 비용 판단이다 — 뒤로 미루면 재실행마다 카드 수만큼 LLM을 다시 부르고 그 결과를 유니크가 버린다. `reveal_time`은 §노출 판정 그대로 장중만 `+1분`이고 갭은 클램프 결과뿐이며, 둘 다 `TIME`으로 저장한다. 트랜잭션 경계는 확정 메서드 전체가 아니라 **저장 둘에만** 둔다(`PriceMoveCardWriter`, `KisHistoricalCandleImportWriter`와 같은 형태) — 경계를 없애면 카드와 근거가 따로 커밋돼 근거 0건 카드가 남고 다음 실행이 중복 판정으로 영영 안 고치며, 반대로 메서드 전체를 감싸면 LLM 대기 20초 동안 커넥션을 쥔 채 종목 수만큼 반복한다. 쓰기는 전부 그 컴포넌트를 거치고 거기 주입된 리포지토리도 `price_move_events`·`price_move_event_sources` 둘뿐이라 원장에 닿는 경로가 없다. V13·엔티티 무변경.

- 12:45 — `FeedbackBatchService` 신설(08:45 별도 크론 + `zone`, `READY` 확인, §C-6 5단계 순서). 요약·브리핑(1·2·5단계)은 **패키지 전용 메서드 자리**로 남겼다 — 주석으로 남기면 순서를 단정할 대상이 없어 뒤에 조용히 바뀐다(배치 ③이 이 이슈 완료 조건이다). **카드는 종목 루프 안에서 두 종류를 함께 만들지 않고 종류로 한 바퀴씩 돈다** — 그래야 실행이 길어져도 뒤쪽 종목의 갭 카드가 09:00까지 만들어진다(§C-6). 탐지는 종목당 1회만 하고 두 단계가 나눠 쓴다. `pool.size` 7 → 8과 `application.yml` 주석의 개수 계산을 같은 커밋에서 갱신했다. **`NewsCollectionScheduleTest`가 소스 텍스트로 `NewsCollectionService` 언급을 금지하고 있어** javadoc에서 클래스명을 빼고 뜻만 남겼다(호출이 아니라 "부르지 않는다"는 설명이었다).

- 13:10 — spec 012의 첫 컨트롤러(`PriceMoveController`)와 `PriceMoveQueryService`·응답 DTO 3종 신설. 게이트는 `reveal_time <= LocalTime.now(clock)` 한 줄인데 그것이 곧 §C-5의 `(서비스 날짜 + reveal_time) <= now()`다 — `TIME`으로 저장한 덕분에 재재생 날에도 그대로 성립한다. 재생세션 미준비·카드 0건·코인 종목은 전부 200이고, 코인만 `originTradeDate=null`·빈 배열로 두고 분기 자리를 주석으로 남겼다(plan.md 8번). 근거는 `JOIN FETCH` 일괄 조회로 N+1을 접었고 응답에는 제목·언론사·URL·발행시각만 싣는다. `revealTime`은 응답에 없다. 인증은 `anyRequest().authenticated()`가 이미 걸어 SecurityConfig 변경이 없다. **문서 동기화(api-routes·api-contracts)는 planner 몫으로 남겼다 — 계약과 어긋나는 자리는 없었다.** 리뷰 [권장] 반영으로 카드 정렬에 2차 키 `id`를 붙였다(첫 분봉이 09:00인 날 갭 카드와 장중 첫 후보의 `window_start`가 같아 순서가 실행마다 달라진다). `id`는 생성 순서라 §C-6의 3→4단계를 그대로 따르고, `event_type`으로 가르면 순서가 enum 선언 순서에 묶인다 — `api-contracts.md`의 "상대 순서를 보장하지 않는다" 문장도 함께 고쳤다.

- 13:40 — 리뷰 완료, **차단 0건 / 권장 3건 / 참고 3건 — 머지 가능.** 완료 조건 21건 전부 대응 테스트 확인, 트랜잭션 경계·풀 크기 8·§C-6 생성 순서·게이트 TIME 저장 모두 의도대로. 권장은 `application-crypto-real.yml` 주석의 옛 개수(5), 카드 목록 정렬 2차 키 부재(같은 `window_start`), §C-6 "중첩 레코드" 표기 정정.

- 14:20 — 이슈 #188 착수. `feedback.news`에 목록 상한 3종(`max-items-per-news-list`·`max-items-per-briefing`·`max-items-per-summary`)을 yml + `@DefaultValue` 양쪽에 더하고 1 미만이면 기동을 막았다 — 0이면 `items`가 통째로 비고 요약 프롬프트에 기사가 안 실리는데 상태값은 그대로 `READY`라 예외도 로그도 없이 화면만 빈다. 개장·장 마감 벽시계 상수를 `MarketSessionTimes` 하나로 모아 `PriceMoveCardService.MARKET_OPEN_TIME`과 `NewsMatcher.PRE_MARKET_FROM/TO_TIME`의 독립 선언 3개를 없앴다(§C-6, 곧 Part C·D가 세 번째·네 번째 사용처다). **동작을 바꾸지 않는 이동이라 새 단정을 만들지 않았고**, 기존 테스트 3곳의 생성자 호출만 인자 3개 추가로 기계적으로 맞췄다. 스키마·컨트롤러 무변경이라 마이그레이션과 문서 동기화 대상이 없다.

- 15:50 — 이슈 #188 tasks 2번. `feedback.batch.cron`·`news.collect-cron`·`disclosure-cron`을 `"-"`(`Scheduled.CRON_DISABLED`)로 덮는 테스트 전용 yml을 `build.gradle`의 `spring.config.additional-location`에 얹어 `@SpringBootTest` 중 배치·수집 스케줄이 실제로 등록되지 않게 했다 — 평일 배치 시각에 전체 빌드가 걸치면 공유 컨테이너에 의도하지 않은 데이터가 생기고 재현이 시계에 묶여 원인 찾기가 가장 어렵다. **tasks.md가 안전하다고 적은 전제가 틀렸다** — `ConfigDataApplicationContextInitializer`는 그 시스템 프로퍼티를 `@SpringBootTest`와 똑같이 해석해서, 이번에 옮긴 단정뿐 아니라 **건드리지도 않은 `FeedbackBatchPropertiesTest`까지** `"-"`를 보고 깨졌다(실측). 기존 주석의 "`ApplicationContextRunner`에는 영향이 없다"는 초기화자 **없는** 순수 슬라이스에만 해당하는 말이었다. 드리프트 단정을 없애지 않고 두 runner에 `withSystemProperties("spring.config.additional-location=")`를 붙여 원본을 보는 자리를 유지했다(`run()` 종료 시 자동 복원). `NewsCollectionPropertiesIntegrationTest`의 크론 단정 2건은 `NewsCollectionPropertiesYamlTest`로 옮겼고 나머지 3건(근거 매칭·자격증명)은 그대로다. `agent-mistakes.md`에 재현 확인한 행을 남겼다.

- 16:40 — 이슈 #188 tasks 4번. `FeedbackBatchService`의 빈 자리 두 곳을 채우고 확정 로직은 `MarketBriefingService`·`InstrumentNewsSummaryService` 둘로 뺐다(`PriceMoveCardService`와 같은 이유 — 배치에 두면 오케스트레이션과 도메인 로직이 섞이고 코인 배치가 재사용할 수 없다). 호출 위치·시그니처·순서는 그대로이고 `InOrder` 단정도 그대로 통과한다. **격리를 종목 단위로 내렸다** — 호출부 `try/catch`는 단계 방어선이라 한 종목이 터지면 그 범위의 나머지 종목이 로그 한 줄만 남기고 통째로 날아간다. **§뉴스 매칭 범위의 공시 우선 절단을 `NewsItemTruncator` 하나로 모았다**(요약·브리핑 두 자리가 이미 쓰고 Part C·D `items`가 뒤에 같은 규칙을 쓴다). spec이 경고한 대로 **픽스처를 상한 위로 잡아야만 규칙의 유무가 드러나므로** 임시 프로브로 "뉴스 10 + 공시 2, 상한 5 → 공시 2건이 전부 생존하고 정렬은 여전히 최하위", "공시 4 + 뉴스 1, 상한 2 → 공시끼리도 최신순으로 잘린다"를 재현 확인했다. 저장은 UPSERT가 아니라 **존재 시 건너뜀**이고(주식, 배치 ⑤) 서술이 `NONE`이면 `summary=NULL` 행을 남긴다(`EMPTY`와 `UNAVAILABLE`을 구분하는 근거). **기사 0건이면 행을 만들지 않는다** — §C-4 판정 순서 3번이 그 상태를 행 유무와 무관하게 `EMPTY`로 읽으므로 행을 만들어도 화면이 같고 근거 없는 문장을 LLM이 지어낼 자리만 생긴다. V13 무변경.

- 18:10 — 이슈 #188 tasks 5번. spec 012의 두 번째 컨트롤러(`InstrumentNewsController`)와 `InstrumentNewsQueryService`·`InstrumentNewsResponse`·`FeedbackContentStatus` 신설. **판정을 §C-4 표의 6단계 순서 그대로 코드에 폈다** — 특히 3번(기사 0건)이 요약 행 조회보다 앞이라, 순서를 바꾸면 "기사도 없고 서술도 없는" 날이 `UNAVAILABLE`로 보인다. 1번과 2번이 둘 다 `NOT_YET`이면서 `originTradeDate`만 갈리는 것도 그대로 지켰다. **`items` 상한을 구간 질의 자체로 구현했다** — §C-5의 `(서비스 날짜 + clamp(published_at)) <= now()`가 1배속 재생에서는 "뉴스 상한 = 현재 시각"과 같은 판정이라 별도 필터가 필요 없다(`PriceMoveQueryService`가 `reveal_time`에 쓰는 성질과 같다). 하한은 요약과 같은 `D-1 15:30`이고 상한만 재생 시각까지 넓으며, `FULL`이면 15:30에서 멈춘다(열어 두면 다음 거래일 새벽 기사가 섞인다). 절단은 4번이 만든 `NewsItemTruncator`를 그대로 쓰고 상한만 `max-items-per-news-list`다 — **요약 프롬프트의 `max-items-per-summary`와 다른 값이라는 점을 응답 DTO 주석과 계약서 양쪽에 적었다.** 코인은 `EMPTY`·빈 배열로 두고 분기 자리를 주석으로 남겼다(7번 소유). `api-routes.md`의 2차 계획 절에서 행을 실제 라우트 표로 옮기고 머리말 개수를 3 → 2로, `api-contracts.md`에서 `(계획)`을 걷고 **정렬 타이브레이커·두 상한의 차이·코인 미구현 범위**를 문장으로 추가했다. 스키마·마이그레이션 무변경.

- 19:30 — 이슈 #188 tasks 6번. spec 012의 세 번째 컨트롤러(`MarketBriefingController`)와 `MarketBriefingResponse`·`BriefingNewsItem` 신설, 조회 메서드는 §C-6대로 **기존 `MarketBriefingService`에 얹었다**(생성·조회가 같은 구간 질의를 공유해서 나누면 그 질의가 두 벌이 된다). **한 클래스 안에 상한 두 개가 공존한다** — 생성은 `max-items-per-summary`(LLM 입력), 조회는 `max-items-per-briefing`(응답 목록). 바꿔 쓰면 예외도 로그도 없이 목록 길이만 달라지므로 클래스 javadoc에 표로 박고 호출부마다 주석을 달았다. **§C-4 판정 순서에서 Part C와 갈리는 자리는 1번뿐**이다 — 재생세션 미준비가 Part D에서는 `EMPTY`·`originTradeDate=null`이고 `NOT_YET`이 아니다(2번 개장 전만 `NOT_YET`이고 날짜를 채운다). 09:00 하한은 두 API가 `MarketSessionTimes`의 같은 상수를 보므로 게이트 ⑦이 상수 수준에서 닫힌다. `items`는 저장하지 않고 조회 시 `collectPreMarketItems`를 다시 불러 만들며, 브리핑은 언제 조회해도 `전장`뿐이라 Part C처럼 상한이 재생 시각을 따라 넓어지지 않는다. `market` 파라미터 400은 별도 검증 없이 `@RequestParam Market`의 변환 실패를 `GlobalExceptionHandler`가 `VALIDATION_ERROR`로 매핑하는 기존 경로를 탄다(선례 5곳 확인). 코인은 `EMPTY`·빈 배열로 두고 분기 자리를 주석으로 남겼다(7번 소유). `api-routes.md` 계획 절 개수를 2 → 1로 줄이고(남은 것은 `post-sell`) `api-contracts.md`에서 `(계획)`을 걷으며 **정렬·두 상한·공시 우선 절단·코인 미구현 범위**를 문장으로 추가했다. 스키마 무변경.

- 21:00 — 이슈 #188 tasks 7번(마지막 구현 항목). `CryptoFeedbackBatchService` 신설(매시 05분, `zone = "Asia/Seoul"`)과 두 조회의 코인 분기. **확정 로직은 배치가 아니라 `InstrumentNewsSummaryService.refreshCryptoSummary`·`MarketBriefingService.refreshCryptoBriefing`에 뒀다** — 주식과 같은 구조이고 배치는 부르기만 한다. **"직전 생성"을 오늘 행이 아니라 `generated_at` 최신 행으로 잡은 것이 이 항목의 판단이다.** 오늘 행으로 잡으면 새 기사가 없어도 매일 자정 직후에 한 번씩 LLM을 부르게 되는데, 그때 오늘 행이 안 생기는 것은 문제가 아니다 — 조회가 "오늘 날짜 행"이 아니라 `generated_at` 최신 1행을 보기 때문이고 **그 두 규칙은 짝이다**(FEED-008이 "자정 직후와 배치 실패 시각마다 빈다"로 든 이유가 정확히 이것이다). 재생성 판정은 `created_at`이며 파인더 주석에 "`published_at`으로 비교하면 늦게 저장된 기사가 영원히 못 들어간다"를 남겼다. UPSERT는 별도 writer 없이 **조회 후 detached 엔티티를 변경하고 `save()`로 merge**하는 형태다(`refreshNarrative` 추가, 유니크 축은 안 건드린다) — LLM 호출을 트랜잭션 밖에 두는 기존 방침을 유지하면서 쓰기 한 번으로 끝난다. 코인 조회는 §C-4의 1·2번(미준비·개장 전)을 타지 않고 3~6번만 쓰며 `originTradeDate`는 `null`, `summaryScope`는 언제나 `ROLLING_24H`다. **주식 경로의 게이트·판정 순서는 한 줄도 건드리지 않았다** — 코인 분기를 메서드로 빼고 진입점에서 갈랐다. 풀 크기 8 → 9와 개수 계산을 `application.yml`·`application-crypto-real.yml` **두 곳 모두** 갱신했고(#125 사고 방지), 테스트 전용 yml에 `crypto-cron: "-"`를 더했다 — 매시 05분이라 전체 빌드가 한 시간에 한 번은 반드시 그 시각을 지나 끄지 않으면 공유 컨테이너에 행이 쌓인다. 5번이 임시로 뒀던 코인 `EMPTY` 단정 1건을 `ROLLING_24H`로 교체했다. 스키마·마이그레이션 무변경(엔티티에 갱신 메서드만 추가).

- 22:10 — 이슈 #188 리뷰 완료, **차단 0건 / 권장 2건 / 참고 4건 — 머지 가능.** 완료 조건 17건 전부 대응 테스트 확인. 배치 호출 순서·시그니처 무변경, 격리가 종목 단위로 협력자 mock에서 단정됨, 두 상한이 섞이지 않고 단위 테스트가 서로 다른 값으로 그것을 고정함, `refreshNarrative`는 코인 경로에서만 호출, `pool.size` 9가 두 yml에 함께 반영됨, GET 두 경로 모두 읽기 전용, 게이트 픽스처가 상한 위(53건·33건). 권장은 코인 24시간 창의 세 번째 중복과 통합 테스트 상한 상수를 빈에서 읽는 안이다.

- 21:40 — PR #194 reviewer [권장 1] 반영. 코인 24시간 창이 세 곳(`InstrumentNewsQueryService`·`InstrumentNewsSummaryService`·`MarketBriefingService`)에 `now.minusHours(24)`로 독립 선언돼 있던 것을 `MarketSessionTimes.ROLLING_WINDOW` 하나로 모았다 — 이 PR이 개장·장 마감 시각을 그 클래스로 모은 것과 같은 부류의 중복이고, 한 곳만 바뀌면 **요약이 다루는 창과 화면 목록의 창이 갈리는데 예외도 로그도 없이** "목록에 있는 기사가 요약에 없는" 상태로만 보인다. **주식 두 값과 성격이 다르다는 것을 상수 주석과 클래스 javadoc에 남겼다** — 09:00·15:30은 원본 거래일에 붙여 절대 시각을 만드는 벽시계이지만 이 값은 조회·배치 시각에서 거꾸로 빼는 **길이**이고, 코인은 거래일 경계가 없어 기준점이 고정 시각이 아니라 "지금"이기 때문이다. 한 타입에 함께 두는 근거는 셋 다 §C-2가 정한 시장 규칙이라는 것뿐이며 설정값으로 빼지 않았다(§튜닝 조정 대상이 아니다). `MarketSessionTimes`는 세 호출부가 전부 `feedback.service` 안이라 package-private을 유지했다. **순수 중복 제거로 동작은 바뀌지 않았다.**

- 14:20 — 이슈 #208 tasks 1번. spec 012의 **네 번째(마지막) 컨트롤러**(`PostSellFeedbackController`)와 `PostSellFeedbackService`·`PostSellFeedbackResponse` 신설, `portfolio`에 `SellAllocationQueryService`·`SellAllocationSummaryDto`와 배분 파인더 1개 추가. 검증 순서는 `getOwnedTrade`의 404 → 403을 그대로 타고 뒤에 `side != SELL` 400 → `market = CRYPTO` 400이다(`JournalService`와 같은 형태). **`buyAt`·`sellAt`을 원본 거래일 축으로 옮긴 것이 이 항목의 판단이다** — 계약이 "원본 거래일 기준 체결 시각"으로 정했고, 같은 응답의 `priceMoves[].windowStart`·`closeAt`·`holdHighAt`이 전부 그 축이라 `executedAt`(서비스 벽시계)을 그대로 쓰면 한 객체 안에서 날짜가 갈린다. `holdingMinutes`도 그 두 값 사이로 재서 응답이 자기모순이 되지 않게 했다. **`sameSessionCompleted`는 배분 lot 전부의 원본 거래일을 매도 체결과 대조**하고(하나라도 다르면 `false`) 판정은 `feedback`이 갖는다 — `portfolio`는 lot별 날짜만 돌려준다(§C-6 — 다른 도메인 repository 직접 주입 금지). 파인더 정렬은 `lot.executedAt` 오름차순 + `lot.id` 오름차순이고 **2차 키가 곧 `buyAt` 값이라 이름에 드러냈다.** **계약의 필드 집합 28개를 처음부터 전부 선언하고 아직 안 채우는 값은 `null`·`[]`로 뒀다** — 2·3·4번이 각자 채운다. 그 과정에서 spec §C-6에 이름 3개(`PostSellFeedbackStatus`·`HeldPriceMoveItem`·`CounterfactualScenario`)를 추가했다(`PriceMoveItem`·`FeedbackContentStatus`로는 계약의 JSON 필드 집합이 성립하지 않는다). `api-routes.md`의 2차 계획 라우트 절이 비어 **절 제목과 머리말을 "구현 완료"로 정리**하고 `api-contracts.md`에서 `(계획)`을 걷었다. 스키마·마이그레이션 무변경(엔티티 무변경).

- 16:10 — 이슈 #208 tasks 2번. 파생 사실 7값과 `priceMoves`를 채웠다 — 극값은 **분봉 `close`만** 쓰고 `high`/`low`를 보지 않는다(`high`로 잡으면 이 서비스가 낼 수 없는 가격이 되고 3번이 그것을 반사실 `atHoldHigh`에 그대로 올린다). 보유 구간 카드 파인더를 `PriceMoveEventRepository`에 하나 더했고 **`windowEnd`로 구간을 좁혔다** — `minutesAfterBuy`·`minutesBeforeSell`과 `atFirstMoveAfterBuy`가 전부 `windowEnd` 기준이라 `windowStart`로 좁히면 매도 뒤에 끝난 카드가 섞여 `minutesBeforeSell`이 음수가 된다. 정렬 두 키(`windowStart` 오름차순 + `id` 오름차순)를 이름에 드러냈고 **이 응답에서는 그 순서가 화면 순서만이 아니라 반사실 기준 카드까지 정한다**는 것을 주석에 남겼다. **게이트 상한을 `LocalTime.now(clock)`이 아니라 그 체결의 서비스 날짜로 계산한 것이 이 항목의 판단이다** — `PriceMoveQueryService`가 현재 시각을 그대로 넘기는 것은 오늘 재생 중인 세션만 보기 때문이고, 매도 회고는 과거 서비스 날짜의 체결을 조회하므로 그대로 쓰면 **어제 판 체결을 오늘 오전에 열었을 때 그날 오후 카드가 다시 감춰진다**(게이트 ⑭과 같은 되돌림, 같은 날 조회만 재현하면 두 구현이 같은 답을 낸다). `buyToNewsMinutes`는 `(T0 − 매수시각)`이라 매수가 앞이면 양수이고, 모수를 **게이트를 통과한 카드의 근거**로 한정했다(감춰진 카드의 기사 시각이 분 수로 새는 것을 막는다). `getFullDayCandles`의 게이트 우회는 **`[buyAt, sellAt]`로 잘라내는 것**으로 지켰고 매도 이후 구간은 3번 소유라 건드리지 않았다. `sameSessionCompleted=false`면 7값 전부 `null`·`priceMoves=[]`다. 스키마·마이그레이션·컨트롤러 무변경(문서 동기화 대상 없음). 생성자 인자가 2 → 6이 되어 `PostSellFeedbackServiceTest`의 조립 한 줄을 기계적으로 맞췄다(단정 무변경).

- 16:40 — 이슈 #208 `holdingMinutes` 음수 구멍 수정(tester 발견, 사용자 결정 반영). `buyAt`·`sellAt`이 서로 다른 원본 거래일 축이고 같은 원본 거래일을 여러 서비스 날짜에 재생할 수 있어 **매도의 원본 거래일이 매수 lot의 것보다 앞선 조합이 성립**하는데, 그때 뺄셈이 음수로 나가고 예외도 로그도 없었다. **판정을 `sellAt.isBefore(buyAt)` 한 줄로 뒀다** — FIFO가 같은 세션 안의 역전을 이미 막으므로 이것이 곧 "원본 거래일 역전"이고, 동시에 **응답에 싣는 두 값으로 직접 판정**해 결함의 정의(그 둘의 차가 음수)와 조건이 어긋날 자리가 없다. clamp도 서비스 벽시계 경과분 대체도 하지 않았다 — 둘 다 같은 응답의 `buyAt`·`sellAt`과 산술이 어긋나 화면이 표시된 두 시각의 차를 복원할 수 없다. **조건은 역전뿐이고 `sameSessionCompleted=false` 전체가 아니다**(정상 cross-session은 값이 의미가 있고 계약의 nullable 목록에도 없다). spec §파생 사실 계산에 규칙을 추가하고 `api-contracts.md`에 "nullable 목록에 없고 역전 시에만 `null`"을 명시했다(목록 자체는 불변). `returnRate`의 `ZERO` 폴백은 지시대로 건드리지 않았다.

- 17:10 — 이슈 #208 게이트 상한 버그 수정(tester 실측 진단). 과거 서비스 날짜 분기의 `LocalTime.MAX`가 **MySQL TIME 파라미터로 살아남지 못했다** — `reveal_time`은 소수 초가 없는 `TIME`인데 `LocalTime.MAX`는 `23:59:59.999999999`이고, Connector/J가 나노초를 실어 보내면 MySQL이 소수 초를 **올림해 `00:00:00`으로 접는다.** 그래서 상한이 자정이 되어 **어제 이전에 판 체결의 `priceMoves`가 항상 빈 배열이고 `buyToNewsMinutes`가 항상 `null`인데 예외도 로그도 없었다** — 게이트 ⑭식 되돌림을 막으려 넣은 분기가 더 심하게 감추는 방향으로 뒤집혀 있었다. 상수 `PAST_SERVICE_DATE_CUTOFF = LocalTime.MAX.withNano(0)`으로 빼고 **"`LocalTime.MAX`로 되돌리지 마라"를 실측 증거와 함께 상수 javadoc에 박았다** — sentinel을 "가장 큰 시각"으로 되돌리는 회귀가 조용히 같은 버그를 되살리기 때문이다. **나노초를 버리는 것 말고 다른 방향(질의를 `<`로 바꾸기·상한 제거)은 게이트 판정 자체를 바꾸므로 쓰지 않았다.** 회귀는 `PriceMoveEventRepositoryTest`가 실제 쿼리로 못박아 뒀고 그 테스트와 단위 상한 단정(`isAfterOrEqualTo(23:59:59)`)은 건드리지 않았다.

- 10:20 — 이슈 #208 tasks 3번. `postSellFlow`(게이트 + 값 전체)·`counterfactuals`(게이트 + 3종 `price`·`at`)·`peerComparison`(상수 `NOT_YET`)을 채웠다. **게이트는 `serviceDateOf(trade)` + `MarketSessionTimes.MARKET_CLOSE_TIME`으로 "그 체결의 서비스 날짜 15:30"이고 상수를 새로 선언하지 않았다** — 오늘로 잡으면 어제 판 체결이 오늘 오전에 `NOT_YET`으로 되돌아간다(게이트 ⑭). **`atClose`·`closePrice`는 `getFullDayCandles`의 마지막 원소**로 잡았다(오름차순 정렬) — 리터럴 15:30은 그 분봉이 없는 날 예외 없이 `null`이 되고, `getPreviousTradingDayClose`가 시가 갭에서 같은 이유로 같은 방식을 쓴다. **분봉 조회를 한 번으로 합쳤다** — 극값(2번)이 이미 하루치를 읽고 있었으므로 `findHoldExtremes`가 리스트를 인자로 받게 바꾸고, 노출을 가르는 것은 조회 횟수가 아니라 "어디까지 잘라 쓰는가"임을 주석에 남겼다(극값은 매도 시각까지, 매도 이후 구간은 게이트가 열린 뒤에만). `postSellHigh`의 경계를 **배타**로 둔 것은 극값이 매도 분봉을 포함(양 끝 포함)하기 때문이다 — 같은 분봉이 두 값에 동시에 잡히면 화면이 나란히 놓는 의미가 없다. **`postSellFlow`도 `sameSessionCompleted=false`에서 필드 자체를 `null`로 뒀다** — 계약의 nullable 목록에 명시돼 있고 §파생 사실 계산의 "위 전부"에 `[매도 후 흐름]` 블록이 포함된다. `status`는 게이트만 반영하고 데이터 유무를 반영하지 않는다(§C-4가 게이트로만 정의한다) — 게이트 뒤 분봉이 없으면 `READY` + 값 `null`이고, `NOT_YET`으로 감추면 장 마감 뒤에도 영원히 "아직"으로 보인다. **`peerComparison`은 `priceMoveId`조차 채우지 않았다** — 기준 카드는 알지만 계약이 `NO_EVENT`에서 그 필드까지 `null`로 정했고, `NOT_YET`에서만 채우면 7번이 판정을 붙이는 순간 같은 체결의 값이 사라지는 조합이 생긴다. **7번 경계는 응답 조립 지점에 블록 주석으로 남겼다**(`returnRate` 수수료 재계산식, `peerComparison` 판정 순서, 그로 인해 5번 재생성 게이트가 구조적으로 안 열린다는 것). `api-contracts.md`에 "현재 구현 범위" 인용 블록을 더해 두 자리가 값 누락이 아니라 이슈 경계임을 명시했다. 스키마·마이그레이션·컨트롤러 무변경.

- 13:30 — 이슈 #208 tasks 4번. **트랜잭션 경계를 세 조각으로 갈랐다** — `PostSellFeedbackReader`(신설, `@Transactional(readOnly = true)`, 원장·분봉·카드 읽기) → `TradeFeedbackRepository.findByTradeId`(기존 서술) → `NarrativeService.resolvePostSellNarrative`(**트랜잭션 없음**) → `TradeFeedbackWriter`(신설, `@Transactional`, 저장만). `PostSellFeedbackService`는 진입점으로 남고 **`@Transactional`을 뗐다.** **읽기를 별도 빈으로 뺀 것이 이 항목의 핵심 판단이다** — 트랜잭션이 메서드 단위라 읽기와 LLM 호출이 한 메서드에 있으면 경계를 좁힐 방법이 없고, 같은 클래스 private 메서드에 애노테이션을 붙이는 것은 자기호출이라 무효다(`PriceMoveCardWriter`를 저장 쪽에서 뺀 것과 같은 판단·방향만 반대). 그냥 `@Transactional`만 떼면 **`open-in-view: false`라 `Trade.instrument`·`stockReplaySession` lazy 접근이 `LazyInitializationException`으로 터진다** — 그래서 reader가 lazy 연관을 전부 값으로 바꿔 응답 record로 돌려준다. 조회 경로에 LLM이 들어오는 첫 자리이고 호출이 중앙값 2.5초·p95 3.1초(#198)라 **사용자가 기다리는 그 3초 동안 커넥션을 쥐면 풀 크기가 곧 동시 조회 수의 상한**이 된다. 서술 셋을 얹는 것은 `PostSellFeedbackResponse.withNarrative` 하나로 모았다(호출부가 25개를 다시 나열하면 필드가 늘 때 그 목록이 조용히 뒤처진다). `narrativeStatus`는 **상수 `READY`이고 분기가 없다** — 보장을 지키는 것은 `NarrativeService`의 1단계 템플릿 폴백이라 여기서 상태값을 판정하면 그 근거가 둘로 갈린다. **프롬프트에 반사실을 넣지 않았고**(`PostSellPromptDto`에 필드가 없다) 집단 비교 자리는 매핑만 해 뒀다 — 지금은 항상 `NOT_YET`이라 넘길 값이 없는 것이 정상 상태다. 템플릿 셋째 문장 생략은 `NarrativeTemplateBuilder`가 이미 `holdHighPrice == null`로 처리하고 있어 값만 그대로 넘겼다. **`UNIQUE(trade_id)` 동시 삽입 충돌을 `DataIntegrityViolationException` 한 곳에서 흡수했다** — 화면 이중 요청이면 둘 다 "기존 행 없음"을 보고 각자 생성하는데, 여기서 500을 내면 조회가 실패한다(§실패 처리). 쓰기 경로는 `TradeFeedbackWriter` 하나뿐이고 주입된 리포지터리도 `trade_feedbacks` 하나라 **원장에 닿는 경로가 없다.** 5번이 얹을 전이 메서드 자리를 writer 주석에 남겼다. 스키마·마이그레이션·컨트롤러·문서 무변경. 단위 테스트 3파일의 조립 지점을 reader로 기계적으로 재지정했다(반환 타입이 같아 단정은 무변경).

- 16:20 — 이슈 #208 tasks 5번(마지막 구현 항목). 재생성 판정을 `shouldRegenerate`(확정 전 → 누적 상한 안 → 게이트) 세 조건으로 폈고 **순서를 그렇게 둔 이유는 상한을 게이트 뒤에 두면 상한을 넘긴 체결이 게이트가 열린 동안 계속 LLM을 부르기 때문**이다. **게이트는 `isRegenerationGateOpen` 하나에 두고 `postSellFlow == READY && peerComparison.status != NOT_YET`로 §C-5 그대로 썼다** — "확정 집계 행 존재"로 두지 않은 것이 핵심이다(보유 구간 카드 0건이면 `price_move_peer_stats` 행이 애초에 안 생기고 그것이 오히려 흔한 경우인데, 행 존재로 판정하면 그 체결에서 매도 후 흐름이 반영된 서술이 영원히 안 만들어지며 예외도 로그도 없다). **`postSellFlow`만 보게 느슨하게 고치지 않았다** — 그러면 15:30~집계 사이에 조회한 사용자가 집단 비교 빠진 문장으로 굳고 재생성은 1회뿐이라 되돌릴 기회가 없다. 지금은 3번이 `peerComparison.status`를 상수 `NOT_YET`으로 둬서 **구조적으로 열리지 않으며, 7번이 판정을 붙이면 이 조건을 한 줄도 안 고치고 열린다**는 것을 게이트 javadoc에 적었다. **누적 상한은 `regeneration_attempts >= max-narrative-retry`로 세고 성공·실패 양쪽에서 증가시켰다** — 컬럼이 세는 것이 "몇 번 시도했는가"이고 성공만 빼면 그 수가 실제 재호출 횟수와 어긋난다(성공은 `narrative_finalized`가 게이트를 먼저 닫으므로 상한 판정에는 영향이 없다). **재생성이 템플릿으로 폴백하면 실패로 취급해 기존 서술을 유지한다** — 템플릿 문장에는 매도 후 흐름·집단 비교가 없어서 덮으면 재생성할수록 서술이 빈약해진다. 실패 시 `generated_at`을 갱신하지 않아 **날짜 기준 리셋이 애초에 성립하지 않는다.** 전이 메서드 2개(`applyRegeneratedNarrative`·`recordFailedRegeneration`)를 `TradeFeedback`에 더하고 게이트 판정은 엔티티에 두지 않았다(게이트가 보는 두 상태값이 저장 컬럼이 아니라 조회 시 계산값이다). **저장은 4번의 `TradeFeedbackWriter`에 메서드 둘을 얹었고 새 경계 컴포넌트를 만들지 않았다** — 두 메서드 모두 **행을 자기 트랜잭션 안에서 다시 읽는다**(호출부의 엔티티는 detached라 전이 메서드를 불러도 더티 체킹 대상이 아니어서 아무 일도 일어나지 않고, 재생성이 매 조회 반복되면서 상한도 안 오른다). 프롬프트 입력은 최초 생성과 **같은 매핑**을 쓴다 — 게이트가 열렸으면 `closePrice`·`sellToCloseRate`와 집단 비교 지표가 채워져 줄이 붙고 그래서 다른 문장이 나온다(매핑을 따로 만들면 그 줄이 빠진 프롬프트로 재생성해 게이트가 무의미해진다). `narrativeStatus`는 여전히 상수 `READY`다. 스키마·마이그레이션·컨트롤러·문서 무변경(V13 컬럼이 이미 있다).

- 03:40 — 이슈 #208 리뷰: **차단 1건 / 권장 5건 / 참고 4건.** 완료 조건 19건은 전부 대응 테스트가 있고 원장 불변은 `flush()` + 체결 행 전 컬럼·`cash_balance`·`remaining_quantity` 값 비교로, 게이트 ⑭은 날짜를 하루 넘긴 조회로, 카드 0건 재생성은 `NO_EVENT` 픽스처로 실제 검증됨. 차단은 보유 구간 경계가 초 단위가 붙은 `executed_at`을 그대로 써서 **운영에서는 매수 분봉이 항상 빠지는데(§파생 사실 계산의 "양 끝 포함") 분 단위 픽스처로는 두 구현이 같은 답을 낸다**는 것. 권장은 `sameSessionCompleted=true`인 채 원본 거래일이 역전되는 조합(계약의 "true면 채워진다"와 충돌), `DataIntegrityViolationException` 포괄 흡수, 트랜잭션 없는 통합 테스트 부재, PRD의 FEED-007↔009 오기다.

- 18:40 — 이슈 #208 reviewer 차단 1건 + 권장 1 + 참고 1 반영. **[차단] 보유 구간 경계를 `candleBoundary`(=`truncatedTo(MINUTES)`) 하나로 모아 세 자리에 적용했다**(극값 필터 하한·상한, 매도 후 최고가 하한, 카드 파인더의 `window_end` 두 경계). `executed_at`이 `DATETIME(6)`이고 `OrderExecutionService`가 `LocalDateTime.now(clock)`으로 찍어 운영 값이 항상 `09:30:17.4xxxxx` 꼴인데 `candle_time`·`window_end`는 정시라, 그대로 비교하면 **하한만 09:30 분봉을 탈락시켜 "양 끝 포함"이 비대칭으로 깨지고** 극값이 매수 분봉일 때 `holdHigh*`·`sellVsHighRate`와 반사실 `atHoldHigh`가 한 봉 밀린 채 조용히 틀렸다. 소수 초 ≥ .5s면 MySQL TIME 파라미터 1초 올림으로 한 번 더 움직인다. **정시 픽스처로는 안 잡힌다는 것까지 주석에 적었다.** **응답의 `buyAt`·`sellAt`은 건드리지 않았다** — 계약이 정한 체결 시각이고 만든 것은 비교용 경계뿐이다. **[권장] 예외 흡수를 타입 좁히기가 아니라 "행 재조회"로 갈랐다** — Hibernate `ConstraintViolationException`은 `HibernateJpaDialect`가 기반 타입으로 번역하고 `DuplicateKeyException`은 JDBC 에러코드 경로의 하위 타입이라, 좁혔다가 매핑이 다르면 정상 경합이 500이 된다. 이 환경에서는 MySQL 없이 실제 번역 타입을 확인할 수 없어 **기억으로 타입을 고르지 않았다.** 재조회는 번역 타입과 무관하게 성립하고 FK·NOT NULL 위반은 행이 없으므로 `WARN`으로 드러난다(조용히 넘기면 그 체결이 조회마다 LLM을 부르는데 200이라 신호가 없다). **[참고] `revealCutoff`의 오늘 분기에 `.withNano(0)`을 붙여** 23:59:59.5~.999 조회에서 상한이 자정으로 접히는 하루 0.5초 창을 닫았다. `PriceMoveQueryService`의 같은 자리는 #180 소유라 건드리지 않았다.

- 20:10 — 이슈 #208 reviewer 후속 2건. **① 분 계산 절삭 버그** — `Duration.between(...).toMinutes()`가 0 방향 절삭이라 `09:30:17.4 → 11:25`이 **114**가 되어 계약 예시 `minutesAfterBuy: 115`가 재현되지 않았다. 대상 넷(`holdingMinutes`·`minutesAfterBuy`·`minutesBeforeSell`·`buyToNewsMinutes`)을 `minutesBetween`(두 끝점을 분으로 내린 뒤 `ChronoUnit.MINUTES.between`) 하나로 모았고, **규칙의 단일 출처를 `onMinuteBoundary`로 뽑아 `candleBoundary`(분봉·카드 구간)와 함께 쓰게 했다** — 두 곳이 다른 규칙으로 내리면 "카드가 보유 구간에 들어왔는데 `minutesAfterBuy`가 음수"처럼 한 응답 안에서 앞뒤가 안 맞는 조합이 생긴다. **부호 대칭을 확인했다**: 0 방향 절삭은 양수에서 값을 줄이고 음수에서는 키우는데(`-104.7 → -104`), 끝점을 먼저 내리면 양쪽에서 같은 규칙이 된다. **② `sameSessionCompleted` 역전** — 같은 원본 거래일을 두 서비스 날짜에 재생하면 원본 거래일이 **같은 채로** 시각만 역전되는 조합(첫 재생일 오후 매수 → 다음 재생일 오전 매도)이 성립하는데, 날짜만 대조하던 판정이 `true`를 내면서 파생 사실이 전부 비어 계약("`true`면 채워진다")과 어긋났다. 사용자 결정대로 **역전을 `false` 조건으로 더했고 판정을 `isReversed` 하나로 모아 `holdingMinutes`의 `null` 조건과 같은 함수를 가리키게 했다** — 두 곳에 따로 쓰면 한쪽만 고치는 순간 "`sameSessionCompleted=true`인데 `holdingMinutes=null`" 조합이 나간다. **역전 ⇒ 두 값 모두, 그 역은 성립하지 않는다**를 코드·spec·계약 세 곳에 같은 문장으로 남겼다. spec §파생 사실 계산에서 reviewer가 자기모순으로 짚은 문장을 고쳤다 — 재재생 조합은 원본 거래일이 **같으므로** 날짜 대조로는 안 걸리고, 이제 참인 이유가 "역전 자체가 `false` 조건"이라는 것을 명시했다. `api-contracts.md`는 nullable 목록을 건드리지 않고 그 목록에 들어오는 경우가 하나 늘었다는 관계와 분 계산 규칙만 더했다.
- 10:40 — 이슈 #212 tasks 1번. `buildCounterfactuals`·세 `scenarioAt*` 메서드에 `quantity`·`buyBasis`(배분 매수원가+매수수수료)를 넘겨 `counterfactualReturnRate`가 `OrderExecutionService.priceOrder`와 같은 식(매도금액 FLOOR → 그 금액에 요율 곱해 다시 FLOOR)으로 시나리오별 수익률을 계산하게 했다. `STOCK_FEE_RATE`는 그 클래스의 private 필드라 공개 의존을 만들지 않고 **같은 값(0.00015)만 로컬 상수로 재선언**했다. `price·null`은 이미 상위에서 `CounterfactualScenario` 자체를 `null`로 걸러 두므로 손대지 않았다. `api-contracts.md` 예시(`atClose -0.0117`·`atHoldHigh 0.0111`·`atFirstMoveAfterBuy -0.0103`)를 수기로 재현해 검증. `PostSellPromptDto`·게이트·마이그레이션 무변경.
- 11:05 — 이슈 #212 tasks 2번. `portfolio`에 `HolderPopulationQueryService.countHoldersAtTime(instrumentId, at)` 신설 — `HoldingLotRepository`·`TradeAllocationRepository`에 각각 "holding 단위로 시점 `at` 이전(포함) 수량 합"을 JPQL 생성자 표현식(`HoldingQuantitySum` record)으로 묻는 조회를 더하고, 서비스가 두 합의 차가 양수인 holding 수를 센다. **`original_quantity`(불변)만 쓰고 `remaining_quantity`(가변)는 쓰지 않았다** — spec [집단 비교] 블록 그대로. 매도 쪽은 **`trade_allocations.created_at`이 아니라 그 배분이 속한 매도 체결(`sellTrade`)의 `executed_at`**을 기준으로 필터했다. `holdingId`는 서비스 밖으로 나가지 않고 반환값은 `int` 카운트 하나뿐이라 회원 식별자가 노출되지 않는다(§완료 조건). `feedback`은 이 서비스를 아직 주입하지 않는다 — 호출부(`PeerStatsBatchService`)는 3번 항목이 만든다. 3번 항목이 검증할 함정 픽스처("T 시점 보유, 집계 시점 전 전량 매도")는 이 식이 매도 `executed_at`이 `at`보다 늦으면 순보유량 차감에서 빠지므로 그대로 모집단에 남는다. 스키마·엔티티·컨트롤러·문서 무변경.
- 12:10 — 이슈 #212 tasks 3번. `PeerStatsBatchService`(트랜잭션 미개방) 신설, 재생세션 `READY` 확인 → `PriceMoveEventRepository.findByMarketAndOriginTradeDate(STOCK, originTradeDate)`(신설, 노출 게이트 없이 그날 카드 전체)를 순회 → 카드마다 `existsByPriceMoveEventIdAndServiceDate`(신설)로 사전 확인 후 스킵 또는 저장. **모집단 조회 서비스에 `minutesToSellForHoldersAtTime(instrumentId, at)`을 추가**하면서 기존 `countHoldersAtTime`과 공유하던 "T 시점 보유 holding id 집합" 계산을 `holderIdsAtTime` private 메서드로 뽑았다 — `TradeAllocationRepository`에 "T 뒤(초과) 첫 매도 `executed_at`"을 holding 단위로 묻는 조회(`findFirstSellExecutedAtByHoldingForInstrumentAfter`, 신설 record `HoldingSellTime`)를 더해 그 결과를 holder id 집합과 교차시켜 매도까지 걸린 분의 정수 리스트만(회원 식별자 없이) 반환했다. 배치가 이 리스트로 `soldWithin30MinCount`(≤30 카운트)와 `medianMinutesToSell`(정렬 후 중앙값, 빈 리스트면 `null`)을 계산해 저장한다. **`serviceDate`는 `Clock` 기반 오늘 날짜**라 재재생 시 서비스 날짜별로 행이 갈린다. 크론 `feedback.batch.peer-stats-cron`(`FeedbackBatchProperties`에 필드 추가, yml·`@DefaultValue` 양쪽 `0 32 15 * * MON-FRI`)에 `zone = "Asia/Seoul"`을 붙였다. **풀 크기는 `@Scheduled` cron 전수를 grep으로 다시 세어(9→10) `application.yml`·`application-crypto-real.yml` 두 곳의 주석과 `pool.size`를 함께 갱신**했고, 테스트 전용 `feedback-schedules-disabled-for-tests.yml`에도 `peer-stats-cron: "-"`를 추가해 공유 컨테이너 오염을 막았다(기존 cron·crypto-cron과 같은 패턴). 저장은 배치 오케스트레이션에서 `PriceMovePeerStatRepository.save()`를 직접 호출한다 — 카드 1건 저장이 짧아 별도 writer 컴포넌트를 만들지 않았다(지시문의 대안 허용). 스키마·엔티티·컨트롤러·문서 무변경(V13 컬럼 이미 존재).
- 12:45 — 이슈 #212 tasks 4번. `PriceMovePeerStatRepository`에 `findByPriceMoveEventIdAndServiceDate`(신설)를 더해 "그 카드의 그 매도 체결의 서비스 날짜" 확정 집계 행 하나만 조회하게 했다. `PostSellFeedbackReader.buildPeerComparison`이 `priceMoves.get(0)`(반사실 `atFirstMoveAfterBuy`와 같은 기준 카드)이 없으면 `NO_EVENT`를 1순위로 즉시 반환하고, 있으면 `serviceDateOf(trade)`(매도 체결의 서비스 날짜)로 그 조회를 불러 없으면 기존 `peerComparisonNotYet()`(전 필드 `null`, `priceMoveId` 제외)을 그대로 재사용, 있으면 `holderCount < 5`로 `INSUFFICIENT_SAMPLE`/`READY`를 가른다. **`yourMinutesToSell`은 새로 계산하지 않고 `priceMoves.get(0).minutesBeforeSell()`을 그대로 썼다** — `toHeldPriceMoveItem`의 그 필드가 정의상 이미 "매도시각 − 카드 windowEnd" 분이라 같은 카드·같은 값이다. `soldWithin30MinRate`는 저장된 `soldWithin30MinCount ÷ holderCount`를 scale 4 `HALF_UP`(신설 상수 `PEER_RATE_SCALE`, 다른 파생 비율 상수와 독립)으로 이 자리에서 계산했다 — 배치는 개수만 저장했다(3번 항목). 응답 매핑에 회원 식별자를 넣지 않았음을 확인(엔티티 자체가 식별자를 안 가짐). `PostSellFeedbackReader`의 "plan.md 7번이 끼울 자리" 경계 주석과 `PostSellFeedbackService`의 관련 게이트·프롬프트 javadoc 두 곳에서 "이슈 #208 범위에서 구조적으로 안 열린다"류의 낡은 서술을 이 항목이 실제 판정을 붙였다는 사실로 갱신했다 — **게이트 판정 코드(`isRegenerationGateOpen`) 자체는 한 글자도 안 건드렸다.** 스키마·엔티티·컨트롤러·문서 무변경(V13 컬럼 이미 존재, 컨트롤러 필드는 기존 자리를 채우기만 함).
- 12:55 — 이슈 #212 tasks 5번(마지막 구현 항목). `PostSellFeedbackRegenerationIntegrationTest`의 `@MockitoBean PostSellFeedbackReader`와 `facts()`/`givenFacts()`를 걷어내고 실제 원장 픽스처(매수 lot·매도 체결·`TradeAllocation`·분봉 5개)로 바꿨다 — `PostSellFeedbackPeerComparisonGateIntegrationTest`(4번 항목)가 이미 만든 픽스처 패턴을 그대로 재사용했다. **카드 0건 → `NO_EVENT`는 카드를 저장하지 않는 것으로, `INSUFFICIENT_SAMPLE`은 카드 1건 + `PriceMovePeerStat.create(..., holderCount=4, ...)` 직접 저장으로, `NOT_YET`은 카드만 저장하고 통계 행을 안 만드는 것으로** 재현했다 — 배치(3번 항목)를 다시 돌리지 않았다. 기존 완료 조건 3건(게이트 통과 후 1회, 누적 상한, 카드 0건 재생성)의 시나리오·단정은 그대로 두고 대역만 바꿨다. **검증력 증가를 실측했다** — `buildPeerComparison`의 `NO_EVENT` 분기를 임시로 `peerComparisonNotYet()`(구 "확정 집계 행 존재로만 판정" 버그)으로 되돌리자 카드 0건에 의존하는 테스트 3건이 실제로 실패했고, 원복 후 5건 전부 재통과를 확인했다. 파일 상단 주석의 "PostSellFeedbackReader를 mock으로 대체한다" 단락을 "실제 판정 경로로 재현한다"로 갱신했다. 스키마·엔티티·컨트롤러·문서 무변경.
- 13:20 — 이슈 #212 tasks 6번(원장 불변 확장). `PeerStatsBatchServiceIntegrationTest`에 원장 불변 테스트를 추가해 `PeerStatsBatchService` 실행 전후로 원장 6테이블(행 수)과 계좌 잔액·lot 잔여수량(값)이 그대로임을 확인했고, `PostSellFeedbackNarrativeIntegrationTest`에 카드 1건 + `PriceMovePeerStat`(holderCount=5, READY) 픽스처를 심어 `peerComparison`이 실제로 채워진 조회에서도 같은 보장이 성립함을 새 테스트로 확인했다 — 기존 두 테스트가 이미 세운 "flush 후 값 비교" 패턴을 그대로 재사용했다(행 수만으론 UPDATE를 못 잡는다는 agent-mistakes.md 2026-08-04 기록). **뮤테이션 검증** — 두 배치·서비스 각각에 `HoldingLot.consume(1)`을 임시로 끼워 넣어(배치 쪽은 생성자에 `HoldingLotRepository`를 임시로 더해 주입, 조회 쪽은 테스트 안에서 raw SQL로 `remaining_quantity`를 감소) 신설 테스트 2건이 실제로 빨개지는 것을 확인한 뒤 두 파일 모두 원상복구했다(`git diff` 무변경 확인). 스키마·엔티티·컨트롤러·문서 무변경.
- 17:30 — 이슈 #212(반사실 시뮬레이션·집단 비교) 리뷰: 차단 0건 / 권장 0건 / 참고 0건 — 머지 가능. FLOOR 수수료 재계산·`peerComparison` 4단계 판정·`HolderPopulationQueryService`의 lot·배분 재구성·`PeerStatsBatchService`(트랜잭션 미개방, 재재생 서비스 날짜 분리)·원장 불변 확장·문서 동기화(api-contracts.md 인용 블록 제거, prd.md FEED-010·011 완료) 전부 spec.md §확정값과 일치 확인. run-log의 뮤테이션 검증 기록(각 항목이 실제로 실패하는 것을 확인 후 원복)까지 대조해 테스트가 실제로 구현을 검증함을 확인.
- 09:10 — 이슈 #225 tasks 1번 구현. `PriceStore`에 `recordSnapshot`·`getSnapshots`(Sorted Set `price:crypto:{symbol}:snapshots`, score=epoch millis, member=`"{epochMillis}:{price}"`)를 추가하고, `market`에 `CryptoPriceSnapshotService`(신설, `market.crypto.price-snapshot-cron` 매 분 정각·`zone=Asia/Seoul`)를 신설해 `InstrumentService.getInstrumentEntities(CRYPTO)`로 종목을 얻고 `isPriceAvailable=false`면 건너뛰며 기록한다. `sigma-lookback-hours`는 신설 `market.crypto.sigma-lookback-hours`(`MarketCryptoProperties`, 기본 24)로 `market`이 소유하고 기록마다 그 창을 넘는 원소를 `removeRangeByScore`로 가지치기했다 — feedback.crypto.sigma-lookback-hours(2번 항목에서 신설)와 같은 값을 유지해야 함을 두 곳 주석에 남겼다. `pool.size` 10→11, `application.yml`·`application-crypto-real.yml` 계산 주석 갱신. `compileJava`·`spotlessApply` 통과. 테스트는 tester 담당(미작성).
- 09:35 — 이슈 #225 tasks 2번 구현. `feedback.crypto.*` 6개 키(§C-7 값 그대로)를 `FeedbackCryptoProperties`(신설, `FeedbackDetectionProperties`와 같은 형태)로 바인딩하고 `application.yml`에 같은 블록을 추가했다(`z-score-k`는 새로 두지 않고 `feedback.detection.z-score-k` 재사용). `NewsMatcher`에 `matchCrypto(instrumentId, occurredAt)`를 별도 메서드로 추가 — 근거창 `[occurredAt - crypto.match-before-minutes, occurredAt]`이고 기존 `findByInstrumentIdAndTypeAndPublishedAtBetweenOrderByPublishedAtAsc`(NEWS만, 공시 제외)를 그대로 재사용, 정렬·상한 절단도 기존 `sortAndTruncate`(`max-sources-per-card`)를 그대로 재사용했다. 새 리포지토리 메서드·새 마이그레이션·컨트롤러 변경 없음. `compileJava` 통과. 테스트는 tester 담당(미작성).
- 09:50 — tester가 `FeedbackCryptoProperties`를 등록하는 `@EnableConfigurationProperties` 설정 클래스가 빠져 `NewsMatcher` 조립이 `NoSuchBeanDefinitionException`으로 실패하고, 이 실패가 `@SpringBootTest` 컨텍스트 캐시를 오염시켜 feedback 도메인 전체(110건)·무관한 order 도메인 테스트까지 실패로 찍히는 것을 발견. `FeedbackDetectionConfig`와 같은 형태로 `FeedbackCryptoConfig`(신설, `feedback/config`)를 추가해 해결. `compileJava` 통과.
- 10:15 — 이슈 #225 tasks 3번 구현. `feedback`에 `CryptoPriceMoveWatcher`(신설)를 추가해 §탐지 알고리즘(코인) 의사코드를 그대로 구현했다 — `CryptoPriceSnapshotService.getSnapshots`로 p_now·p_past를 ±1분 허용오차로 찾고, `rolling-window-minutes` 간격으로 자른 겹치지 않는 구간(최대 288개)의 로그수익률로 σ24를 계산해 표본 부족·σ=0을 각각 조용히 종료시켰다. `k`는 `feedback.detection.z-score-k`를 그대로 썼다(새로 안 만듦). 쿨다운·일일 상한 판정을 위해 `PriceMoveEventRepository`에 `findFirstByInstrumentIdAndMarketOrderByOccurredAtDesc`·`countByInstrumentIdAndMarketAndOriginTradeDate`를 추가했다(기존 `idx_price_move_events_instrument_occurred_at` 인덱스로 충분, 새 인덱스 없음). 근거 매칭은 `NewsMatcher.matchCrypto`, 서술은 `NarrativeService.resolvePriceMoveNarrative`, 저장은 기존 `PriceMoveCardWriter.persist`를 그대로 재사용했다. `feedback.batch.crypto-watch-cron`(`FeedbackBatchProperties`에 필드 추가, 매 분 30초·`zone=Asia/Seoul`)을 신설하고 `pool.size` 11→12, `application.yml`·`application-crypto-real.yml` 계산 주석을 최종 형태(합계 12)로 갱신했다. 새 마이그레이션·컨트롤러 변경 없음. `compileJava` 통과. 테스트는 tester 담당(미작성).
- 11:05 — 이슈 #225 tasks 4번 구현. `PriceMoveEventRepository`에 `findByInstrumentIdAndMarketAndOccurredAtBetweenOrderByOccurredAtAscIdAsc`(신설, market=CRYPTO·`[now-24h, now]`·기존 인덱스 재사용)를 추가하고, `PriceMoveItem.ofCrypto(event, sources, rollingWindowMinutes)`(신설 팩토리 — `windowEnd=occurredAt`·`windowStart=occurredAt-rollingWindowMinutes`, 이미 `LocalDateTime`이라 `ofStock`의 날짜 결합이 불필요)를 더했다. `PriceMoveQueryService`의 임시 `Market.CRYPTO → empty()` 분기를 `getCryptoPriceMoves` 실제 조회로 교체 — `originTradeDate`는 `PriceMoveListResponse.of(null, moves)`로 항상 `null`이고(새 팩토리 불필요), 노출 게이트가 없어 `reveal_time`을 보지 않는다. 정렬은 `occurredAt` 오름차순 + `id` 오름차순 — `windowStart`가 `occurredAt`에서 파생되는 단조 값이라 기존 계약의 "windowStart 오름차순"과 동치임을 리포지토리 javadoc에 남겼다. `rollingWindowMinutes`는 `FeedbackCryptoProperties`를 서비스에 새로 주입해 조회 시점 현재값을 쓴다. 근거 목록은 기존 `findSources`(`PriceMoveEventSourceRepository.findAllByPriceMoveEventIdIn`)를 그대로 재사용. 새 마이그레이션·컨트롤러 변경 없음. `compileJava` 통과. 테스트는 tester 담당(미작성).
- 21:50 — 이슈 #225 tasks 5번(원장 불변 확장). 3번 항목이 이미 `CryptoPriceMoveWatcherIntegrationTest`에 원장 불변 테스트를 심어 뒀어 나머지 두 몫만 채웠다. **`CryptoPriceSnapshotServiceIntegrationTest`(신설)** — 테스트 전용 코인 종목에 `PriceStore.saveTick`+`saveConnectionStatus(CONNECTED)`로 실제 가용 가격을 채운 뒤 `recordSnapshots()`를 실행해 Redis에 스냅샷이 실제로 적재됐음을 `getSnapshots`로 먼저 확인하고, 그 상태에서 원장 6테이블 행 수가 그대로임을 단정했다 — 이 스케줄은 Redis에만 쓰고 원장을 읽지도 않아 UPDATE 경로 자체가 없으므로 값 비교(mutableLedgerValues류)는 추가하지 않았다(행 수 비교로 충분한 이유를 파일 상단 주석에 남겼다). **`PriceMoveQueryGateIntegrationTest`에 코인 분기 테스트 1건 추가** — 실제로 코인 카드+근거 기사를 저장한 뒤 `getPriceMoves`를 호출해 응답에 카드가 채워졌음을 확인하면서 동시에 원장 6테이블 행 수가 그대로임을 단정했다(이 조회는 애초에 원장을 읽지 않는다). 두 파일 모두 다른 원장 불변 테스트와 같은 `LEDGER_TABLES` 목록·`entityManager.flush()` 후 `rowCounts` 패턴을 그대로 재사용했다. Docker로 세 파일(`CryptoPriceSnapshotServiceIntegrationTest` 1건, `PriceMoveQueryGateIntegrationTest` 13건) 전건 통과 확인 — `CryptoPriceMoveWatcherIntegrationTest`는 3번 항목에서 이미 통과 확인된 상태라 재실행하지 않았다. 스키마·엔티티·컨트롤러·문서 변경 없음(구현 코드 변경 없이 테스트만 추가).
- (문서) — 이슈 #225 tasks 6번(문서 동기화, spec 012 마지막 항목). `api-contracts.md`에서 "코인 분기는 아직 구현되지 않았다 (2026-08-04, 이슈 #180)" 인용 블록을 걷어내고 노출 필터 문단에 "게이트 없이"를 보태 실제 코인 동작과 맞췄다. "카드 개수" 문단(코인은 `daily-limit`건)은 이미 실제와 일치해 손대지 않았다. `prd.md` §3 "AI 피드백 — 코인 변동 감시" 행을 미착수 → 완료로 갱신했다 — **근거 칸의 PR 번호는 PR이 아직 생성 전이라 이슈 번호 `(#225)`로 임시 기재했고, 오케스트레이터가 마무리 단계에서 실제 PR 번호로 교체해야 한다.**
- 이슈 #244 tasks 1번. `FeedbackCryptoProperties`에 `watchLockTtlSeconds`(`@DefaultValue("30")`, 검증 없음)를 추가하고 `application.yml` `feedback.crypto` 블록에 `watch-lock-ttl-seconds: 30`을 ADR-0014 인용 주석과 함께 더했다. 드리프트 테스트 2종(`FeedbackCryptoPropertiesTest`·`FeedbackCryptoPropertiesYamlTest`)에 일곱 번째 값 단정을 추가했고, 필드 추가로 깨진 기존 6개 테스트 파일의 `new FeedbackCryptoProperties(...)` 직접 생성 호출에 `30`을 덧붙여 컴파일을 복구했다(`CryptoPriceMoveWatcherTest`·`NewsMatcherCryptoTest`·`NewsMatcherCryptoMatchingWindowTest`·`NewsMatcherTest`·`NewsMatcherMatchingWindowTest`·`PriceMoveCardConfirmationTest`·`PriceMoveQueryServiceTest`). `compileJava`·`compileTestJava` 통과. `CryptoWatchLock` 신설(2번 항목)·`watchOne()` 통합(3번 항목)은 이 항목 범위 밖.
- 이슈 #244 tasks 2번. `feedback.service.CryptoWatchLock`(신설)을 `PriceStore`와 별개로 자체 `StringRedisTemplate`을 주입받게 만들었다(같은 스프링 빈이지만 `PriceStore`를 거치지 않음 — §C-6 대상 아님, ADR-0014). 키는 `feedback:crypto-watch:lock:{instrumentId}`. `tryLock`은 `opsForValue().setIfAbsent(key, UUID.randomUUID(), Duration.ofSeconds(watchLockTtlSeconds))`(1번 항목 프로퍼티) 한 번의 원자적 명령으로 획득하고 성공 시 토큰을 `Optional<String>`으로 반환한다. `unlock`은 `DefaultRedisScript<Long>`(GET==ARGV[1]이면 DEL, 아니면 0)로 check-then-delete를 한 Lua 호출로 묶었다. 두 메서드 모두 `RuntimeException`을 잡아 `DEBUG` 로그만 남기고 조용히 실패로 처리한다(획득 실패와 동일 취급, ADR-0014 §결과 "Redis 장애 중엔 카드 생성이 멈추되 감시 전체는 죽지 않는다"). `compileJava`·`compileTestJava`·`spotlessApply` 통과. `watchOne()` 통합(3번 항목)·단위/통합 테스트는 이 항목 범위 밖(tester 담당).
- 이슈 #244 tasks 3번. `CryptoPriceMoveWatcher`에 `CryptoWatchLock` 필드를 추가하고, `watchOne()`의 z-score 게이트(`score < detectionProperties.zScoreK()`) 통과 직후·`isWithinCooldown` 호출 전에 `cryptoWatchLock.tryLock(instrument.getId())`를 호출했다. 실패하면(`Optional.empty()`) `DEBUG` 로그 한 줄과 함께 `return false`로 그 종목 이번 틱만 종료한다. 성공하면 쿨다운 확인부터 `priceMoveCardWriter.persist`까지 전부를 `try { ... } finally { cryptoWatchLock.unlock(instrument.getId(), lockToken.get()); }`로 감싸 정상 종료·조기 `return false`·예외 어느 경로로 빠져나가도 해제되게 했다 — `watch()`의 기존 종목별 `try/catch(RuntimeException)`은 손대지 않았다(락 해제가 `finally`에 있어 그 바깥 catch와 상호작용해도 락이 새지 않는다). 기존 `CryptoPriceMoveWatcherTest`는 생성자에 `CryptoWatchLock` mock을 추가하되 `tryLock`이 항상 `Optional.of(token)`을 반환하도록 고정해 락 유무와 무관한 기존 오케스트레이션·σ 계산 단정 20건이 그대로 통과함을 확인했다(락 자체의 동작은 이 항목 범위 밖 — tester가 별도 테스트로 검증). `compileJava`·`compileTestJava`·`test --tests CryptoPriceMoveWatcherTest`(20건)·`spotlessApply` 통과.
- 이슈 #244 tasks 4번(오케스트레이터 직접 지시 — "방어가 없으면 실제로 중복이 생긴다"를 먼저 증명). 기존 `CryptoPriceMoveWatcherConcurrencyIntegrationTest`(방어 켠 상태, 카드 1건 단정)를 `CryptoWatchLockConcurrencyTest`로 옮기고 같은 클래스에 재현 테스트를 나란히 추가했다. **재현 테스트**는 Spring 빈이 아니라 이 클래스 안에서 직접 만든 `mock(CryptoWatchLock.class)`을 끼운 별도 `CryptoPriceMoveWatcher` 인스턴스를 조립한다 — `tryLock`을 호출마다 매번 `Optional.of(UUID.randomUUID())`로 성공시켜, "두 스레드 다 자기가 락을 얻었다고 믿지만 서로를 막지 못하는" 무방비 상태를 그대로 재현했다(실제 Redis 락을 쓰는 [방어 켠] 테스트의 스프링 빈과는 별개 인스턴스라 서로 영향이 없다). 두 테스트 모두 `LimitOrderConcurrencyIntegrationTest`의 ready/start `CountDownLatch` 관례로 진짜 동시 실행을 강제했다. **Docker로 실제 실행해 재현이 진짜임을 확인했다** — 재현 테스트가 카드 2건·`NarrativeService` 2회 호출을 실제로 단정해 통과했고(`TEST-...CryptoWatchLockConcurrencyTest.xml`: `tests="2" failures="0" errors="0"`), [방어 켠] 테스트는 같은 조건에서 1건만 통과함을 대조 확인했다 — 억지로 통과시키지 않고 실측으로 검증됨. `compileJava`·`compileTestJava`·`spotlessApply`·`test --tests CryptoWatchLockConcurrencyTest`(Docker, 2건) 통과.
- 이슈 #244 tasks 5번(문서 동기화 검증, 코드 변경 없음). spec.md §C-9(432줄 "다중 인스턴스에서는 Redis 락(ADR-0014)이 1차 방어선")·§탐지 알고리즘(코인)(688줄 게이트 순서·708줄 락 범위·TTL 참조)·§C-7(366줄 `watch-lock-ttl-seconds: 30`)·§실패 처리(1125줄 "그 틱을 건너뛴다. 대기하지 않는다")·§완료 조건(1289-1292줄 "동시성 방어" 2건)을 `CryptoWatchLock.java`(키 `feedback:crypto-watch:lock:{instrumentId}`·Lua check-then-delete·`tryLock`/`unlock` 시그니처)·`CryptoPriceMoveWatcher.java`(z-score 게이트 → 락 → 쿨다운 → 일일 상한 → 근거 매칭 → 서술 → 저장, 락 실패 시 DEBUG+`return false`)·`application.yml`(`watch-lock-ttl-seconds: 30`)과 대조했다. **불일치 없음** — 클래스명·키 형식·TTL 프로퍼티 키·게이트 순서·실패 시 로그 레벨(DEBUG)·완료 조건 2건 ↔ 커버 테스트(2·3번=`CryptoWatchLock`+`watchOne()` 통합, 4번=`CryptoWatchLockConcurrencyTest`) 전부 spec·ADR 원문 그대로라 spec.md 수정 없음. `ai/api-routes.md`·`docs/api-contracts.md`·`ai/prd.md` §3은 5번 항목 설명대로 대상 아님(재확인 생략).
- 19:35 — 이슈 #244 PR 리뷰: 차단 0건, 권장 0건, 참고 0건 — 락 구현·통합 지점·동시성 재현 테스트가 ADR-0014·spec과 일치, 원장·마이그레이션 영향 없음 확인, 머지 가능.
- 19:57 — 이슈 #244 독립 2차 리뷰: 차단 0건 / 권장 4건(통합테스트 접미사 `IntegrationTest` 유실, `watch-lock-ttl-seconds` 0 이하 미검증 시 카드가 조용히 0건, Redis 장애가 정상 경합과 같은 DEBUG로 묻힘, 재현 테스트의 2건 단정이 인터리빙 미보장) / 참고 3건 — 락 원자성·해제 경로·원장/마이그레이션 무영향은 이상 없어 머지 가능.
- 20:10 — 2차 리뷰 권장 4건 + 참고 1건 전부 반영. **[권장 1]** `CryptoWatchLockConcurrencyTest`를 `CryptoWatchLockConcurrencyIntegrationTest`로 `git mv`(컨벤션의 `XxxIntegrationTest` 접미사 복원, 클래스 선언·`@Import` 자기참조도 함께 변경). **[권장 2]** `FeedbackCryptoProperties` compact constructor에 `watchLockTtlSeconds < 1` 검증을 다섯 번째로 추가하고("0 이하 → Redis 오류 → tryLock이 삼켜 항상 empty → 카드 영구 0건"을 주석에 남김) `FeedbackCryptoPropertiesTest`에 실패 케이스 테스트를 더했다. **[권장 3]** `CryptoWatchLock.tryLock`의 정상 경합(`setIfAbsent`가 예외 없이 `false`)은 `DEBUG`로 남기고, `catch(RuntimeException)`(진짜 Redis 장애) 경로만 이 저장소의 인프라 실패 관례(`WARN`)로 올렸다 — `unlock`의 catch도 동일하게 `WARN`으로 올렸다. `CryptoPriceMoveWatcher`의 로그 문구도 "다른 인스턴스가 처리 중이거나 Redis 문제로 락을 얻지 못함"으로 원인을 단정하지 않게 완화했다. **[권장 4, 가장 중요]** 재현 테스트의 `ready`/`start` latch는 `watch()` 진입만 맞출 뿐이라 한 스레드가 먼저 커밋을 끝내면 다른 스레드가 쿨다운에 걸려 우연히 1건만 나올 위험이 있었다 — `narrativeService.resolvePriceMoveNarrative` 호출 지점(둘 다 쿨다운·일일상한·근거매칭을 이미 통과한 시점)에 `CyclicBarrier(2)`를 넣어 두 스레드가 반드시 인터리빙된 상태로 저장 단계에 진입하게 만들어 재현을 결정론적으로 바꿨다("방어 켠" 테스트는 손대지 않음 — 락이 있으면 애초에 barrier가 필요 없다). **[참고]** run-log의 5번 항목 행을 `(시각미상)`에서 실제 시각(19:20)으로 정정. `FeedbackCryptoPropertiesTest`(8건)·`CryptoWatchLockTest`(7건)·`CryptoWatchLockConcurrencyIntegrationTest`(Docker, 2건) 전건 통과, `compileJava`·`compileTestJava`·`spotlessApply` 통과.
- 21:05 — `/review-pr 254`(승인, 권장 6건·참고 4건) 전부 반영. **[권장 1]** `CryptoWatchLock.unlock`이 `redisTemplate.execute(UNLOCK_SCRIPT, ...)`의 반환값(`Long`)을 이제 받아 `1`이 아니면(토큰 불일치 — TTL 만료 후 다른 인스턴스가 락을 새로 잡음) `WARN`으로 남긴다(ADR-0014 §후속의 TTL 재조정 근거 확보). **[권장 2+3]** TTL 기본값을 30 → **45**로 올렸다 — 락 안에서 도는 LLM 호출(`feedback.llm.timeout-seconds`, 20초)이 최악 시간이라 30과의 실질 여유는 약 1.5배(10초)뿐이었던 것을 45(약 2배, 25초)로 정정했다. `FeedbackCryptoProperties`(`@DefaultValue`+주석에 "llm.timeout-seconds보다 커야 한다" 하한 근거 명시)·`application.yml`·`spec.md` §C-7·`FeedbackCryptoPropertiesTest`(기본값 45, 오버라이드 값은 45와 겹치지 않게 60으로 교체)·`FeedbackCryptoPropertiesYamlTest`를 함께 갱신했고, `ADR-0014` §결정·§결과·§후속의 "10배 가까운 여유" 오기를 "약 2배(25초)"로 정정했다. **[권장 4]** `CryptoPriceMoveWatcherTest`에 `LockReleaseGuarantee` 중첩 클래스를 신설해 (a) 정상 카드 생성 후 `tryLock`이 돌려준 토큰 그대로 `unlock`이 호출되는지, (b) `newsMatcher.matchCrypto`가 예외를 던져도 `finally`에서 `unlock`이 호출되는지 2건을 추가했다. **[권장 5]** `CryptoWatchLockConcurrencyIntegrationTest`에 결정론적 신규 테스트를 추가 — 테스트 스레드가 실제 스프링 빈 `cryptoWatchLock`으로 먼저 `tryLock`을 쥔 채 `cryptoPriceMoveWatcher.watch()`를 동시성 없이 호출해 카드 0건·`NarrativeService` 0회를 단정한다(기존 2-스레드 테스트는 그대로 둠). **[권장 6]** `tasks-244.md` 1번 항목에 "2차 리뷰([권장 2])로 방침이 바뀌어 검증을 추가했다" 정정 문단을 덧붙였다. **[참고 1]** `context-notes.md`의 "#244를 구현이 아니라 판정 이슈로 잡은 근거" 절 끝에 결론(ADR-0014 승인 → Redis 락 채택 → PR #254) 한 줄을 추가했다. **[참고 2]** run-log의 남은 `(시각미상)` 6곳(1~4번 항목·1차 리뷰 2곳)을 18:40~19:35 실제 시각으로 정정했다. **[참고 3]** `runConcurrently`의 `awaitTermination` 단정을 `finally` 밖으로 옮겨 try 블록의 실패 원인이 가려지지 않게 했다. **[참고 4]** `verify(narrativeService, times(N)).resolvePriceMoveNarrative(any())`를 `argThat`으로 좁혀 이 테스트의 종목명("락경합코인")과 일치하는 호출만 세도록 격리를 강화했다. `FeedbackCryptoPropertiesTest`(8)·`FeedbackCryptoPropertiesYamlTest`(3)·`CryptoWatchLockTest`(7)·`CryptoPriceMoveWatcherTest`(22)·`CryptoWatchLockConcurrencyIntegrationTest`(Docker, 3) 전건(43건) 통과, `compileJava`·`compileTestJava`·`spotlessApply` 통과.
- 22:34 — PR #254 3라운드 리뷰(승인, 차단 0 / 권장 2 / 참고 4) 전부 반영. **[권장 1, 실제 테스트 결함]** `CryptoWatchLockTest.unlockExecutesTheCheckThenDeleteScriptWithTheLockKeyAndGivenToken`이 `redisTemplate.execute`를 스텁하지 않아 mock 기본값 `null`이 돌아왔고, "정상 해제"라는 이름과 달리 2라운드에서 추가한 `deleted == null` WARN 분기를 타며 통과하고 있었다 — **정상 경로(1L)를 어떤 단위 테스트도 커버하지 않던 상태다.** `thenReturn(1L)`을 명시해 실제로 정상 경로를 지나게 고치고, 토큰 불일치(`0L` 반환 — TTL 만료 후 다른 인스턴스가 락을 새로 잡은 경우)에 예외 없이 넘어가는지 단정하는 테스트를 한 건 추가했다(7건 → 8건). **분기 분리를 실측했다** — `unlock`의 `deleted == 1L` 자리에 임시로 `AssertionError`(catch(RuntimeException)에 안 잡힌다)를 넣자 정상 경로 테스트만 실패하고 0L·예외 테스트는 통과해, 세 테스트가 실제로 서로 다른 분기를 지남을 확인한 뒤 원복했다. **[권장 2]** `tasks-244.md` 1번 항목의 30/45·검증 유무 혼재를 정리했다 — 26·29줄 수치를 45로, "검증에는 추가하지 않는다"를 "검증(`< 1`)도 나머지 넷과 같이 추가한다"로 고치고, 정정 문단은 현재 값 서술을 빼고 "왜 바뀌었는지"만 남겼다. **[참고 1]** `CryptoWatchLockTest`·`CryptoPriceMoveWatcherTest`의 하드코딩 TTL `30`을 `IRRELEVANT_WATCH_LOCK_TTL_SECONDS` 상수로 뽑고 "이 테스트의 관심사가 아니다"를 주석으로 드러냈다(값은 그대로, TTL이 실제 검증 대상인 `tryLockPassesConfigured...`의 `5`는 손대지 않음). **[참고 2]** ADR-0014에서 "PR #254 리뷰 정정"·"초기 판단은 틀렸다" 같은 **정정 경위 문구**를 걷어냈다 — 이 ADR은 이 PR이 처음 도입하는 문서라 머지 후 독자에게 저장소에 없던 이력을 가리키게 된다. TTL 45의 판단 근거(`llm.timeout-seconds` 20의 약 2배, 여유 25초)는 그대로 남겼고, 정정 경위의 정본은 이 run-log다. **[참고 3]** ADR-0014 §후속에 "TTL 조정 시 `feedback.llm.timeout-seconds` 대비 하한을 함께 확인한다"를 추가했다(기동 검증은 `< 1`만 보므로 사람이 확인해야 한다는 점까지 명시 — 코드는 그대로 뒀다). **[참고 4]** `CryptoWatchLockConcurrencyIntegrationTest`의 `"락경합코인"` 리터럴 2곳(`setUp`·`isThisTestsInstrument`)을 `INSTRUMENT_NAME` 상수 하나로 묶었다 — 한쪽만 바뀌면 매처가 조용히 0건을 세어 단정이 무력해지기 때문이다. `FeedbackCryptoPropertiesTest`(8)·`FeedbackCryptoPropertiesYamlTest`(3)·`CryptoWatchLockTest`(8)·`CryptoPriceMoveWatcherTest`(22)·`CryptoWatchLockConcurrencyIntegrationTest`(Docker, 3) 전건(44건) 통과, `compileJava`·`compileTestJava`·`spotlessApply` 통과.
- 23:30 — PR #254 4라운드 리뷰(승인, 차단 0 / 권장 1 / 참고 3) 전부 반영. **[권장 1]** 3라운드에서 정상 경로를 `thenReturn(1L)`로 고정하면서 `deleted == null` 서브분기 커버가 사라진 것을 리뷰가 짚었다. 리뷰가 제안한 `thenReturn(null)` + `doesNotThrowAnyException` 형태로 먼저 만든 뒤 **가드(`deleted == null ||`)를 임시로 지우고 돌려보니 9건이 전부 그대로 통과했다** — 가드가 없으면 `deleted != 1L`에서 Long 언박싱 NPE가 나지만 그 NPE도 같은 메서드의 `catch(RuntimeException)`에 잡혀 삼켜져, 밖에서 본 동작이 "예외 없이 끝난다"로 동일하기 때문이다(맞는 구현과 틀린 구현이 같은 답을 내는 테스트). 두 경우를 가르는 유일한 외부 관찰점이 로그 메시지라, 이 저장소에 이미 있는 `DartDisclosureCollectorTest`의 `ListAppender` 헬퍼 방식을 그대로 가져와 `contains("지우지 못했다")`·`doesNotContain("Redis 장애")`·`getThrowableProxy()).isNull()` 세 가지를 함께 단정하도록 교체했다. **같은 프로브를 다시 돌려 이 테스트 하나만 실패(`failures="1"`)하는 것을 확인한 뒤 `git checkout`으로 원복했다.** **[참고 1]** `IRRELEVANT_WATCH_LOCK_TTL_SECONDS` 주석이 사실과 달랐다 — `tryLockReturnsTokenWhenSetIfAbsentSucceeds`가 TTL을 매처(`eq(Duration.ofSeconds(...))`)에 걸고 있어 "나머지는 TTL이 무엇이든 결과가 같다"가 성립하지 않았다. 그 테스트의 관심사가 토큰 반환이지 TTL이 아니므로 **매처를 `any(Duration.class)`로 푸는 쪽**을 택해 주석이 사실이 되게 했다(TTL 전달 검증은 전용 테스트가 값 5로 그대로 지킨다). **[참고 2]** PR 참조 표기를 "코드 주석은 유지, ADR은 최종 근거만"으로 통일 — `isThisTestsInstrument`에서 빠졌던 참조를 되살렸다. **[참고 3]** `context-notes.md:555`의 "PR #254(리뷰 3라운드 완료, 승인·머지 대기)"가 머지 순간부터 스테일해지는 서술이라 "구현은 PR #254에서 했다 — 라운드별 경위는 run-log.md가 정본"으로 시점 중립 서술로 바꿨다. `CryptoWatchLockTest`(9건) 통과, `gradlew build` 전체 게이트 통과(SHA `1174c76`).
- 00:41 — 이슈 #275 1번: 코인 집단 비교 배치 크론(`feedback.batch.crypto-peer-stats-cron` = `0 5 0 * * *`, §C-1)을 record `@DefaultValue`와 `application.yml` 양쪽에 추가. `@Scheduled`가 아직 없어 `spring.task.scheduling.pool.size`는 그대로 12이며 2번 항목이 스케줄 등록과 같은 커밋에서 13으로 올린다(§C-1 "풀 크기 = 그 시점 등록된 `@Scheduled` 수" 불변식). `compileJava` 통과. 드리프트 테스트(`FeedbackBatchPropertiesTest`)는 tester 몫으로 남겼다.
- 01:05 — 이슈 #275 2번: `PeerStatsBatchService.runCryptoPeerStatsBatch` 신설(`@Scheduled(cron = "${feedback.batch.crypto-peer-stats-cron}", zone = "Asia/Seoul")`). 대상은 `findByMarketAndOccurredAtBetween(CRYPTO, 전날 00:00, 전날 23:59:59.999999)`이고 `Between`이 양 끝을 포함하므로 상한을 다음 날 00:00이 아니라 마이크로초 하나 앞으로 뒀다 — `LocalTime.MAX`(나노초)는 `DATETIME(6)` 정밀도를 넘어 드라이버 반올림으로 경계가 조용히 어긋난다. 카드마다 `aggregateCard(card, occurredAt.toLocalDate(), occurredAt)`을 부른다(**서비스 날짜 = 배치 실행일이 아니라 카드 `occurred_at`의 날짜** — `CryptoPostSellFeedbackReader.buildPeerComparison`의 `card.windowEnd().toLocalDate()`와 같은 키). 재생세션 확인은 넣지 않았고 이유를 Javadoc에 남겼다. `pool.size` 12 → 13, `application.yml`·`application-crypto-real.yml`의 개수 계산 주석도 함께 갱신(신설 8 / 합계 13). `feedback-schedules-disabled-for-tests.yml`에 `crypto-peer-stats-cron: "-"`를 추가해 00:05에 걸친 전체 빌드가 공유 컨테이너에 행을 남기지 않게 했다. `compileJava`·`spotlessCheck` 통과, 스케줄 단정 테스트(`FeedbackBatchScheduleTest`)는 tester 몫으로 남겼다.
- 02:35 — 이슈 #275 6번: `ai/prd.md` §3의 FEED-007 행과 FEED-010·011 행 **근거 칸에만** 코인 지원 사실과 이슈 #275를 더했다. 판정은 둘 다 `완료` 그대로고 새 행은 만들지 않았다 — FEED-012는 새 요구사항이 아니라 기존 세 ID의 코인 차이를 규정한 절이라서다. 근거에는 "무엇이 열렸는지"를 적었다(FEED-007: 코인 체결이 400 → 200, KST 자정 게이트·`sameSessionCompleted` 항상 `true`·200분 초과 시 일봉 종가 극값과 `holdHighBasis`·매도일 일봉 종가 `atClose` / FEED-010·011: `runCryptoPeerStatsBatch`의 매일 00:05 확정 집계와 `service_date` = 카드 `occurred_at` 날짜, 반사실 `returnRate`의 코인 수수료율 `0.0005`). PR 번호는 아직 없어 이슈 번호로 적었다 — PR 생성 뒤 메인 세션이 맞춘다.
- 04:10 — 이슈 #275 코인 매도 회고 리뷰(리뷰 모드): **차단 3건 / 권장 3건 / 참고 2건 — 머지 불가.** 차단은 (1) `ai/api-routes.md` 66행이 여전히 "2차는 주식 전용 — 코인 체결은 400"이라 라우트 지도가 구현과 반대(CLAUDE.md 규칙 7), (2) `CryptoPostSellFeedbackReader.findHoldExtremes`(MINUTE 경로)·`highestCloseAfterSell`이 받은 1분봉을 구간으로 다시 거르지 않는데 실제 공급자(`BithumbRestCandleProvider`)는 `from`을 하한으로 강제하지 않고 `to`+`count`만 보내므로 빠진 분이 있으면 **매수 전·매도 이전 봉이 극값에 섞인다**(같은 클래스의 `dailyCandlesWithinHold`·`scenarioAtFirstMoveAfterBuy`는 거른다. `FakeCryptoCandleProvider`가 구간을 거르므로 테스트로는 잡히지 않는다), (3) `NarrativePromptBuilder.postSellPrompt`가 매수·매도·극값 시각을 `HH:mm`으로만 넘겨 **여러 날에 걸친 코인 보유**(§FEED-012 결정 4의 `DAILY` 경로가 곧 이 경우다)에서 서술이 같은 날 매매로 읽히거나 매도가 매수보다 이른 문장이 된다. 권장은 `sellDayClose` 중복 외부 호출, 읽기 트랜잭션 안의 외부 REST 최대 4~5회, 공급자 장애 시 조회 전체 실패(계약 오류 목록에 없는 코드). 산술 이동(`PostSellArithmetic`)은 값·scale·라운딩·부호가 주식과 동일해 회귀 없음, 배치 `service_date`(카드 `occurred_at` 날짜) ↔ 조회 키(`card.windowEnd().toLocalDate()`) 일치, `MAX_MINUTE_SPAN_MINUTES = 199`는 공급자 `count = 분차 + 1` 계산과 정확히 맞음, 일봉 표본은 매수일~매도 전날로 매도일이 섞이지 않음 — 지정된 중점 4자리는 모두 정상.
- 05:20 — 리뷰 차단 3건 + 권장 2건 반영. **[차단 1]** `ai/api-routes.md` 66행을 주식·코인으로 갈라 다시 썼다 — "코인 체결은 400"을 지우고 200·KST 자정 게이트·00:05 확정 배치·`holdHighBasis`를 적고 계약 상세는 `api-contracts.md`의 "코인 체결의 차이" 소절로 넘겼다(92행은 여전히 사실이라 그대로). **[차단 2]** `minuteCandlesWithin(symbol, from, to)`을 만들어 `findHoldExtremes`의 MINUTE 경로와 `highestCloseAfterSell`이 받은 봉을 구간으로 다시 거르게 했다. **공급자가 `from`을 하한으로 보내지 않는다는 것**(`BithumbRestCandleProvider.resolveCount`는 `to`+`count`만 만든다)을 코드로 확인하고 그 이유를 Javadoc에 남겼다 — 없으면 `dailyCandlesWithinHold`와 중복으로 보고 지운다. **[차단 3]** `PostSellPromptDto`의 매수·매도·극값 시각을 `LocalDateTime`으로 넓히고 `multiDayHold`·`holdHighBasis` 두 필드를 더했다. 하루를 넘긴 보유면 프롬프트·템플릿이 `M월 d일 HH:mm`으로 적고, `DAILY` 극값은 시각 대신 "그 일자의 종가"로 적는다(`23:59`는 일봉 라벨이라 재지 않은 시각을 단정하게 된다). **주식은 `multiDayHold`를 `sameSessionCompleted && 매수일 != 매도일`로 정해 언제나 거짓**이므로 문장이 한 글자도 달라지지 않는다 — 골든 마스터 테스트가 그대로 통과하는 것으로 확인했다. `spec.md` §FEED-012의 "LLM 경로는 시장과 무관하다" 문장도 "구조는 무관하되 시각 표기는 다르다"로 정정했다. **[권장 1]** `sellDayClose`를 `read`에서 한 번 구해 두 빌더에 넘겨 같은 일봉 REST 호출이 두 번 나가지 않게 했다(일봉은 `CachedCryptoCandleProvider`가 분봉만 캐시해 실제로 두 번 다 외부 호출이었다). **[권장 2]** 봉 조회를 `candles(...)`로 감싸 `MARKET_DATA_PROVIDER_ERROR`만 흡수하고 `WARN`을 남긴다 — 계약(실패 처리 표)이 "일봉을 못 받으면 값이 `null`이고 `status`는 `READY`"라고 적고 있는데 그대로 던지면 회고 200이 통째로 사라졌다. 다른 `ErrorCode`는 그대로 올린다. **범위 밖으로 둔 것** — 외부 REST를 읽기 트랜잭션 밖으로 빼는 구조 변경, 200일 초과 보유의 일봉 절단. 기존 테스트 4개 파일의 `PostSellPromptDto` 픽스처는 시그니처 변경에 맞춰 기계적으로 옮겼고(주식 기대 문자열은 그대로), `PostSellFeedbackServiceTest`의 `buyAt`·`sellAt` 단정 2건을 `LocalDateTime` 비교로 고쳤다. `gradlew build` 전체 통과(3307건).
- 06:05 — 수정 커밋 `5ad19e8d` 재리뷰: **차단 1건 / 권장 3건 / 참고 2건.** 앞선 차단 3건은 전부 닫혔다 — 라우트 66행이 주식·코인으로 갈려 코인에 대해 거짓인 문장이 남지 않았고, `minuteCandlesWithin`이 MINUTE 극값과 `highestCloseAfterSell` 양쪽에 실제로 걸렸으며(일봉·첫 카드 경로의 기존 필터도 그대로), `multiDayHold = sameSessionCompleted && 매수일 != 매도일`은 주식에서 `sameSessionCompleted=true`가 곧 `earliestBuySourceTradingDate == sellSourceTradingDate`를 뜻해 언제나 거짓이라 주식 문장이 바뀌지 않는다. `sellDayClose`의 `null`도 두 빌더가 `dayClosed`를 먼저 보고 빠져나가 "게이트 전"과 "일봉 미수신"이 섞이지 않는다. 남은 차단은 **새로 만든 네 분기(구간 재필터·`MARKET_DATA_PROVIDER_ERROR` 흡수·`multiDayHold` 날짜 표기·`DAILY` 종가 표기)에 테스트가 한 건도 없다는 것** — 픽스처 이관만 있고 신규 단정이 없어 차단 2·3이 되돌아가도 빌드가 통과한다. 권장은 같은 프롬프트의 카드 시각·첫 기사 시각이 여전히 `HH:mm`뿐(여러 날 보유에서 매수·매도만 날짜를 갖는 비대칭), 공급자 장애 흡수가 저장된 서술을 빈약한 채로 굳힐 수 있는 창, `ai/prd.md` 229행의 "코인 매도 회고는 3차로 미룬다" 잔존.
- 06:15 — 재리뷰 권장 2건 반영. **[권장 1]** 차단 3에서 넓힌 것 옆에 같은 종류가 남아 있었다 — `HeldPriceMoveDto.windowStart`·`windowEnd`와 `PostSellPromptDto.firstNewsAt`가 아직 `LocalTime`이라 여러 날 보유 코인에서 매수·매도 줄만 날짜를 갖고 카드·기사 줄은 시분만 남았다(프롬프트 마지막 지시 줄이 하필 "매수·매도 시각이 변동·기사와 어떤 순서였는지를 중심으로 써줘"다). 세 필드를 `LocalDateTime`으로 넓히고 카드 구간·근거 기사 줄도 `holdMoment`를 태웠다. `PostSellFeedbackService`의 `toLocalTime()` 축소 두 곳을 걷어냈고, **주식은 `multiDayHold`가 언제나 거짓이라 출력이 그대로**인 것을 골든 마스터(`NarrativePromptBuilderTest`의 spec 예시 완전 일치)가 수정 없이 통과하는 것으로 확인했다. **[권장 2]** `ai/prd.md` 229행의 "코인 매도 회고는 3차로 미룬다"에 3차에서 열었다는 사실과 근거(이슈 #275, spec §FEED-012의 KST 자정 게이트·일봉 종가·200분 경계·00:05 배치)를 덧붙이고 §3 구현 현황 행을 가리켰다. **범위 밖으로 둔 것** — 공급자 장애 흡수 시 `narrative_finalized` 미확정(서술 확정 정책 변경), 포맷터 중복 추출, `holdHighBasis` null 표기. `gradlew build` 전체 통과(3307건).
- 23:53 — 이슈 #282 1번: `PostSellFeedbackContextReader`(트랜잭션 A — 검증 + 배분 조회 + `Hibernate.initialize` 2회)와 `StockPostSellFeedbackReader`(주식 조립 전부를 값·순서 그대로 이관)를 신설하고 `PostSellFeedbackReader`를 `@Transactional` 없는 오케스트레이터로 줄였다. `compileJava`·`spotbugsMain` 통과. **기존 단위 테스트 5개 파일이 옛 7인자 생성자를 써 `compileTestJava`가 깨진 상태**이며, 그 분해는 tasks-282.md 1번이 tester에게 남긴 몫이다.
- 00:10 — 이슈 #282 2번: `CryptoPostSellFeedbackDbReader`(트랜잭션 B `findHeldPriceMoves` · C `buildPeerComparison`)를 신설하고 `CryptoPostSellFeedbackReader.read`를 `B → REST 4종 → C → 조립` 순서로 재배열했다(조회를 응답 생성자 인자에서 지역 변수로 끌어내 순서를 코드에 드러냈다). 계산·폴백 로직은 값 하나 바꾸지 않았고 기존 단위 18건이 그대로 통과한다. **DB 픽스처를 `CryptoPostSellFeedbackDbReaderTest`로 가르는 일은 tester 몫**으로 남겼다 — 지금은 리더 테스트가 실물 DbReader를 조립해 넘긴다.
- 00:21 — 이슈 #282 2번 테스트: `CryptoPostSellFeedbackDbReaderTest`(신설 9건 — 조회 구간 분 내림·카드 0건에서 기사 미조회·`windowStart` 파생·카드별 근거 분배·집단 비교 4판정과 조회 키가 카드 `occurred_at`의 KST 날짜라는 점·두 메서드의 `@Transactional(readOnly = true)`)를 만들고, `CryptoPostSellFeedbackReaderTest`의 실물 DbReader를 mock으로 갈아 끼워 **REST/극값 계산과 DB 조회를 두 파일로 갈랐다**(tasks-282.md 2번의 "한 파일에 섞여 남지 않게"). 리더 테스트에 순서 단정(`InOrder`: `findHeldPriceMoves` → 캔들 REST → `buildPeerComparison`)과 `@Transactional` 부재 단정을 더했다 — 순서가 뒤집혀도 값은 그대로라 기존 18건은 전부 초록이었다. 순서 단정이 실제로 붉어지는지는 `buildPeerComparison`을 REST 앞으로 옮겨 `VerificationInOrderFailure`를 재현해 확인하고 `md5sum` 대조로 구현을 원복했다. 20 + 9건 0실패, `spotlessCheck` 통과. **트랜잭션 활성 여부·lazy 초기화는 여전히 미검증**이며 3번 항목의 Testcontainers 통합 테스트 몫이다.
- 00:45 — 이슈 #282 3번: `CryptoPostSellFeedbackTransactionBoundaryIntegrationTest`(신설 1건, 클래스 `@Transactional` 없음)로 실제 MySQL에서 **캔들 REST 4종이 전부 트랜잭션 밖(`false`)이고 `priceMoves`·`peerComparison` 조회는 트랜잭션 안(`true`)임을 호출 순서까지** 고정했다 — 감시는 `CryptoCandleProvider` 데코레이터와 `CryptoPostSellFeedbackDbReader` 상속 빈이 `@TestConfiguration`에서만 맡고 운영 코드에는 스위치를 넣지 않았다(상속이어야 트랜잭션 프록시 **뒤에서** 관측된다). 응답 단정이 곧 lazy 초기화 회귀 검증이다. 픽스처가 커밋되므로 전용 연도(2034)·심볼(`TXB282`)을 쓰고 `@AfterEach`가 FK 역순으로 지운다. 통과가 진짜인지는 `PostSellFeedbackReader.read`에 `@Transactional`을 되살려 캔들 넷이 `[true,true,true,true]`로 red 되는 것을 보고 원복해 확인했다. 신설 1건 + 기존 `PostSellFeedback*IntegrationTest` 8개 파일 36건을 **수정 없이** 함께 돌려 37건 0실패.
- 00:50 — 이슈 #282 4번: `PostSellFeedbackReader` 클래스 Javadoc을 새 구조에 맞춰 다시 썼다 — "알려진 한계(코인 REST가 읽기 트랜잭션 안)" 문단을 걷어내고(이슈 체크리스트 3번째), "LLM 때문에 읽기를 별도 빈으로 뺐다"는 서술을 **무트랜잭션 오케스트레이터 두 겹**(서비스 → 리더)으로 고쳤으며, 조립·검증 순서 소유를 세 협력자 쪽으로 넘겼다. 코드 변경 없음. spec.md §FEED-012 결정 5의 빈 경계 표 5행(클래스명·트랜잭션 유무·`read(Trade, SellAllocationSummaryDto)` 시그니처·`Hibernate.initialize` 2회·B → REST 4종 → C 순서)을 구현과 1:1 대조해 어긋남 0을 확인했다. `ai/prd.md` §3·`api-routes.md`·`api-contracts.md`는 제공 기능·계약이 그대로라 **대상 아님**(CLAUDE.md 규칙 10의 갱신 비대상).

### 2026-08-07 — 이슈 #179 코인 뉴스 질의어·제목 필터 개정 (실측 근거)

**측정 방법은 재현 가능하나 원자료는 2026-08-07 스냅샷이다.** 덤프는 gitignore 대상이고 네이버는 최신순 100건만 주므로 지금 다시 받으면 다른 기사 집합이 온다 — 아래 숫자를 그대로 재현하려면 그 스냅샷이 필요하다. `tools/coin-news-measure/fetch.py`가 네이버를 12종목 × 100건씩 받아 **원문 그대로** 덤프하고, `CoinNewsFilterMeasurementTest`(외부 호출 없음, 덤프 없으면 건너뜀)가 그 덤프에 필터 후보를 적용한다. **분석은 반드시 `--rerun-tasks`로 돌린다** — 덤프가 test task의 입력이 아니라 Gradle이 `UP-TO-DATE`로 건너뛰고, `BUILD SUCCESSFUL`만 보면 이전 스냅샷 보고서를 새 결과로 읽게 된다(자세한 함정은 `tools/coin-news-measure/README.md`). 수집과 분석을 나눈 이유는 **후보마다 새로 받으면 그 사이 기사 목록이 바뀌어 무엇 때문에 숫자가 달라졌는지 구분되지 않기 때문이다.** 제목 손질(태그 제거 → 엔티티 해제 → 절단)은 측정기가 다시 구현하지 않고 운영 코드 `NaverNewsCollector.cleanTitle`을 그대로 부른다 — 그래서 그 메서드를 패키지 전용으로 넓혔다.

**기준선 (개정 전 규칙, 12종목 1,200건).** 제목 하나를 두 값으로만 본다 — `S`(자기 이름이 독립적으로 등장) / `O`(다른 종목명이 독립적으로 등장).

| 질의어 | 개정 전 통과 `!O` | 후보1 `S&&!O` | 후보2 `S\|\|!O` | **합본 `S`** |
|---|---:|---:|---:|---:|
| 이름만 | 739 | 138 | 771 | 170 |
| 개정 전 (`이름 + " 코인"`) | 680 | 115 | 710 | **145** |
| **개정 후 (`이름 + 심볼`)** | 735 | 220 | 776 | **261** |

**이슈의 전제가 뒤집혔다.** 이슈는 방향①(정상 기사 대량 누락)을 표제로 잡았는데 실측은 반대였다.

- 제외된 520건 중 자기 이름이 든 것은 **30건(6%)** 뿐이다 — 제외 규칙만 완화하는 후보2는 1,200건에서 30건을 살린다. **단 이 판정은 개정 전 규칙에 대해서만 참이다** (아래 "새 규칙의 누락" 참조).
- 반대로 개정 전 통과 680건 중 **565건(83%)의 제목에 그 코인 이름이 없었다**(`680 − 115` = 개정 전 통과 − 후보1, 위 표의 `S` 열 12종목 합계와 일치). 실제로 통과하던 제목 — `체인링크`에 `"두바이듀티프리 암호화폐 결제, 디르함 정산이 핵심"`, `비트코인캐시`에 `"260레인 CXL 스위치, 마벨 AI 메모리 병목 겨냥"`.
- `비트코인캐시`·`이더리움클래식`은 **각각 필터를 통과한 51건**(둘을 합쳐 102건) 중 자기 이름이 든 제목이 **0건**이었다.
- **`" 코인"` 접미가 오히려 해로웠다** — 일반 암호화폐 기사를 끌어와 이름만 쓰는 것(170)보다 낮은 145를 냈다. **종목별로는 7종목 하락·5종목 무변화이고 오른 종목은 없다** — "12종목에서 일관되게 나빠졌다"가 아니다.

**후보 1과 2가 서로 직교한다.** 바꾸는 칸이 겹치지 않아(후보1은 `S=F,O=F`, 후보2는 `S=T,O=T`) 합치면 규칙이 `통과 = S` 한 줄로 줄고, **접두 쌍 보호는 기존 구간 대조를 방향만 뒤집어 그대로 유지된다.**

**채택 — 심볼 질의어 + 합본 필터(`S`).** 261건이 남고 전부 제목에 그 코인 이름이 있다. 대가는 저장량 680 → 261건이고, `비트코인캐시`(3건)·`이더리움클래식`(4건)은 카드가 거의 생기지 않는다. **무관한 근거가 붙은 카드보다 카드가 없는 편이 낫다**고 판단했다 — 근거가 틀린 카드는 사용자를 적극적으로 오해시킨다.

**보류 — 별칭(심볼) 인정 (331건).** 위험은 `ETC`·`DOT`가 영어 일반어(`etc.`·`dot`)와 겹치는 것이고, 구간 대조가 단어 경계를 보지 않으므로 **위험 자체는 유효하다.** 다만 **이 표본에서는 관측되지 않았다** — 3,600건에서 영어 용법 히트 **0건**이고, 별칭으로만 추가되는 11건은 전부 정상 기사였다(`"DOT 토큰 폭락"`, `"ETC, 관세 쇼크 속 주말 랠리 주도"`). 관측된 오탐이 아니라 **관측되지 않은 잠재 위험**을 근거로 보류한 것이다. 회복분은 `이더리움클래식` 4 → 8건, `폴카닷` 15 → 22건 — 바로 위 "대가"가 지목한 종목들이라 후속 판단의 재료다. 채택 규칙 변경은 재측정·재리뷰가 필요해 이 PR에서는 바꾸지 않았다.

**새 규칙의 누락은 이 측정이 구조적으로 볼 수 없다.** 지표 `S`가 곧 채택 규칙이라 "`S`가 놓치는 정상 기사"는 정의상 어느 칸에도 잡히지 않는다. 리뷰가 실제 사례를 찾았다 — `이더리움클래식`이 **띄어쓰기**(`"이더리움 클래식(ETC)"`)로 3건을 놓치고, 그 3건이면 4 → 7건이 된다. 후속 후보로 남긴다.

**주식은 건드리지 않았다.** 이슈의 제외 범위이고 실측하지 않았다(C-005). `NewsTitleFilter`가 시장으로 갈라지며, `NewsTitleFilterTest`의 주식 3건이 개정 전과 같은 기대값으로 남아 있는 것이 "코인만 갈렸다"의 증거다.

**구현 후 재현 확인.** 같은 덤프에 운영 코드를 다시 돌려 세 질의 방식 전부 **운영 현재 = 합본 `S`** 로 일치했다(심볼 261 vs 261). 보고서가 이 대조를 매번 출력하므로, 코드가 문서와 다른 규칙을 돌리면 그 줄이 "어긋남"이 된다.

- 09:27 — PR #258 리뷰(로직 관점) 완료. 차단 0건 / 권장 4건 / 참고 1건 — 직교성·접두 보호·길이 가드·주식 불변·심볼 null 경로는 전부 성립, 남은 지적은 다중 등장 순회를 지키는 단정 부재(프로브로 재현)와 스테일 javadoc이다. 머지 가능.
- 09:38 — PR #258 리뷰 차단 3건·권장 문서분 반영(문서·javadoc·측정 도구만, 규칙 무변경). ①인용 오류 `535건(79%)` → **`565건(83%)`**(`680 − 115`, 보고서 `S` 열 합계로 검산) ②별칭 기각 근거를 "관측된 오탐"에서 **"이 표본에서는 관측되지 않은 잠재 위험"**으로 정정하고 회복분(이더리움클래식 4→8·폴카닷 15→22)을 기록 ③재현 명령에 **`--rerun-tasks`**를 넣어 UP-TO-DATE 무음 스킵을 막음(`build.gradle` 입력 배선은 전역 test task를 오염시켜 채택하지 않음, 근거는 신설 `tools/coin-news-measure/README.md`). 함께 "51건은 두 종목 각각(합 102)·저장이 아니라 필터 통과", 원자료는 2026-08-07 스냅샷, 접미 효과는 7종목 하락·5종목 무변화, 새 규칙의 누락은 이 측정이 구조적으로 못 본다(이더리움클래식 띄어쓰기 3건, 4→7)를 명시. `NewsTitleFilter`·`NewsCollector` javadoc을 시장별 규칙으로 재작성, `fetch.py`가 `link`·`pubDate`를 덤프하고 부분 덤프에 `-partial` 접미·종목 수·수집 시각을 남긴다. `compileJava` 통과.

### 2026-08-08 — 이슈 #273 코인 뉴스 0건의 원인 판정 (수집 결함이 아니었다)

**판정 — 코인 수집에는 결함이 없다. 로컬이 애초에 네이버를 부르지 않았다.**

이슈는 "같은 배치가 도는 주식은 정상 적재되는데 코인만 0건"을 전제로 후보 넷(검색어·교차 필터·수집기 분기·저장)을 남겼다. **전제가 사실이 아니었다.** 근거는 로컬 DB 실측이다.

- `market_news_items` 192건이 **전부 같은 시각 `2026-08-04 12:20:52.899050`** 에 들어왔고 그 뒤로 한 건도 없다. 종목당 정확히 12건씩 16종목 균일이다 — 실수집 분포가 아니다.
- URL이 `https://news.example.test/198/{종목}/{n}` 이다. **이슈 #198(LLM 응답 시간 실측)의 QA 픽스처**이지 수집 결과가 아니다.
- 즉 이 DB에서 **수집 배치가 저장에 성공한 적은 한 번도 없다.** 주식이 되고 코인이 안 된 것이 아니라 **둘 다 0건**이고, 주식만 픽스처가 남아 있었다.

원인은 수집기 선택이 **키 유무가 아니라 프로필**이라는 데 있다. `local,oauth-real,crypto-real`은 `!prod`라 `FakeNewsCollector`가 등록되고, 그 구현은 계약대로 항상 빈 목록을 준다. `.env`에 네이버 키가 채워져 있어도 **호출 자체가 일어나지 않는다.**

**후보 넷은 실측으로 전부 배제했다** (2026-08-08, 운영 코드 그대로 · 실제 네이버 호출).

`python tools/coin-news-measure/fetch.py --symbol-suffix` → 12종목 **각 100건, 합 1,200건 수신**. 이어서 `gradlew test --tests CoinNewsFilterMeasurementTest --rerun-tasks`의 `운영 현재` 열이 **275건**(합본 `S`와 건별 전건 일치, 어긋남 0). 2026-08-07 스냅샷의 261건과 같은 자릿수다.

| 후보 | 판정 | 근거 |
|---|---|---|
| 검색 호출 자체 | 아니다 | 12/12 종목 HTTP 200, 각 100건 |
| 검색어(`이름 + 심볼`) | 아니다 | 같은 호출이 1,200건을 돌려준다 |
| 교차 필터 | 아니다 | 운영 `NewsTitleFilter`가 275건 통과시킨다 |
| 저장 단계 | 도달하지 않는다 | 수집기가 빈 목록이라 `save`가 `collected.isEmpty()`에서 즉시 반환한다 |

**고친 것 — `news-real` 프로필을 열었다.** `NaverNewsCollector`가 `@Profile({"prod", "news-real"})`, `FakeNewsCollector`가 `@Profile("!prod & !news-real")`이다. `crypto-real`·`oauth-real`과 같은 방식이고, 그 둘이 있는데 뉴스만 없어서 **로컬에서 실기사를 볼 방법이 아예 없었다.** 기본 로컬·테스트는 그대로 Fake라 "키 없이 기동·`./gradlew build` 정상"(§실패 처리)은 유지된다.

**공시(DART)는 바꾸지 않았다.** 이슈의 제외 범위다 — `news-real`을 켜도 로컬 공시는 `FakeDisclosureCollector`다.

**확인 — 로컬 실적재 성공.** `SPRING_PROFILES_ACTIVE=local,news-real`로 기동해 수집 크론을 1분으로 낮춰 한 번 돌렸다(2026-08-08 23:21). `뉴스 수집 완료 (신규 저장 1654건)` 로그와 함께 `market_news_items`에 **코인 275건**이 들어왔다 — 위 측정의 275건과 정확히 같다(주식은 픽스처 192 → 1,571건). 이슈의 완료 조건 "코인 종목 한 건 이상 실제 적재"를 충족한다.

**원장 불변은 단정하지 않는다.** 검증 기동 중 같은 로컬 DB에서 사람이 프론트로 거래하고 있었다(`orders` 6 → 7 — 23:20:54 `MARKET SELL`, `trades` 3 → 5). 수집과 무관한 사용자 주문이지만 **앱 인스턴스가 둘 동시에 붙은 상태**였으므로 행 수 대조를 근거로 쓰지 않는다. 대신 경로로 말한다 — `NewsCollectionService`가 주입받는 리포지터리는 `MarketNewsItemRepository` 하나뿐이고 `instruments`는 `InstrumentService`로 읽기만 한다. **원장에 닿는 경로가 애초에 없다**(§C-6).

- (문서) — 이슈 #285 tasks-285.md 5번(문서 동기화). 다른 작업 항목(1~4, `NewsCollectionService.collectForInstrument`·`CryptoPriceMoveWatcher` 통합·단위/통합 테스트)이 아직 병렬 진행 중이라 코드는 실재하지 않는다 — ADR-0017 §결정에 적힌 설계만 근거로 삼았다. spec.md 4곳을 갱신했다. ①§탐지 알고리즘(코인) 의사코드의 "근거 기사 0건이면 종료" 줄에 온디맨드 수집·1회 재매칭을 끼워 넣었다. ②§뉴스 매칭 범위의 "코인 근거창에 `match-after-minutes`를 더하지 않는 이유" 문단에 온디맨드 재매칭이 이제 먼저 시도된다는 문장을 추가하고, "다음 수집 때 근거가 들어와도 되살리지 않는다"는 그대로 두되 "온디맨드 재매칭까지 실패한 이후의 이야기"임을 명시했다. ③§실패 처리 표의 "근거 기사 0건" 행에 코인 분기를 덧붙이고 "코인 온디맨드 수집 실패" 행을 새로 추가했다(기존 종목별 `try/catch` 격리를 그대로 재사용한다는 서술, 새 메커니즘 없음). ④FEED-005 요구사항에 온디맨드 수집·재매칭 문장을 덧붙였다. `ai/api-routes.md`·`docs/api-contracts.md`는 컨트롤러 변경이 없어 대상 아니다(이슈 본문 명시). `ai/prd.md` §3 197행 "AI 피드백 — 코인 변동 감시" 행은 **판정(완료)은 그대로 두고 근거 칸에만** 이슈 #285와 온디맨드 수집 사실을 추가했다 — PR이 아직 없어 "PR 생성 후 번호 갱신 필요"로 임시 기재했다(#225 tasks 6번·#275 tasks 6번과 같은 관행). 코드 변경이 없어 `./gradlew.bat compileJava`는 실행하지 않았다.

- (implementer) — 이슈 #285 tasks-285.md 1번. `NewsCollectionService.collectForInstrument(Instrument)`를 지시된 시그니처 그대로 신설하고(같은 시장 종목명만 매번 재조회, 기존 `save()` 재사용, 타입은 `NEWS` 고정), Javadoc에 ADR-0017 §결정 2를 인용했다. `NewsCollectionServiceTest`에 단위 테스트 4건(같은 시장 종목명 인자 검증·`createdAt`=수집 시각·중복 URL 무저장·반환값=저장 건수)을 추가했고, 기존 `holdsNoRepositoryOtherThanMarketNewsItemRepository`는 새 리포지토리 주입이 없어 그대로 통과한다. `compileJava compileTestJava` BUILD SUCCESSFUL.
- (implementer) — 이슈 #285 tasks-285.md 2번. `CryptoPriceMoveWatcher`에 `newsCollectionService` 필드를 `newsMatcher` 바로 다음에 추가하고, `watchOne()`의 첫 매칭이 비면 `collectForInstrument` 호출 후 1회만 재매칭하도록 고쳤다(재매칭도 비면 기존대로 `return false`, 첫 매칭에 근거가 있으면 수집 호출 없음). 락 범위는 그대로 두고(기존 `try/finally` 밖으로 코드를 옮기지 않음) 111행 근방 의사코드 주석·155행 근방 주석·클래스 상단 Javadoc을 ADR-0017 인용으로 갱신했다. 생성자 시그니처가 바뀌어 `CryptoPriceMoveWatcherTest`의 기존 `new CryptoPriceMoveWatcher(...)` 호출부는 3번 항목이 처리할 몫이라 그대로 두었다(현재 `compileTestJava`는 실패 상태). `compileJava` BUILD SUCCESSFUL.
- (implementer) — 이슈 #285 tasks-285.md 3번. `CryptoPriceMoveWatcherTest`의 `watcher(...)` 팩터리에 `newsCollectionService` mock을 끼워 넣어 `compileTestJava` 실패를 해소했다(2번이 바꾼 생성자 시그니처 반영). 같은 원인으로 깨져 있던 `CryptoWatchLockConcurrencyIntegrationTest`(별도 파일, 실제 `NewsCollectionService` 빈을 새로 `@Autowired`해 배선)도 함께 고쳐야 `compileTestJava` 전체가 통과해 같이 손댔다 — 작업 항목 4가 다루는 `CryptoPriceMoveWatcherIntegrationTest`는 손대지 않았다. 새 `@Nested OnDemandCollection` 클래스로 "첫 매칭 비면 수집 후 재매칭해 카드 생성"·"재매칭도 비면 수집·재매칭 각 1회만, 카드 미생성" 2건을 추가하고, `LockReleaseGuarantee`에 "온디맨드 수집이 예외를 던져도 unlock" 1건을, 게이트 4종 테스트(`ZScoreThreshold`·`Cooldown`·`DailyLimit`·`WatchLockAcquisitionFailure`)와 `Evidence.persistsTheMatchedSourcesAsIsWhenEvidenceExists`에 `verify(newsCollectionService, never()).collectForInstrument`를 한 줄씩 추가했다. `compileTestJava` BUILD SUCCESSFUL, `test --tests CryptoPriceMoveWatcherTest` 전 nested class(13개) 통과·회귀 없음.
- (implementer) — 이슈 #285 tasks-285.md 4번. `CryptoPriceMoveWatcherIntegrationTest`에 `@MockitoBean NewsCollector`를 추가하고(`NewsCollectionIntegrationTest` 선례), 신규 테스트 5건을 더했다 — ①근거 사전 미저장 상태에서 온디맨드 수집이 `market_news_items`에 실제로 써 카드 1건이 생성(성공률 상승, 완료 조건 핵심) ②수집 후에도 0건이면 카드 미생성 + `collect` 호출은 있었음을 `verify` ③`watch()` 온디맨드 저장 후 `collectNews()`(30분 배치)를 이어 호출해도 같은 URL 행이 1건 그대로(중복 방지) ④코인 종목 2개 중 하나만 `collect`가 예외를 던져도 `watch()`가 예외 없이 끝나고 건강한 종목의 카드는 그대로(종목별 실패 격리) ⑤온디맨드 수집이 `market_news_items`에 실제로 쓰는 시나리오에서도 원장 6테이블 불변. 기존 `neverWritesOutsideThePriceMoveTables`는 지시대로 손대지 않았다. `CryptoPriceMoveWatcher.java`는 항목 2가 이미 끝내 놓은 상태라 건드리지 않았다. `compileJava`·`compileTestJava` 둘 다 BUILD SUCCESSFUL(테스트 실행은 tester 담당).
- (implementer) — 이슈 #285 후속. `./gradlew build` 전체 실행 시 `NewsCollectionScheduleTest.noOtherSourceFileTriggersCollection()`이 `CryptoPriceMoveWatcher.java`를 걸러내 실패해, 스캔에서 이 파일만 명시적으로 제외하고 예외 기준(재생 경로 아님 + 실시간 시각 사용, ADR-0017)을 테스트 주석에 남겼다. `test --tests NewsCollectionScheduleTest` BUILD SUCCESSFUL.
- (리뷰) — PR #290(#285) 최종 상태 리뷰: 차단 0건, 권장 0건, 참고 1건(설계 판단 확인 요청, 코드 결함 아님) — 머지 가능.
- (이슈 #386) — 항목 7 통합 테스트(`PostSellFeedbackJournalIntegrationTest` 7건, 실 MySQL) 통과. 단위 테스트가 리포지터리를 mock으로 두어 비어 있던 자리 — `journal_fingerprint`·`journal_regenerations`의 실제 왕복 — 을 이 파일이 `jdbcTemplate`으로 직접 읽어 메웠다. 인젝션 케이스는 실제 모델을 부를 수 없어 "후검증이 권유 문장을 템플릿으로 떨어뜨린다"까지만 고정하며, 모델이 지시문에 흔들리는지는 §튜닝의 `TEMPLATE` 비율 관측 몫이다.
- (implementer) — 이슈 #570 tasks-570.md 1번. 기존 `FeedbackBatchProperties`·`application.yml`·`FeedbackBatchLock`을 spec/ADR 및 `RedisLock` 계약과 대조했고 추가 production 변경은 불필요했다. `.\gradlew.bat compileJava` BUILD SUCCESSFUL; 테스트는 지시대로 실행하지 않았다.
- (구현·검증) — 이슈 #570. `FeedbackBatchLock`과 공통 TTL 3600초를 추가하고 뉴스·공시·개장 전·주식 집단통계·코인 집단통계·코인 요약/브리핑의 6개 예약 진입점에 `scheduled` 고정 키와 fail-closed 처리를 적용했다. 락 범위는 종목/카드 조회부터 외부 호출·저장·계측 종료까지이며, 획득 후 예외에도 토큰을 `finally`에서 해제한다. 대상 단위 테스트와 신규 `PeerStatsBatchServiceTest` 통과, `compileJava compileTestJava spotlessJavaCheck spotbugsMain spotbugsTest` 통과. `FeedbackBatchLockIntegrationTest`와 기존 Testcontainers 통합 회귀 테스트는 Docker Desktop Linux 엔진 미기동으로 로컬 실행 불가 상태라 CI에서 재확인해야 한다.
- (리뷰 반영) — PR #571의 차단 2건·권장 3건을 반영했다. 개장 전 배치도 `FeedbackBatchLock.SCHEDULED_SCOPE`를 사용하도록 키 계약을 통일했고, 실제 Redis Testcontainers와 네 서비스 객체를 두 스레드에서 호출하는 `FeedbackBatchConcurrencyIntegrationTest`를 추가했다. 설정 테스트에 공통 TTL의 기본값·YAML 바인딩·덮어쓰기·양수 검증을 추가했으며, #568 적용 후 pre-market 최근 7일 최댓값/3600초 비율(0.8 경고, 1.0 초과 차단)을 운영 측정 기준으로 문서화했다.
