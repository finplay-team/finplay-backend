HELP.md
.gradle
build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/main/**/build/
!**/src/test/**/build/

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache
bin/
!**/src/main/**/bin/
!**/src/test/**/bin/

### IntelliJ IDEA ###
.idea
*.iws
*.iml
*.ipr
out/
!**/src/main/**/out/
!**/src/test/**/out/

### NetBeans ###
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/

### VS Code ###
.vscode/

### Local env ###
.env

### Serena (도구 캐시) ###
.serena/

### graphify (코드 지식 그래프 산출물 — graph.json이 20MB대라 커밋하지 않는다) ###
graphify-out/

### 배포 스택 (nginx가 서빙할 프론트 빌드 산출물 — finplay-frontend에서 복사해 온다) ###
/frontend-dist/
/frontend-dist-blue/
/frontend-dist-green/

### 코인 뉴스 측정 산출물 (이슈 #179)
tools/coin-news-measure/out/

### JVM 치명 오류 덤프 (워커 크래시 시 저장소 루트에 생긴다 — PATH·USERNAME 등 로컬 환경이 통째로 들어 있다)
hs_err_pid*.log
replay_pid*.log

### 커뮤니티 게시물 이미지 로컬 저장 디렉터리 (COM-006, 이슈 #248)
/data/

### Terraform (infra/terraform/, ADR-0030) — state에 DB 비밀번호가 평문으로 남는다.
### .terraform.lock.hcl은 커밋 대상이다(provider 버전 고정 — package-lock.json과 같은 역할).
infra/terraform/.terraform/
infra/terraform/*.tfstate
infra/terraform/*.tfstate.*
infra/terraform/terraform.tfvars
infra/terraform/*.tfplan
infra/terraform/tfplan
